# v0.6.68 Go Detached Leader Binding Cutover

Result: v0.6.68 cuts over the `tmux/windows.ts` detached `ensureSwarmWindow()` leader target fallback from direct TypeScript target-based `display-message` to the existing Go-backed `resolvePaneBindingAsync(leaderPaneId, signal)` path.

This slice removes only the final target-based leader-pane window id fallback after pane setup. The direct pane setup `list-panes` step is superseded by v0.6.69; post-creation window lookup, session/window creation, labels, mutating lifecycle, state, task/report/PlanRun, UI, and release/package behavior remain out of scope:

```text
tmux/windows.ts ensureSwarmWindow() detached branch
  -> firstPaneInWindow(initialTarget, signal) selects leaderPaneId by v0.6.69
  -> resolvePaneBindingAsync(leaderPaneId, signal)
  -> workerLifecycle.inspectPane
  -> compact target from Go-backed binding
```

## Runtime Behavior

- `tmux/windows.ts` keeps `const binding = await resolvePaneBindingAsync(leaderPaneId, signal)` and uses `binding?.target` as the sole detached leader target source after pane setup.
- The direct TypeScript `runTmuxAsync(['display-message', '-p', '-t', leaderPaneId, '#{window_id}'], undefined, signal)` fallback is removed from the detached branch.
- If `resolvePaneBindingAsync(leaderPaneId, signal)` cannot provide a target, `ensureSwarmWindow()` throws compact `Error('Failed to resolve agentteam leader pane binding')`.
- Successful detached behavior still returns `{ session, window, target, leaderPaneId }` from the Go-backed binding target.
- Raw stdout, raw stderr, cwd, stack, helper paths, mailbox/report bodies, worker transcript bodies, and full helper diagnostics are not exposed.

## Preserved Boundaries

- Pane setup `runTmuxAsync(['list-panes', '-t', initialTarget, '-F', '#{pane_id}'], undefined, signal)` is superseded by the v0.6.69 `firstPaneInWindow(initialTarget, signal)` cutover.
- Post-creation `list-windows -F '#{window_id}\t#{window_name}'`, `new-session`, `new-window`, marking, labels, kill, state/task/UI/release/package remain TypeScript-owned.
- `markWindowAsAgentTeam()` and `refreshWindowPaneLabels()` remain TypeScript-owned.
- `createTeammatePane()`, pane splitting, pane labels, `wakePane()`, and `killPane()` remain TypeScript-owned.
- The inside-tmux current binding fallback remains the v0.6.67 `captureCurrentPaneBinding()` reuse cutover.
- `sessionExists()` remains the v0.6.66 Go-backed session existence facade.
- `findAgentTeamWindowTarget()` remains the v0.6.65 Go-backed marked-window discovery facade.
- The native helper path and binary name remain unchanged.

No Go source or native artifact rebuild is required for this slice. `package.json` remains `0.6.8`.

## Guard Evidence

The v0.6.68 guard suite verifies:

- `ensureSwarmWindow()` detached leader target resolution uses `resolvePaneBindingAsync(leaderPaneId, signal)`.
- the direct TypeScript target-based `display-message -p -t leaderPaneId '#{window_id}'` fallback is removed.
- missing leader binding throws only the compact `Failed to resolve agentteam leader pane binding` error.
- no hidden TypeScript target-based `display-message` fallback remains for the same behavior.
- pane setup `list-panes` is superseded by v0.6.69, while post-creation `list-windows`, session/window creation, marking, and label refresh remain TypeScript-owned.
- `resolvePaneBindingAsync()` remains Go-backed through `workerLifecycle.inspectPane` and compact `target`.
- Go source and existing native artifact files are unchanged by this slice.
- package/native path/name/version/release guardrails hold.

Evidence files:

- `tests/fixtures/kernel/v0668/goDetachedLeaderBindingCutover.cjs`
- `tests/suites/go-kernel-v0668-go-detached-leader-binding-cutover.cjs`
