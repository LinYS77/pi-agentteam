# v0.4.22 Native Helper Package Metadata Checkpoint

> Scope: Slice 7 final GitHub-only native helper package metadata checkpoint review. This summarizes Slice 1-6 evidence for metadata-owner dry-run work. It does not implement npm/default/native cutover, change `package.json`, add real package metadata, add `optionalDependencies`, add lifecycle hooks, add helper build/download/install scripts, add package scripts for helper release, add lockfiles, add `go.mod`/`go.sum`, add checked-in native binaries, add tarballs, add generated manifests/artifacts, include `kernel/` in package contents, run `npm version`, run `npm publish`, commit, tag, push, make Go default, change current `go-cutover`, or delete the TypeScript parser fallback.

## Decision

Final v0.4.22 checkpoint recommendation:

- GO only for GitHub-only v0.4.22 metadata-owner dry-run checkpoint after leader approval.
- STOP for npm/default/native cutover.
- STOP for real package inclusion or native package publication.
- STOP for `package.json` metadata or version changes.
- STOP for `optionalDependencies` or native companion dependency declarations.
- STOP for lifecycle hooks, helper downloads, helper build/install scripts, or install-time `go build`.
- STOP for lockfiles, Go modules, checked-in native artifacts, tarballs, generated manifests, or generated package artifacts.
- STOP for treating `go-packaged-preview`, metadata fixtures, dry-run fixtures, or manifest guards as normal-user availability proof.
- STOP for default Go enablement.
- STOP for current `go-cutover` behavior changes.
- STOP for TypeScript parser fallback deletion.

This checkpoint says the v0.4.22 metadata owner boundary, companion package metadata fixtures, package dry-run simulation, manifest compatibility guard, preview/runtime invariants, and package/native guardrails are ready to record in GitHub history after leader approval. It does not approve npm release behavior, real package inclusion, default/native runtime behavior, or fallback removal.

## Context and Links

Prior checkpoint:

- v0.4.21 runtime availability checkpoint: `docs/perf/v0.4.21-go-runtime-availability-checkpoint.md`

v0.4.22 artifacts:

- Slice 1-6 owner boundary and requirements doc: `docs/perf/v0.4.22-native-helper-package-metadata.md`
- Slice 1 docs guard: `tests/suites/go-kernel-v0422-native-package-metadata-docs.cjs`
- Slice 2 companion package metadata fixtures: `tests/suites/go-kernel-v0422-native-package-metadata-fixtures.cjs`
- Slice 3 package dry-run simulation: `tests/suites/go-kernel-v0422-native-package-dry-run.cjs`
- Slice 4 manifest compatibility/provenance/license/checksum guard: `tests/suites/go-kernel-v0422-manifest-compatibility-guard.cjs`
- Slice 5 packaged preview/runtime invariants: `tests/suites/go-kernel-v0422-packaged-preview-invariants.cjs`
- Slice 6 package/native guardrails: `tests/suites/go-kernel-v0422-package-native-guardrails.cjs`
- Slice 7 checkpoint guard: `tests/suites/go-kernel-v0422-native-package-metadata-checkpoint-docs.cjs`

## Slice 1-6 Summary

| Slice | Result | Checkpoint implication |
| --- | --- | --- |
| Slice 1 metadata owner decision | `docs/perf/v0.4.22-native-helper-package-metadata.md` defines the owner boundary for future metadata schema and dry-run fixture planning while keeping package publication and resolver defaulting out of scope. | v0.4.22 can document metadata ownership, but source package/runtime behavior remains unchanged. |
| Slice 2 companion package metadata fixtures | `tests/suites/go-kernel-v0422-native-package-metadata-fixtures.cjs` validates temp/generated companion package metadata rows for Linux, Darwin, and Windows sample platform tuples. | Future package metadata shape is exercised only as test fixture data, not as real package metadata. |
| Slice 3 package dry-run owner simulation | `tests/suites/go-kernel-v0422-native-package-dry-run.cjs` simulates exact package contents for POSIX and Windows helper packages under temp roots and verifies cleanup. | Package dry-run evidence exists without running npm publish/version or writing tarballs into the repo. |
| Slice 4 manifest compatibility guard | `tests/suites/go-kernel-v0422-manifest-compatibility-guard.cjs` validates manifest compatibility, provenance, license metadata, checksum, executable bit, platform tuple, and rejection cases using temp fixtures. | Future manifest skew rules are documented and tested conceptually without changing the runtime resolver. |
| Slice 5 packaged preview/runtime invariants | `tests/suites/go-kernel-v0422-packaged-preview-invariants.cjs` proves metadata-owner work does not change default, preview, cutover, read-model, fail-closed, or `/team` behavior. | `go-packaged-preview` remains explicit-only/non-default and `go-cutover` remains explicit helper-path based. |
| Slice 6 package/native guardrails | `tests/suites/go-kernel-v0422-package-native-guardrails.cjs` distinguishes allowed docs/tests fixture strings from forbidden source repo/package metadata changes and scans package/native hazards. | Temp fixture definitions are not real package inclusion, and package/native state remains unchanged. |

## Runtime and Package State

Runtime behavior remains unchanged:

- default/unset remains disabled/TypeScript.
- disabled, typescript, go, and auto behavior remains unchanged.
- current `go-cutover` remains explicit/local-only and helper-path based.
- `go-packaged-preview` remains explicit-only, non-default, and not normal-user availability proof.
- packaged helper discovery does not run in default, disabled, typescript, go, auto, or current `go-cutover`.
- `tmuxSnapshotParse` is the only cutover-owned module.
- `compactReadModelFingerprint` remains TypeScript fallback / non-cutover.
- runtime `/team` stays quiet; no helper path, packaged helper env, cutover reason, stack trace, package metadata, fixture text, manifest body, or preview branch is rendered in panel/runtime UI surfaces.
- TypeScript/pi remains the control plane for tmux capture, worker lifecycle, state/repository, task/report governance, PlanRun, full-text boundaries, package metadata, and release operations.

Package/native behavior remains unchanged:

- `package.json` version remains `0.6.8`.
- `package.json#files` excludes `kernel/` and native/helper/generated artifact paths.
- no `optionalDependencies`.
- no native companion package metadata in the main package.
- no lifecycle hooks: `preinstall`, `install`, `postinstall`, `prepare`, `prepublish`, `prepublishOnly`, `publish`, or `postpublish`.
- no helper build/install/download/package/version/publish scripts.
- no `package-lock.json` or `npm-shrinkwrap.json`.
- no root or helper `go.mod`.
- no root or helper `go.sum`.
- no checked-in `.exe`, `.dll`, `.so`, `.dylib`, helper binary, native artifact, generated manifest artifact, generated package artifact, or tarball.
- no native package fixture outside allowed docs/tests fixture sources.
- no `kernel/` package inclusion.
- no `npm version`, `npm publish`, commit, tag, or push is part of this checkpoint.

## Evidence Gathered

Evidence added by v0.4.22:

- docs/reference guard verifies the metadata owner decision, v0.4.21 prerequisite links, docs/tests-only scope, package/native STOP gates, and unchanged package/runtime facts.
- companion package metadata fixture tests validate package name/version/license/os/cpu/files/helper manifest tuple shape and reject scripts, dependencies, optional dependencies, lifecycle hooks, raw `kernel/` paths, and helper build/download/install metadata.
- package dry-run simulation tests validate exact POSIX and Windows file lists, manifest/helper placeholder presence, no package tarballs, cleanup, no source checkout artifacts, and no package publication/versioning behavior.
- manifest compatibility guard validates schema version, package version, helper version, protocol version, module, platform/libc, filename, size, sha256, executable bit, provenance, license metadata, and fail-closed skew mapping vocabulary.
- packaged preview invariant tests prove default/unset remains disabled/TypeScript, current `go-cutover` does not discover packaged helpers, explicit helper precedence is unchanged, preview/cutover failures remain fail closed without TypeScript parser fallback callback or migration fallback metadata, `compactReadModelFingerprint` remains non-cutover, and runtime `/team` remains quiet.
- package/native guardrails scan the real repo and main package while allowing only docs and JS test suites to contain temp fixture definitions/placeholder strings.
- package/native sanity confirms package version `0.6.8`, no package metadata changes, no optionalDependencies, no lifecycle hooks, no helper scripts, no locks, no Go modules, no native artifacts, and no `kernel/` package inclusion.

## Remaining Blockers Before Real Native Package Metadata or Default Cutover

STOP future real native package metadata, npm package inclusion, default/native cutover, or fallback removal until later explicit owner slices resolve all of these blockers:

- real package metadata owner slice approves exact main-package and/or companion-package metadata changes with rollback plan.
- package release owner accepts package naming, versioning, provenance, attestation, checksum, license, and supported platform matrix responsibilities.
- generated native helper artifacts exist for the supported OS/arch/libc matrix with executable-bit validation and checksum manifests.
- clean install smokes pass across supported platforms without a Go toolchain, source checkout, manual helper env, lifecycle download, install-time build, or hidden network fetch.
- unsupported-platform behavior and remediation text are accepted for every AgentTeam-supported platform.
- compact diagnostics UX is implemented and no-leak guarded before any normal-user default/native fail-closed path surfaces errors.
- resolver defaulting and packaged helper discovery are separately approved and guarded for no path leaks, no stdout/stderr leaks, no stack traces, and no package internals in `/team`.
- rollback story covers bad package metadata, bad helper artifact, checksum/provenance mismatch, unsupported platform, and package unpublish/deprecation scenarios.
- parser failure policy proves no pane loss, false empty snapshots, force reconcile, kill panes, worker lifecycle mutation, or stale success.
- TypeScript parser fallback deletion remains blocked until native/default availability and diagnostics are separately proven.
- `compactReadModelFingerprint` remains non-cutover unless a later approved slice solves its own availability and fallback policy.
- Go helper authority remains parser-only/stdin-stdout `tmuxSnapshotParse` and does not gain tmux execution/capture, state/repository writes, network/listeners, worker lifecycle, PlanRun/governance, full-text access, package/release authority, or control-plane behavior.

## Validation Matrix

Required final validation for this checkpoint:

- `node tests/run.cjs`
- `npm run typecheck`
- `npm run -s check:boundaries`
- `git diff --check`
- `npm run --silent bench:team-panel-tmux`
- `PI_AGENTTEAM_KERNEL=go-packaged-preview npm run --silent bench:team-panel-tmux`
- package/native sanity scan

Expected validation posture:

- default bench metadata stays TypeScript/default with no Go enablement.
- preview bench metadata reports `requestedMode:"go-packaged-preview"`, remains TypeScript when no helper is packaged/injected, keeps `fallbacks:0`, and does not run read-model shadow.
- package/native sanity remains unchanged from the v0.4.21 package policy and v0.4.22 package/native guardrails.
- focused v0.4.22 guard suites pass without creating repo artifacts.

## Final Recommendation

Proceed only with a GitHub-only v0.4.22 metadata-owner dry-run checkpoint after leader approval.

Do not proceed with npm/default/native cutover. Do not add real package inclusion or native package publication. Do not change `package.json` metadata or version. Do not add `optionalDependencies`, lifecycle hooks, helper download/build/install scripts, lockfiles, Go modules, native binaries, tarballs, generated manifests, or generated package artifacts. Do not treat preview/dry-run fixtures as normal-user availability proof. Do not make Go default. Do not change current `go-cutover`. Do not delete the TypeScript parser fallback. Do not commit, tag, or push as part of this checkpoint.
