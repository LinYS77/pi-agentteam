# v0.6.85 Go Kill-Pane Gate

Result: v0.6.85 defines the destructive `tmux/panes.ts killPane(paneId)` Go cutover gate without implementing runtime mutation.

This is gate-only evidence for exactly one future candidate: replacing `runTmuxNoThrow(['kill-pane', '-t', paneId])` in `tmux/panes.ts killPane(paneId)` with a future `workerLifecycle.killPane` helper operation.

No Go handler, TypeScript adapter method, `tmux/panes.ts` cutover, native helper rebuild, or package/release action is made in this slice.

## Current TypeScript-Owned Behavior

`killPane(paneId)` remains TypeScript-owned, no-throw, and `void`:

```ts
export function killPane(paneId: string): void {
  runTmuxNoThrow(['kill-pane', '-t', paneId])
}
```

The current direct `runTmuxNoThrow(...)` call keeps the existing public no-throw behavior for tmux kill failures. This gate does not change that behavior.

`clearPaneLabelSync(paneId)` remains TypeScript-owned and unchanged:

```ts
export function clearPaneLabelSync(paneId: string): void {
  runTmuxNoThrow(['set-option', '-up', '-t', paneId, '@agentteam-name'])
  runTmuxNoThrow(['select-pane', '-t', paneId, '-T', ''])
}
```

`createTeammatePane(...)` remains the v0.6.80 Go-backed pane discovery/creation/layout/resize cutover and is not changed here.

Detached swarm session/window creation remains unchanged:

- `createAgentTeamKernelAdapter().createDetachedSwarmSessionAsync(SWARM_SESSION, SWARM_WINDOW, signal)` remains the v0.6.82 Go-backed detached missing-session `new-session` cutover.
- `createAgentTeamKernelAdapter().createDetachedSwarmWindowAsync(SWARM_SESSION, SWARM_WINDOW, signal)` remains the v0.6.84 Go-backed detached missing-agentteam-window `new-window` cutover.

Current kill orchestration remains TypeScript-owned and outside this gate:

- `adapters/tmux/teamPanes.ts clearAndKillTeamPanes(...)` / `killTeamPanes(...)`
- `commands/teamActions.ts clearOrKillMemberPane(...)` and orphan cleanup
- `tools/workerSpawnService.ts` pane cleanup path

## Authorized Future Go Command Surface

This gate authorizes only one future tmux command for a later `workerLifecycle.killPane` cutover:

- `tmux kill-pane -t <paneId>`

No other Go tmux lifecycle command is authorized by this gate.

Future Go must pass the pane id as an argv value only, never shell-interpolate it.

A future helper must compactly validate `paneId` as a `%123`-style tmux pane id before executing tmux. Future helper diagnostics should use compact failure kinds/status only. Raw tmux stdout/stderr/helper output must not leak through public behavior, diagnostics, task reports, logs, or tests.

In compact policy terms: raw tmux stdout/stderr/helper output must not leak.

## Future Cutover Rules

The future cutover is not implemented here. When a later task performs it:

- TypeScript/pi remains the public facade and pi extension compliance boundary.
- Only `tmux/panes.ts killPane(paneId)` may move to Go.
- The future public facade must preserve current no-throw/void behavior through TypeScript.
- Helper failures should not throw publicly from `killPane(...)` unless a later gate explicitly changes the public API.
- Future helper failure diagnostics must be compact failure kinds/status only, with no raw stdout/stderr/helper output leakage.
- Future Go must use argv-only execution for exactly `tmux kill-pane -t <paneId>`.
- Future Go must compactly validate `%123`-style pane ids before execution.
- After an actual cutover, `tmux/panes.ts killPane(paneId)` must not retain a hidden TypeScript fallback for the same kill-pane behavior unless a later gate explicitly authorizes a compatibility fallback.

## Explicitly Out of Scope

Already implemented operations remain unchanged:

- `createTeammatePane(...)` remains the v0.6.80 Go-backed pane split/layout/resize cutover and is not changed here.
- `createDetachedSwarmSessionAsync(SWARM_SESSION, SWARM_WINDOW, signal)` remains the v0.6.82 Go-backed detached missing-session `new-session` cutover and is not changed here.
- `createDetachedSwarmWindowAsync(SWARM_SESSION, SWARM_WINDOW, signal)` remains the v0.6.84 Go-backed detached missing-agentteam-window `new-window` cutover and is not changed here.
- `setPaneLabel(paneId, label, signal)` remains v0.6.76 Go-backed with only pane-level `set-option -p` and `select-pane -T <label>` commands.
- `clearPaneLabel(paneId, signal)` remains v0.6.78 Go-backed with only pane-level `set-option -up` and `select-pane -T ''` commands.
- `markWindowAsAgentTeam(target, signal)` remains v0.6.72 Go-backed with only window marking `set-option -w` commands.
- `refreshWindowPaneLabels(target, signal)` remains v0.6.74 Go-backed with only pane-border window `set-option -w` commands.

Still forbidden for this gate:

- runtime cutover, Go handler, TypeScript adapter method, native helper rebuild
- `clearPaneLabelSync(paneId)` migration or deletion
- `clearPaneLabelsForTeam(...)` / `clearAndKillTeamPanes(...)` orchestration migration
- `commands/teamActions.ts` cleanup orchestration changes
- `tools/workerSpawnService.ts` pane cleanup changes
- wake/send-keys / worker delivery
- kill-window / kill-session / respawn-pane / buffers / broader destructive lifecycle
- state repository
- task/report/PlanRun/mailbox/governance
- team panel/UI view-model
- release/package control plane
- native artifact path/name changes
- package-manager native delivery or lifecycle hooks

## Native, Package, and Release Status

No Go source or native artifact rebuild occurs in v0.6.85.

The existing native helper path and binary name remain unchanged:

- `native/tmuxSnapshotParse/0.3.0-read-model-shadow/linux-x64-glibc/agentteam-tmuxSnapshotParse`

`package.json` remains `0.6.8`.

No npm version, npm publish, tag, release, release asset, package lockfile, `go.mod`, `go.sum`, lifecycle hook, postinstall download, optional native dependency, native path rename, binary rename, or package-manager native dependency/download flow is introduced.

## Evidence

- `tests/fixtures/kernel/v0685/goKillPaneGate.cjs`
- `tests/suites/go-kernel-v0685-go-kill-pane-gate.cjs`

## Validation Plan

- `node tests/run.cjs go-kernel-v0685-go-kill-pane-gate`
- focused v0.6.84 / v0.6.83 / v0.6.82 / v0.6.80 regression guard batch
- `npm run typecheck`
- `npm run -s check:boundaries`
- `git diff --check`
- `node -p "require('./package.json').version"`
- `sha256sum -c native/tmuxSnapshotParse/0.3.0-read-model-shadow/linux-x64-glibc/SHA256SUMS`
- diff guard confirming no v0.6.85 runtime cutover in `kernel/go/agentteam-kernel/main.go`, `native/tmuxSnapshotParse/0.3.0-read-model-shadow/linux-x64-glibc`, `core/kernel.ts`, `tmux/windows.ts`, or `tmux/panes.ts` beyond gate evidence checks
