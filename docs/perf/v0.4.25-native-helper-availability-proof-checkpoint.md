# v0.4.25 Native Helper Availability Proof Checkpoint

> Scope: final GitHub-only v0.4.25 checkpoint docs/tests. This checkpoint freezes package/native guardrails and summarizes native helper availability proof evidence for `tmuxSnapshotParse`. It is evidence only, not release/default approval.

## Decision

GO only for GitHub-only v0.4.25 native helper availability proof checkpoint after leader/user approval.

GO for evidence only:

- generated artifact shape validation under temp roots.
- manifest/checksum/provenance/license/executable validation prototype.
- clean-install simulation.
- unsupported-platform/rollback/version-skew policy.
- future resolver/default/fallback deletion gate definition.

STOP for:

- npm version/publish.
- package version change.
- package.json metadata changes.
- optionalDependencies.
- lifecycle hooks/downloads.
- helper build/install/package scripts.
- lockfiles.
- go.mod/go.sum.
- checked-in native binaries.
- tarballs.
- generated manifests/artifacts/package artifacts.
- real package inclusion/native artifact approval.
- default Go.
- current go-cutover behavior changes.
- go-packaged-preview availability semantics changes.
- TypeScript fallback deletion.
- hidden TS fallback rollback after cutover.
- compactReadModelFingerprint cutover.
- broad Go authority.
- native Go pi extension assumption.
- /team readiness expansion.
- commit/tag/push as part of implementation slice.

v0.4.25 still does not prove normal-user native availability and does not pass the packaged/default/fallback deletion gate.

## Linked Artifacts

Prior checkpoint:

- `docs/perf/v0.4.24-explicit-readiness-command-integration-checkpoint.md`

v0.4.25 artifacts and guards:

- owner contract and evidence doc: `docs/perf/v0.4.25-native-helper-availability-proof.md`
- Slice 1 contract guard: `tests/suites/go-kernel-v0425-native-availability-contract-docs.cjs`
- Slice 2 artifact/manifest prototype guard: `tests/suites/go-kernel-v0425-artifact-manifest-prototype.cjs`
- Slice 3 clean-install smoke guard: `tests/suites/go-kernel-v0425-clean-install-smoke.cjs`
- Slice 4 unsupported rollback policy guard: `tests/suites/go-kernel-v0425-unsupported-rollback-policy.cjs`
- Slice 5 resolver/default cutover gate guard: `tests/suites/go-kernel-v0425-resolver-default-cutover-gate.cjs`
- Slice 6 final checkpoint guard: `tests/suites/go-kernel-v0425-native-availability-checkpoint-docs.cjs`

## Slice 1-5 Outcomes

- Slice 1 owner contract / TS-pi boundary / T013 finding: defined native availability proof ownership, froze that pi extension/provider/tool surfaces are TS/JS/Node-based, assumed no native Go pi extension/provider ABI, kept TS/pi control plane mandatory, and constrained Go helper use behind TS adapter/ports via subprocess/RPC/stdin-stdout.
- Slice 2 temp artifact/manifest validator prototype: proved generated artifact shape and manifest/checksum/provenance/license/executable validation under temp roots only; no generated artifacts/manifests/helpers are checked in.
- Slice 3 temp clean-install smoke simulation: proved a future installed helper layout can be located and smoke-tested under temp roots without Go toolchain, source checkout, manual helper env, lifecycle download, install-time build, hidden network fetch, or default resolver activation.
- Slice 4 unsupported-platform rollback/version-skew policy: defined fail-closed compact no-leak diagnostics/remediation for unsupported OS/arch/libc and helper/package skew, plus rollback as release/tag/package/deprecation/default-disable policy rather than hidden runtime TS fallback after cutover.
- Slice 5 resolver/default module cutover gate: converted Slice 1-4 evidence into a future gate matrix for packaged/default resolver consideration and separate `tmuxSnapshotParse` TypeScript fallback deletion consideration.

## Runtime and Package Invariants

TS/pi control plane remains mandatory. Go is helper/kernel behind TS adapter/ports via subprocess/RPC/stdin-stdout.

Runtime invariants remain unchanged:

- default/unset remains disabled/TypeScript.
- `go-packaged-preview` remains explicit-only/non-default and its availability semantics remain unchanged.
- current `go-cutover` remains helper-path based and unchanged.
- packaged helper discovery does not run in default/disabled/typescript/go/auto/current `go-cutover`.
- `tmuxSnapshotParse` remains the only cutover-owned module under discussion.
- `compactReadModelFingerprint` remains TypeScript fallback/non-cutover.
- Go authority remains parser-only stdin/stdout for `tmuxSnapshotParse` and does not own tmux execution/capture, state, worker lifecycle, task/report governance, PlanRun, full-text boundaries, package/release authority, UI rendering, or command control plane.
- `/team readiness` remains transitional reviewer tooling and is not expanded.
- no hidden TS fallback after cutover; rollback must be release/tag/package/deprecation/default-disable policy.

Package/native invariants remain unchanged:

- `package.json` version remains `0.6.8`.
- no package.json metadata changes.
- no optionalDependencies.
- no lifecycle hooks/downloads.
- no helper build/install/package scripts.
- no lockfiles.
- no go.mod/go.sum.
- no checked-in native binaries.
- no tarballs.
- no generated manifests/artifacts/package artifacts.
- no real package inclusion/native artifact approval.
- no npm version/publish.
- no commit/tag/push as part of implementation slice.

## Remaining Blockers

Before real native/default/fallback deletion can be discussed, these blockers remain:

- real generated artifacts/checksums/provenance/license/executable validation outside temp fixtures.
- clean install smokes across supported platforms/package managers.
- unsupported-platform remediation acceptance.
- rollback/default-disable/deprecation execution plan.
- package release ownership and companion package metadata approval.
- normal-user diagnostics UX if needed.
- parser failure policy in real default path.
- explicit user approval.

## Validation Matrix

Required v0.4.25 focused suites:

- `node --check tests/suites/go-kernel-v0425-native-availability-contract-docs.cjs`
- `node --check tests/suites/go-kernel-v0425-artifact-manifest-prototype.cjs`
- `node --check tests/suites/go-kernel-v0425-clean-install-smoke.cjs`
- `node --check tests/suites/go-kernel-v0425-unsupported-rollback-policy.cjs`
- `node --check tests/suites/go-kernel-v0425-resolver-default-cutover-gate.cjs`
- `node --check tests/suites/go-kernel-v0425-native-availability-checkpoint-docs.cjs`
- `node tests/run.cjs go-kernel-v0425-native-availability-contract-docs`
- `node tests/run.cjs go-kernel-v0425-artifact-manifest-prototype`
- `node tests/run.cjs go-kernel-v0425-clean-install-smoke`
- `node tests/run.cjs go-kernel-v0425-unsupported-rollback-policy`
- `node tests/run.cjs go-kernel-v0425-resolver-default-cutover-gate`
- `node tests/run.cjs go-kernel-v0425-native-availability-checkpoint-docs`

Relevant prior suites where practical:

- v0.4.21 runtime availability/native artifact/package policy/packaged preview suites.
- v0.4.22 manifest compatibility/native package metadata/package guardrail suites.
- v0.4.23 compact native failure diagnostics suites.
- v0.4.24 explicit readiness command integration suites.

Broad validation:

- `node tests/run.cjs`
- `npm run typecheck`
- `npm run -s check:boundaries`
- `git diff --check`
- default bench: `npm run --silent bench:team-panel-tmux`
- preview bench: `PI_AGENTTEAM_KERNEL=go-packaged-preview npm run --silent bench:team-panel-tmux`
- package/native sanity scan.

## Final Recommendation

Proceed only with GitHub-only v0.4.25 native helper availability proof checkpoint review after leader/user approval.

Do not proceed in this implementation slice with production resolver/default behavior, package metadata, real artifacts, native/default cutover, default Go, TypeScript fallback deletion, hidden TS fallback rollback after cutover, compactReadModelFingerprint cutover, broad Go authority, native Go pi extension assumptions, `/team readiness` expansion, npm version/publish, commit/tag/push, or later work.
