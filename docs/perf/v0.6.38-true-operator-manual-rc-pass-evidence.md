# v0.6.38 True Operator Manual RC Pass Evidence

> Scope: T120 rerun after T124 prompt-procedure diagnosis. This artifact records a true `pi` TUI/operator/model manual RC smoke using a clean temporary `PI_AGENTTEAM_HOME`, explicit local extension/session-dir launch, real model/provider tool calls, visible tmux panes, state checks after each step, and sanitized evidence only.

## Result

Result: **pass with one optional limitation**.

The full required manual RC smoke path completed through clean launch, config commands, unsafe team-name rejection, ASCII team creation, researcher/planner/implementer visible worker panes, task create/send/receive/report boundaries, leader receive/close/block/unblock, compact `/team` TUI refresh, PlanRun approve/advance/waiting_review/leader close/final done, pause/resume/check_limits/signal_failure, and cleanup.

Optional PlanRun `cancel` did not execute after bounded retries: the model accepted the prompt text but emitted no `agentteam_planrun` cancel tool result, leaving `PR0001` paused after `signal_failure`. Because the assignment scoped cancel as `if feasible`, this is recorded as an optional not-covered item, not a blocker for the main RC pass.

The run used the T124 mitigation throughout: one tool at a time, terse natural-language `Parameters:` prompts, and no JSON-call/JSON-arguments prompt wording.

## Environment

| Field | Value |
| --- | --- |
| Date | `2026-06-24` |
| Git | `49bf957` |
| Node | `v24.9.0` |
| npm | `11.7.0` |
| pi | `0.80.2` |
| tmux | `tmux 3.6a` |
| Provider/model observed in session | `077/gpt-5.5` |
| `PI_AGENTTEAM_HOME` | Clean temp home under `/tmp/pi-agentteam-v0638-t120-rc.<redacted>`; removed after evidence extraction. |
| Evidence temp summary | `/tmp/pi-agentteam-t120-evidence.<redacted>/sanitized-summary.json`; sanitized only, not checked in. |

Launch shape used inside a dedicated tmux session:

```bash
export PI_AGENTTEAM_HOME="$(mktemp -d /tmp/pi-agentteam-v0638-t120-rc.XXXXXX)"
pi --no-extensions --extension ./index.ts --session-dir "$PI_AGENTTEAM_HOME/pi-sessions"
```

## Prompt Procedure

All model/tool prompts used natural-language `Parameters:` wording. Examples:

```text
Use tool agentteam_spawn only. Parameters: name=researcher-one, role=researcher. No prose.
Use tool agentteam_task only. Parameters: action=create, title=T120 receive boundary, description=Researcher should receive and report done for RC boundary, owner=researcher-one. No prose.
Use tool agentteam_planrun only. Parameters: action=advance, planRunId=PR0002. No prose.
```

No prompt used JSON-call wording such as `Call exactly one tool ... with JSON arguments {...}`.

## Checklist Results

| Item | Result | State-check evidence |
| --- | --- | --- |
| Clean temp home + explicit launch | pass | TUI started with local extension `05_pi-agentteam`; state rooted under temp home. |
| `/team config show` | pass | Did not create `config.json`. |
| `/team config init` | pass | Created v1 config with roles `implementer`, `planner`, `researcher`. |
| `/team config validate` | pass | Config SHA and mtime unchanged. |
| `/team config migrate --dry-run` | pass | Config SHA and mtime unchanged. |
| Unsafe name rejection | pass | `agentteam_create` with `team_name=!!!` returned empty-normalization guidance; no team dir created. |
| ASCII team create | pass | `team.json` for `t120-manual-rc` persisted with `team-lead` bound to pane `%126`. |
| Spawn researcher | pass | `researcher-one` role `researcher`, pane `%128`, session file under temp `worker-sessions`, `bridgeAvailable:true`. |
| Spawn planner | pass | `planner-one` role `planner`, pane `%127`, session file under temp `worker-sessions`, `bridgeAvailable:true`. |
| Spawn implementer | pass | `implementer-one` role `implementer`, pane `%129`, session file under temp `worker-sessions`, `bridgeAvailable:true`. |
| No quarantine | pass | `_quarantine` count stayed `0`; compact `.panel.json` sidecars existed. |
| Task create | pass | `T001` created open, owner `researcher-one`; `T002` created open, owner `planner-one`; PlanRun later created `T003`. |
| Task send | pass | `agentteam_send` persisted task-bound assignment to researcher inbox and later implementer inbox. |
| Worker receive full-text boundary | pass | Researcher and implementer `agentteam_receive markRead=true` moved their mailbox unread counts to `0`. |
| Worker `report_done` report-only | pass | Researcher created `TR0001` and `TR0002` for `T001`; task stayed open until leader close. |
| Worker `report_blocked` report-only | pass | Planner created `TR0003` for `T002`; task stayed open until leader block. |
| Leader receive | pass | Leader unread report notifications moved to `0` after `agentteam_receive`. |
| Leader close | pass | `T001` moved `open -> done`; later `T003` moved `open -> done`. First close prompt had an empty turn and passed on one retry. |
| Leader block/unblock | pass | `T002` moved `open -> blocked:external-input -> open`. |
| `/team` TUI + direct `r` refresh | pass | `team-panel.json` and `team-lead.panel.json` existed, contained compact keys, no full-body markers; unread count stayed `0`. |
| PlanRun approve | pass | `PR0001` and `PR0002` approved from `TR0003`; approval created no tasks. |
| PlanRun advance | pass | `PR0002` advanced to `active`, created `T003` for `implementer-one`. |
| PlanRun report/waiting_review | pass | Implementer reported done for `T003`; `PR0002` moved to `waiting_review`. |
| PlanRun leader close/final advance | pass | Leader closed `T003`; final `agentteam_planrun advance` moved `PR0002` to `done`. |
| PlanRun pause/resume | pass | `PR0001` moved `approved -> paused:leader_paused -> approved`. |
| PlanRun check_limits | pass | `PR0001` returned limits ok; no task/mailbox side effects. |
| PlanRun signal_failure | pass | `PR0001` moved to `paused:validation_failed`. |
| PlanRun cancel | optional not covered | Two cancel prompts produced no cancel tool result; `PR0001` remained `paused:validation_failed`. |
| Cleanup | pass | Recorded tmux session and temp home removed after evidence extraction. |

## Final Compact State

```json
{
  "members": {
    "team-lead": { "role": "leader", "status": "idle", "paneId": "%126" },
    "planner-one": { "role": "planner", "status": "idle", "paneId": "%127", "bridgeAvailable": true },
    "researcher-one": { "role": "researcher", "status": "idle", "paneId": "%128", "bridgeAvailable": true },
    "implementer-one": { "role": "implementer", "status": "idle", "paneId": "%129", "bridgeAvailable": true }
  },
  "tasks": {
    "T001": { "status": "done", "owner": "researcher-one", "title": "T120 receive boundary" },
    "T002": { "status": "open", "owner": "planner-one", "title": "T120 blocked boundary" },
    "T003": { "status": "done", "owner": "implementer-one", "title": "T120 PlanRun implementer step" }
  },
  "reports": 5,
  "planRuns": {
    "PR0001": { "status": "paused", "pauseReason": "validation_failed" },
    "PR0002": { "status": "done", "activeTaskId": null }
  }
}
```

## Provider/Tool-Call Notes

Sanitized session facts:

- JSONL event lines: `148`.
- Tool results observed: `33`.
- Prompt counts: `agentteam_create=2`, `agentteam_spawn=3`, `agentteam_task=7`, `agentteam_send=3`, `agentteam_receive=4`, `agentteam_planrun=15`.
- Empty assistant turns after tool prompts: `agentteam_spawn=1`, `agentteam_receive=1`, `agentteam_task=1`.
- Empty turns were handled with state checks and bounded terse retries; no state was counted as pass until the expected file state changed.
- `agentteam_planrun cancel` remained not called after bounded retries and is recorded as optional not covered.

## No-Leak / Artifact Policy

No raw logs, screenshots, state archives, worker transcripts, full mailbox bodies, full report bodies, provider response identifiers, raw tool-call identifiers, package artifacts, native artifacts, release assets, tags, pushes, `npm version`, `npm publish`, `package.json` edits, default-Go/native/fallback deletion work, or force-added ignored files were created.

The sanitized temp summary was checked for known full-body, provider-id, raw-call-id, and sentinel leak patterns; all were absent. The exact local temp paths are also omitted from this document.

## Recommendation

Use the T124 terse `Parameters:` prompt procedure for future true-operator RCs with provider/model `077/gpt-5.5`. Avoid JSON-call wording. Keep the optional PlanRun cancel gap as a focused follow-up if cancel coverage becomes required for release criteria.
