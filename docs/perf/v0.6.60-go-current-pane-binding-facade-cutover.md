# v0.6.60 Go captureCurrentPaneBinding Facade Cutover

Result: v0.6.60 cuts over the TypeScript `captureCurrentPaneBinding()` facade/default path to a narrow Go-backed `workerLifecycle.captureCurrentPaneBinding` operation.

This slice closes the final synchronous current-pane binding fallback while preserving the outside-tmux fail-closed gate:

```text
tmux/core.ts captureCurrentPaneBinding() -> isInsideTmux guard -> TypeScript kernel adapter -> Go workerLifecycle.captureCurrentPaneBinding -> { paneId, target } | null
```

## Runtime Behavior

- `tmux/core.ts` keeps `if (!isInsideTmux()) return null` before invoking the helper.
- `tmux/core.ts` `captureCurrentPaneBinding()` now delegates to `createAgentTeamKernelAdapter().captureCurrentPaneBinding()`.
- The TypeScript `runTmuxNoThrow(['display-message', '-p', '#{pane_id}'])` and `runTmuxNoThrow(['display-message', '-p', '#{session_name}:#{window_id}'])` fallback calls are removed from `captureCurrentPaneBinding()`.
- Go uses exactly `tmux display-message -p workerLifecycleCurrentPaneBindingFormat` with compact format `#{pane_id}\t#{session_name}:#{window_id}`.
- The adapter forwards only `PATH`, `TMUX`, and `TMUX_PANE` to the helper process.
- Public success remains `{ paneId, target }`; helper diagnostics stay compact on the adapter result.

## Failure Policy

Helper failure, outside-tmux, tmux unavailable, empty pane id, empty target, unsafe response shape, and command timeout all fail closed to `null` at the public facade.

The Go result remains compact/read-only: `readOnly:true`, `stateFilesRead:false`, `stateFilesWritten:false`, and `tmuxMutation:false`.

## Preserved Boundaries

- `resolvePaneBindingAsync(paneId, signal)` remains TypeScript `display-message`-owned to preserve `AbortSignal` semantics.
- `windowExists()` and `firstPaneInWindow()` remain TypeScript window helper paths.
- `inspectPane()`, `paneExists()`, `resolvePaneBinding()`, `targetForPaneId()`, and `listAgentTeamPanes()` remain on their existing Go-backed facade seams.
- Mutating worker lifecycle remains TypeScript-owned: no `wakePane`, `syncPaneLabels`, `createTeammatePane`, `killPane`, `clearPaneLabel`, send-keys, label mutation, split-window, new-window, kill-pane, select-pane, or respawn-pane migration.
- State repository, task/report/PlanRun governance, team panel view-model, release/package verification, and package/native ownership remain unmigrated.
- Native artifact path and binary name remain unchanged.

## Package And Release Guards

No package/release action is authorized in this slice:

- no `npm version`.
- no `npm publish`.
- no tag or GitHub release.
- no package lockfiles.
- no `go.mod` or `go.sum`.
- no lifecycle hooks or postinstall downloads.
- no native artifact path or binary rename.

Because Go source changes, the existing embedded helper is rebuilt in the same approved path with refreshed manifest, checksums, provenance, and placeholder attestation. `package.json` remains `0.6.8`.

## Evidence

- `docs/perf/v0.6.60-go-current-pane-binding-facade-cutover.md`
- `tests/fixtures/kernel/v0660/goCurrentPaneBindingFacadeCutover.cjs`
- `tests/suites/go-kernel-v0660-go-current-pane-binding-facade-cutover.cjs`

## Validation Scope

The v0.6.60 guard suite verifies:

- `captureCurrentPaneBinding()` keeps the outside-tmux guard before helper invocation.
- the direct TypeScript `display-message` fallback is removed from `captureCurrentPaneBinding()`.
- success returns `{ paneId, target }`; helper failure, missing pane id, missing target, and outside tmux return `null`.
- Go uses only the no-target `display-message -p workerLifecycleCurrentPaneBindingFormat` command for this operation.
- `TMUX`/`TMUX_PANE` forwarding is explicit and narrow at the adapter seam.
- `resolvePaneBindingAsync()` remains signal-aware and TypeScript-owned.
- window helpers and mutating lifecycle remain TypeScript-owned.
- package version, native path/name, and release surfaces remain guarded.
