# v0.6.87 Go clearPaneLabelSync Gate

Result: v0.6.87 defines the gate-only contract for future `tmux/panes.ts clearPaneLabelSync(paneId)` Go reuse without changing runtime behavior.

This is gate-only evidence for exactly one future candidate: replacing the two current TypeScript-owned no-throw calls in `clearPaneLabelSync(paneId)`:

```ts
runTmuxNoThrow(['set-option', '-up', '-t', paneId, '@agentteam-name'])
runTmuxNoThrow(['select-pane', '-t', paneId, '-T', ''])
```

No TypeScript adapter method, Go handler, `tmux/panes.ts` runtime change, native helper rebuild, or package/release action is made in this slice.

## Current TypeScript-Owned Behavior

`clearPaneLabelSync(paneId)` remains synchronous, TypeScript-owned, public no-throw, and `void`:

```ts
export function clearPaneLabelSync(paneId: string): void {
  runTmuxNoThrow(['set-option', '-up', '-t', paneId, '@agentteam-name'])
  runTmuxNoThrow(['select-pane', '-t', paneId, '-T', ''])
}
```

This preserves the existing cleanup semantics for synchronous call sites that need to clear a preserved/current pane label without awaiting the async label orchestration path.

`killPane(paneId)` remains the v0.6.86 Go-backed synchronous no-throw facade and is unchanged:

```ts
export function killPane(paneId: string): void {
  try {
    createAgentTeamKernelAdapter().killPane(paneId)
  } catch (_) {}
}
```

`tmux/labels.ts clearPaneLabel(paneId, signal)` remains the existing v0.6.78 async Go-backed helper and is unchanged. It already delegates to `createAgentTeamKernelAdapter().clearPaneLabelAsync(paneId, signal)`.

Other current Go-backed helpers remain unchanged:

- `createTeammatePane(...)` remains the v0.6.80 Go-backed pane discovery/creation/layout/resize cutover.
- `createAgentTeamKernelAdapter().createDetachedSwarmSessionAsync(SWARM_SESSION, SWARM_WINDOW, signal)` remains the v0.6.82 Go-backed detached missing-session `new-session` cutover.
- `createAgentTeamKernelAdapter().createDetachedSwarmWindowAsync(SWARM_SESSION, SWARM_WINDOW, signal)` remains the v0.6.84 Go-backed detached missing-agentteam-window `new-window` cutover.
- `tmux/labels.ts` mark/refresh/set/clear async helpers remain Go-backed and unchanged.

Current cleanup orchestration remains TypeScript-owned and outside this gate:

- `adapters/tmux/teamPanes.ts clearAndKillTeamPanes(...)` / `killTeamPanes(...)`
- `commands/teamActions.ts clearOrKillMemberPane(...)` and orphan cleanup
- `tools/workerSpawnService.ts` pane cleanup path

## Authorized Future Go Command Surface

A later cutover may replace the synchronous `clearPaneLabelSync(paneId)` direct TypeScript calls, but the future Go command surface is limited to the existing pane-label clearing pair already implemented by Go `workerLifecycle.clearPaneLabel`:

- `tmux set-option -up -t <paneId> @agentteam-name`
- `tmux select-pane -t <paneId> -T ''`

Future cutover should prefer reusing the existing Go `workerLifecycle.clearPaneLabel` operation rather than adding a second Go operation, unless a later cutover task explicitly justifies a different design.

In short: future cutover should prefer reusing the existing Go `workerLifecycle.clearPaneLabel` operation.

Future Go must pass the pane id as an argv value only, never shell-interpolate it.

A future helper/facade must compactly validate `paneId` as a `%123`-style tmux pane id before execution. Future helper diagnostics should use compact failure kinds/status only. Raw tmux stdout/stderr/helper output must not leak through public behavior, diagnostics, task reports, logs, or tests.

In compact policy terms: raw tmux stdout/stderr/helper output must not leak.

## Future Cutover Rules

The future cutover is not implemented here. When a later task performs it:

- TypeScript/pi remains the public facade and pi extension compliance boundary.
- Only `tmux/panes.ts clearPaneLabelSync(paneId)` may move away from the direct TypeScript command pair.
- The future public facade must preserve current synchronous no-throw/void behavior through TypeScript.
- Helper, invalid-pane, timeout, and tmux failures should not throw publicly from `clearPaneLabelSync(...)` unless a later gate explicitly changes the public API.
- Future helper failure diagnostics must be compact failure kinds/status only, with no raw stdout/stderr/helper output leakage.
- Future Go must use argv-only execution for exactly `tmux set-option -up -t <paneId> @agentteam-name` and `tmux select-pane -t <paneId> -T ''`.
- Future Go must compactly validate `%123`-style pane ids before execution.
- The future design should reuse existing `workerLifecycle.clearPaneLabel` if possible; adding `workerLifecycle.clearPaneLabelSync` or another second Go operation requires a later explicit justification.
- After an actual cutover, `tmux/panes.ts clearPaneLabelSync(paneId)` must not retain a hidden TypeScript fallback for the same two clear-label commands unless a later gate explicitly authorizes a compatibility fallback.

## Explicitly Out of Scope

Already implemented operations remain unchanged:

- `tmux/labels.ts clearPaneLabel(paneId, signal)` remains the existing v0.6.78 async Go-backed helper and is unchanged.
- `killPane(paneId)` remains the v0.6.86 Go-backed synchronous no-throw facade and is unchanged.
- `createTeammatePane(...)` remains the v0.6.80 Go-backed pane split/layout/resize cutover and is unchanged.
- `createDetachedSwarmSessionAsync(SWARM_SESSION, SWARM_WINDOW, signal)` remains the v0.6.82 Go-backed detached missing-session `new-session` cutover and is unchanged.
- `createDetachedSwarmWindowAsync(SWARM_SESSION, SWARM_WINDOW, signal)` remains the v0.6.84 Go-backed detached missing-agentteam-window `new-window` cutover and is unchanged.
- `setPaneLabel(paneId, label, signal)`, `markWindowAsAgentTeam(target, signal)`, and `refreshWindowPaneLabels(target, signal)` remain already-Go-backed and unchanged.

Still forbidden for this gate:

- runtime cutover, TypeScript adapter method, Go handler, native helper rebuild
- changes to `tmux/panes.ts` runtime behavior
- changes to `tmux/labels.ts clearPaneLabel(paneId, signal)`
- changes to `killPane`, `createTeammatePane`, new-session/new-window, or mark/refresh/set/clear async helpers
- `clearPaneLabelsForTeam(...)` / `clearAndKillTeamPanes(...)` orchestration migration
- `commands/teamActions.ts` cleanup orchestration changes
- `tools/workerSpawnService.ts` pane cleanup changes
- wake/send-keys / worker delivery
- kill-window / kill-session / respawn-pane / buffers / broader destructive lifecycle
- state/task/PlanRun/mailbox/governance
- team panel/UI view-model
- release/package control plane
- native artifact path/name changes
- package-manager native delivery or lifecycle hooks

## Native, Package, and Release Status

No Go source or native artifact rebuild occurs in v0.6.87.

The existing native helper path and binary name remain unchanged:

- `native/tmuxSnapshotParse/0.3.0-read-model-shadow/linux-x64-glibc/agentteam-tmuxSnapshotParse`

The native smoke surface remains the existing v0.6.78 `workerLifecycleClearPaneLabel` smoke plus previously added smokes such as v0.6.86 `workerLifecycleKillPane`; v0.6.87 does not add a new `workerLifecycleClearPaneLabelSync` smoke entry.

`package.json` remains `0.6.8`.

No npm version, npm publish, tag, release, release asset, package lockfile, `go.mod`, `go.sum`, lifecycle hook, postinstall download, optional native dependency, native path rename, binary rename, or package-manager native dependency/download flow is introduced.

## Evidence

- `tests/fixtures/kernel/v0687/goClearPaneLabelSyncGate.cjs`
- `tests/suites/go-kernel-v0687-go-clear-pane-label-sync-gate.cjs`

## Validation Plan

- `node --check tests/fixtures/kernel/v0687/goClearPaneLabelSyncGate.cjs`
- `node --check tests/suites/go-kernel-v0687-go-clear-pane-label-sync-gate.cjs`
- `node tests/run.cjs go-kernel-v0687-go-clear-pane-label-sync-gate`
- focused v0.6.86 / v0.6.85 / v0.6.84 / v0.6.82 / v0.6.80 regression guard batch
- `npm run typecheck`
- `npm run -s check:boundaries`
- `git diff --check`
- `node -p "require('./package.json').version"`
- `sha256sum -c native/tmuxSnapshotParse/0.3.0-read-model-shadow/linux-x64-glibc/SHA256SUMS`
- diff guard confirming no v0.6.87 runtime cutover in `kernel/go/agentteam-kernel/main.go`, `native/tmuxSnapshotParse/0.3.0-read-model-shadow/linux-x64-glibc`, `core/kernel.ts`, `tmux/windows.ts`, or `tmux/panes.ts` beyond docs/tests/roadmap/.gitignore evidence
