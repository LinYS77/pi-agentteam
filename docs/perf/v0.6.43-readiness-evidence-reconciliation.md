# v0.6.43 Readiness Evidence Reconciliation

> Scope: v0.6.43 docs/tests reconciliation after v0.6.38-v0.6.42 evidence. This checkpoint updates the current roadmap/evidence status only; it does not execute manual RC, create tags, push, run `npm version`, publish, create package/release assets, enable default-Go/native/package work, delete fallback, or claim v0.7 release readiness.

## Result

Result: v0.6.43 reconciles the release-readiness evidence map after focused p95 and true-operator evidence improvements.

Final result remains `ready:false`. Evidence improved materially and stale blockers are now closed or superseded, but final v0.7 release governance has not started.

This checkpoint is GO for reviewer inspection of the current evidence map, superseded blocker list, no-leak/governance assertions, and package/runtime invariants.

This checkpoint is STOP for release-ready approval, tag/release/git push, `npm version`, `npm publish`, package release, release assets, default-Go/default resolver, native helper/package delivery, fallback deletion, signing/security attestations, second-platform work, raw operator logs, screenshots, raw state archives, raw mailbox/report bodies, worker transcripts, raw timing JSON, raw hosted records, or force-adding ignored PlanRun cancel evidence.

## Reconciled Evidence

| Evidence | Current status | Source | Notes |
| --- | --- | --- | --- |
| `true-operator-manual-rc-main-checklist` | `accepted-pass` | `docs/perf/v0.6.38-true-operator-manual-rc-pass-evidence.md`; task `T120` / report `TR0132` | Main checklist passed with sanitized repo evidence. |
| `true-operator-planrun-cancel-follow-up` | `accepted-task-board-pass-not-force-added` | task `T129` / report `TR0135` | T129 local ignored sanitized doc is not force-added by this slice. |
| `task-message-report-action-normal-p95` | `covered-focused-pass` | `docs/perf/v0.6.39-task-message-report-p95.md` | `30.434ms <= 50ms`. |
| `task-message-report-action-large-mailbox-p95` | `covered-focused-pass-supersedes-v0639-fail` | `docs/perf/v0.6.39-task-message-report-p95.md` | `106.863ms <= 150ms` after v0.6.40 validation-cache optimization. |
| `fsstore-lock-wait-p95` | `covered-focused-pass` | `docs/perf/v0.6.41-fsstore-lock-wait-p95.md` | `10.644ms <= 25ms`. |
| `data-change-render-debounce-rate` | `covered-focused-pass` | `docs/perf/v0.6.42-data-change-render-debounce.md` | `3.021 renders/sec <= 4`; direct `r` refresh/no-op refresh boundaries preserved. |
| `spawn-bookkeeping-p95` | `covered-focused-pass` | `docs/perf/v0.6.42-spawn-bookkeeping-p95.md` | `21.995ms <= 100ms`; external tmux/provider/operator waits excluded. |

## Superseded Blockers

| Previous blocker | Previous status | Current status | Evidence |
| --- | --- | --- | --- |
| `v0639-large-mailbox-task-message-report-p95-fail` | `fail/blocker` | `superseded-by-v0640-pass` | `docs/perf/v0.6.39-task-message-report-p95.md` |
| `v0638-v0639-missing-fsstore-lock-wait-p95` | `not-covered` | `covered-by-v0641-focused-pass` | `docs/perf/v0.6.41-fsstore-lock-wait-p95.md` |
| `v0638-v0641-missing-data-change-render-debounce-rate` | `not-covered` | `covered-by-v0642-focused-pass` | `docs/perf/v0.6.42-data-change-render-debounce.md` |
| `v0638-v0641-missing-spawn-bookkeeping-p95` | `not-covered` | `covered-by-v0642-focused-pass` | `docs/perf/v0.6.42-spawn-bookkeeping-p95.md` |
| `t120-optional-planrun-cancel-gap` | `optional-not-covered` | `closed-by-t129-task-board-pass-without-force-adding-ignored-doc` | task `T129`, report `TR0135` |

Historical docs may still contain old fail/not-covered rows because they are audit records for the earlier checkpoint. v0.6.43 supersedes the current status without rewriting those historical observations.

## Still Not Authorized

The following remain explicitly unauthorized:

- v0.7 release-ready claim.
- Tag creation, tag movement, tag push, git push, or GitHub release.
- `npm version`, `npm publish`, package release, install-source approval, package metadata expansion, release bundle, or package artifact.
- Default-Go/default resolver enablement, native helper/package delivery, fallback deletion, signing/security attestation, or second-platform support.
- Release assets or raw hosted records.
- Force-adding ignored T129 PlanRun cancel evidence without separate no-leak guard authorization.

## No-Leak Check

No raw terminal logs, screenshots, raw state archives, raw full mailbox/report bodies, worker transcripts, secrets, raw hosted records, or raw timing JSON are checked in by this reconciliation.

The guard searches for and preserves the following no-leak policy markers in controlled fixture/doc/suite files only:

- `V0643_READINESS_RECONCILIATION_FULL_TEXT_SENTINEL_DO_NOT_LEAK`
- `MailboxMessage.text`
- `TaskReport.text`
- `worker transcript`
- `terminal raw log`
- `screenshot`
- `state archive`
- `raw state archive`
- `BEGIN PRIVATE KEY`
- `raw hosted record`
- `provider response id`
- `raw tool-call id`

T129 PlanRun cancel evidence is referenced through the durable task-board fact only. This slice does not force-add its ignored local sanitized doc and does not embed raw operator/provider/session artifacts.

## TypeScript/pi Facade Authority

TypeScript/pi facade authority is preserved:

- `package.json` remains version `0.6.8`.
- `package.json#pi.extensions` remains `['./index.ts']`.
- TypeScript/pi remains the product and control-plane facade.
- v0.6.43 is docs/tests reconciliation only; no production runtime behavior, Go/default/native/package/fallback deletion, tag, release, or publish behavior is approved or implemented.

## Validation

Focused validation for this evidence checkpoint:

- `node --check tests/fixtures/kernel/v0643/readinessEvidenceReconciliation.cjs`.
- `node --check tests/suites/go-kernel-v0643-readiness-evidence-reconciliation.cjs`.
- Direct require-based invocation of `tests/suites/go-kernel-v0643-readiness-evidence-reconciliation.cjs` with `helpers.extRoot=process.cwd()`.
- Broad regression: `npm test`.
- TypeScript: `npm run typecheck`.
- Boundary guard: `npm run -s check:boundaries`.
- Whitespace: `git diff --check`.

## Final Recommendation

Use v0.6.43 as the current readiness evidence reconciliation checkpoint only. Evidence improved and stale blockers are superseded, but final readiness remains `ready:false` until a separate leader/user-authorized release-governance task explicitly decides otherwise.
