# v0.4.27 Generated Artifact Clean-Install Consumption Checkpoint

> Scope: Slice 1 docs/tests-only owner contract. This starts the v0.4.27 Generated Artifact Clean-Install Consumption Checkpoint by defining how future clean-install proof may consume generated helper artifacts after v0.4.26 pipeline prototype evidence. It is a GitHub-only evidence boundary, not package/native/default/fallback/readiness approval. It does not implement package install, production resolver behavior, artifact download, CI workflow, release assets, npm package changes, default Go, TypeScript fallback deletion, `/team readiness` changes, or runtime behavior changes.

## Decision

Slice 1 recommendation:

- GO only for docs/tests-only generated artifact clean-install consumption owner contract after leader review.
- GO only for GitHub-only evidence planning.
- STOP for production clean-install consumption implementation.
- STOP for artifact download, lifecycle install, install-time build, or hidden network fetch.
- STOP for package manager, npm, native companion package, default resolver, or fallback behavior changes.
- STOP for generated artifacts/manifests/helpers checked into the repo.
- STOP for CI workflow implementation, active GitHub Actions artifact storage, GitHub release assets, or npm companion packages.
- STOP for `/team readiness` expansion, ambient `/team` diagnostics, or model-callable native/readiness tools.
- STOP for Slice 2 work without separate approval.

This contract does not prove normal-user native availability. It defines the consumption owner boundary that future v0.4.27 slices must satisfy before generated artifacts can be treated as clean-install evidence.

## Prior Evidence and Links

v0.4.27 follows v0.4.26 because v0.4.26 produced GitHub-only artifact generation pipeline prototype evidence, not clean-install consumption proof.

Prior artifacts:

- v0.4.26 final checkpoint: `docs/perf/v0.4.26-go-helper-artifact-pipeline-checkpoint.md`
- v0.4.26 owner/evidence doc: `docs/perf/v0.4.26-go-helper-artifact-pipeline.md`
- v0.4.26 final checkpoint guard: `tests/suites/go-kernel-v0426-artifact-pipeline-checkpoint-docs.cjs`
- v0.4.26 artifact smoke/clean-install handoff guard: `tests/suites/go-kernel-v0426-artifact-smoke-handoff.cjs`
- v0.4.25 clean-install smoke simulation guard: `tests/suites/go-kernel-v0425-clean-install-smoke.cjs`
- v0.4.25 final native availability checkpoint: `docs/perf/v0.4.25-native-helper-availability-proof-checkpoint.md`

## T013 Runtime Boundary

T013 remains binding for v0.4.27:

- TS/pi control plane mandatory.
- pi extension/provider/tool surfaces remain TS/JS/Node-based.
- Go helper behind TS adapter/ports via subprocess/RPC/stdin-stdout.
- no native Go pi extension/provider ABI assumption.
- Go is not a pi extension/provider surface in this plan.
- Go helper authority remains parser-only stdin/stdout `tmuxSnapshotParse`.
- Go does not own tmux execution/capture, state, worker lifecycle, task/report governance, PlanRun, full-text boundaries, package/release authority, UI rendering, or command control plane.
- `compactReadModelFingerprint` remains TypeScript fallback / non-cutover.

Implication: v0.4.27 clean-install consumption proof is about a TS-owned consumer contract for generated helper artifacts, not replacing the pi extension facade or defaulting runtime behavior to Go.

## Clean-Install Consumption Boundary

Clean-install consumption means a future proof starts from an installed-package-shaped layout and consumes a pre-generated helper artifact plus metadata through TS-owned package-relative rules.

The consumption boundary requires future evidence to show:

- generated artifact input is pre-existing approved evidence, not produced by package install.
- installed layout starts from a clean temp or package-manager equivalent root, not a source checkout.
- manifest/helper paths are package-relative, allowlisted, and traversal-safe.
- integrity metadata covers checksum, size, provenance, license metadata, executable policy, module, protocol, package version, helper version, capability, and platform tuple.
- direct helper smoke runs only through explicit test/preview injection until separately approved.
- no Go toolchain is required.
- no source checkout path is required.
- no manual helper environment override is required.
- no lifecycle download is allowed.
- no install-time build is allowed.
- no hidden network fetch is allowed.
- no default resolver activation is allowed.
- failure is fail-closed and no-leak.
- package manager install behavior, npm tarball behavior, release assets, companion package metadata, and default resolver discovery remain future separate gates.

Slice 1 does not create, copy, download, install, or smoke real generated artifacts. It only defines the owner contract and guardrails for future consumption evidence.

## Owner Responsibilities

Future consumption evidence must keep ownership split explicit:

| Area | Owner boundary in v0.4.27 Slice 1 | Not approved in Slice 1 |
| --- | --- | --- |
| artifact producer | v0.4.26-style generated artifact metadata remains prior/future evidence | helper build commands, `go build`, repo artifacts, release artifacts |
| consumption owner | TS-owned package-relative manifest/helper consumption contract and no-leak fail-closed expectations | production resolver implementation or default discovery |
| package owner | future package-manager and companion package design only after approval | npm package metadata, optional dependencies, lifecycle hooks, package scripts |
| release owner | future storage/release/deprecation policy only after approval | GitHub release assets, active artifact storage, npm publish/version |
| runtime owner | explicit preview/test handoff only | default Go, `go-cutover`/`go-packaged-preview` semantic changes, fallback deletion |
| readiness owner | current transitional reviewer tooling remains contained | `/team readiness` expansion or ambient panel diagnostics |

## Slice 2 — Artifact Bundle Contract from v0.4.26 Outputs

Slice 2 is docs/tests plus OS temp fixture coverage only. It defines how a future artifact bundle produced by v0.4.26-style pipeline outputs can become the input to clean-install consumption proof. It does not create, commit, upload, store, publish, package, install, or smoke real generated artifacts.

Focused suite: `tests/suites/go-kernel-v0427-artifact-bundle-contract.cjs`.

Bundle shape required for future consumption input:

| Bundle file | Required role | Slice 2 fixture behavior |
| --- | --- | --- |
| helper executable | parser-only `tmuxSnapshotParse` helper candidate | fake helper file created under OS temp root only |
| `manifest.json` | bundle manifest and v0.4.26 generator-compatible metadata | generated under OS temp root only and removed after test |
| `SHA256SUMS` | checksum file for helper and metadata handoff | generated under OS temp root only and validated against helper checksum |
| `provenance.json` | provenance placeholder source/workflow/toolchain/generatedAt metadata | placeholder only, not release provenance |
| license metadata/copy/checksum | license name/path/copy/checksum metadata | temp license file plus checksum, not package metadata |
| attestation/signing placeholder | future signing/attestation slot | placeholder-only and explicitly not real signing |

Naming and version dimensions required in bundle metadata:

- `module` = `tmuxSnapshotParse`.
- `helperVersion` identifies the generated helper artifact version.
- `protocolVersion` = `1` until separately changed by protocol owner approval.
- `packageVersion` = `0.6.8` for this repository state.
- `os` and `arch` are required.
- linux bundles must include `libc` such as `glibc` or `musl` where applicable.
- target identity must be package-relative and include enough module/version/platform scope to avoid cross-target reuse.

Safe package-relative path policy:

- accepted bundle metadata contains package-relative paths only.
- no absolute paths.
- no `..` traversal.
- no backslash-only platform escape.
- no repo/cwd/temp absolute path leakage in accepted metadata.
- helper, manifest, checksum, provenance, license, and attestation paths must resolve under the test-local OS temp bundle root during fixture validation.
- failure output must not leak absolute helper path, temp root, repo/cwd, stdout/stderr, raw manifest/provenance/license/attestation body, stack traces, or package internals.

Compatibility expectations:

- bundle manifest shape remains compatible with v0.4.26 generator metadata fields: schemaVersion, packageName/packageVersion, module, helperVersion, protocolVersion, capabilities, OS/arch/libc, safe artifact path, size, SHA-256, executable policy, provenance placeholder, license checksum, and attestation placeholder.
- bundle validation remains compatible with v0.4.25 manifest validation concepts: strict module/protocol/capability/package version checks, supported platform dimensions, linux libc requirement, checksum/size match, executable policy, provenance/license presence, and fail-closed handling for unsafe paths.
- generated-temp bundle metadata is future input contract only, not a release artifact, not normal-user availability proof, not package/default/fallback approval, and not package metadata.

Slice 2 temp-fixture evidence may create a fake bundle under an OS temp root, validate the shape, run negative fail-closed/no-leak cases, and clean the temp root. Repository scans must confirm no checked-in generated bundle, artifact, manifest, tarball, native binary, release metadata, or package artifact exists outside allowed test source.

Slice 2 preserves Slice 1 boundaries: TS/pi control plane mandatory, Go helper behind TS adapter/ports via subprocess/RPC/stdin-stdout, parser-only `tmuxSnapshotParse`, no native Go pi extension/provider ABI assumption, no production clean-install consumption implementation, no package manager install behavior changes, no production resolver behavior changes, no default resolver activation, no default Go, no `go-cutover` or `go-packaged-preview` semantic changes, no TypeScript fallback deletion, no `/team readiness` expansion, and no Slice 3 install layout matrix.

Slice 2 STOP gates:

- STOP for real generated pipeline artifacts.
- STOP for checked-in generated bundles.
- STOP for helper build commands.
- STOP for running `go build`.
- STOP for CI workflow implementation.
- STOP for upload or artifact storage.
- STOP for active GitHub Actions artifact storage.
- STOP for GitHub release assets.
- STOP for package metadata changes.
- STOP for optionalDependencies.
- STOP for package scripts.
- STOP for lifecycle hooks/downloads.
- STOP for postinstall/download/install-time build.
- STOP for npm pack/version/publish.
- STOP for go.mod/go.sum or lockfiles.
- STOP for native binaries or tarballs.
- STOP for production runtime resolver changes.
- STOP for default modes or default resolver activation.
- STOP for current `go-cutover` behavior changes.
- STOP for `go-packaged-preview` availability semantics changes.
- STOP for TypeScript fallback deletion.
- STOP for broadening Go authority beyond parser-only stdin/stdout `tmuxSnapshotParse`.
- STOP for `/team readiness` changes.
- STOP for Slice 3 work without separate approval.

## Slice 3 — Future Package / Install Layout Decision Matrix

Slice 3 is docs/tests-only install layout decision matrix work. It compares future package/install layout options for consuming the Slice 2 artifact bundle contract in a clean-install proof. It does not add package metadata, optionalDependencies, package files entries, package scripts, lifecycle hooks, package manager install simulation, npm tarballs, npm pack/version/publish, native binaries, generated manifests, generated package artifacts, production resolver behavior, default discovery, or Slice 4 clean-install simulation.

Focused suite: `tests/suites/go-kernel-v0427-install-layout-matrix-docs.cjs`.

Candidate install layouts:

| Candidate layout | Package-relative installed path concept | Current Slice 3 decision | Evidence boundary |
| --- | --- | --- | --- |
| future platform companion package preferred path | `node_modules/<future-companion>/native/tmuxSnapshotParse/<helperVersion>/<platform>/agentteam-tmuxSnapshotParse` plus adjacent metadata | preferred future package-owner path only | no companion package metadata, optionalDependencies, package files, install, or publish approval |
| main package bundled fallback path | `native/tmuxSnapshotParse/<helperVersion>/<platform>/agentteam-tmuxSnapshotParse` plus adjacent metadata | STOP until future approval; fallback option for comparison only | no main package inclusion, no package files change, no generated artifacts checked in |
| unsupported os/arch/libc rows | no installed path claim | fail-closed unsupported rows | no normal-user availability proof and no default/native/fallback approval |

Package-relative installed metadata paths for future contract discussion only:

- helper path: `native/tmuxSnapshotParse/<helperVersion>/<platform>/agentteam-tmuxSnapshotParse` or `.exe` on Windows.
- manifest path: `native/tmuxSnapshotParse/<helperVersion>/<platform>/manifest.json`.
- checksum path: `native/tmuxSnapshotParse/<helperVersion>/<platform>/SHA256SUMS`.
- provenance path: `native/tmuxSnapshotParse/<helperVersion>/<platform>/provenance.json`.
- license path: `native/tmuxSnapshotParse/<helperVersion>/<platform>/LICENSE` or `license.json`.
- package-relative paths are future contract examples only, not package files, not package metadata, not checked-in generated manifests, and not an install simulation.

Resolver input expectations for future clean-install consumption:

- manifest path and helper path are package-relative and traversal-safe.
- checksum, provenance, and license paths are package-relative and traversal-safe.
- platform tuple includes `os`, `arch`, and linux `libc` where applicable.
- `module` and `capability` must match `tmuxSnapshotParse`.
- `protocolVersion` must match the TS adapter protocol.
- `helperVersion` must match the installed helper layout version.
- `packageVersion` must match the consuming package version.
- checksum, provenance, license, platform tuple, module, capability, protocol, helper version, and package version skew fail closed.
- resolver input contract does not activate production default discovery in this slice.

Cleanup, upgrade, and stale-helper expectations:

- new helper layout replaces old layout only after future package-owner approval.
- stale helper layout fails closed and does not silently fall back to another helper.
- stale manifest/checksum/provenance/license metadata fails closed.
- helper/package version skew fails closed.
- protocol skew fails closed.
- platform skew fails closed.
- cleanup and upgrade behavior remains future package-manager/package-owner work and is not simulated in Slice 3.

Unsupported platform policy:

- unsupported os/arch/libc rows remain fail-closed.
- unsupported rows do not prove normal-user availability.
- unsupported rows do not permit default resolver activation, native/default cutover, package/native approval, or TypeScript fallback deletion.
- support claims require future generated artifacts, package ownership, clean-install proof, diagnostics, and explicit approval.

Package ownership decision boundary:

- companion package is a future package-owner decision only.
- main package inclusion is STOP until future approval.
- no package metadata is added.
- no optionalDependencies are added.
- no package files entries are changed.
- no package scripts or lifecycle hooks are added.
- no npm pack/version/publish is run or approved.

Slice 3 preserves Slice 1-2 boundaries: TS/pi control plane mandatory, Go helper behind TS adapter/ports via subprocess/RPC/stdin-stdout, parser-only `tmuxSnapshotParse`, no native Go pi extension/provider ABI assumption, no production clean-install consumption implementation, no package manager install simulation, no npm tarball behavior, no production resolver behavior changes, no default discovery, no `go-cutover` or `go-packaged-preview` semantic changes, no TypeScript fallback deletion, no `/team readiness` expansion, and no Slice 4 clean-install simulation.

Slice 3 STOP gates:

- STOP for package.json metadata/files/optionalDependencies/scripts/version changes.
- STOP for package manager install simulation.
- STOP for npm tarball behavior.
- STOP for npm pack/version/publish.
- STOP for lifecycle hooks.
- STOP for postinstall/download/install-time build.
- STOP for native binaries.
- STOP for tarballs.
- STOP for generated manifests.
- STOP for generated package artifacts.
- STOP for go.mod/go.sum.
- STOP for lockfiles.
- STOP for production resolver implementation.
- STOP for default discovery.
- STOP for default modes changes.
- STOP for current `go-cutover` behavior changes.
- STOP for `go-packaged-preview` availability semantics changes.
- STOP for TypeScript fallback deletion.
- STOP for `/team readiness` expansion.
- STOP for broadening Go authority beyond parser-only stdin/stdout `tmuxSnapshotParse`.
- STOP for Slice 4 work without separate approval.

## Slice 4 — Clean-Install Consumption Simulation

Slice 4 is tests/docs/temp-fixture only. It simulates artifact bundle → future installed layout → validate/smoke clean-install consumption proof under OS temp roots. It does not perform real package-manager install, npm tarball behavior, npm pack/version/publish, package metadata/files changes, lifecycle hooks, postinstall/download/install-time build, production resolver/runtime discovery, default discovery, default Go, `go-cutover` or `go-packaged-preview` semantic changes, TypeScript fallback deletion, `/team readiness` expansion, or Slice 5 resolver discovery contract work.

Focused suite: `tests/suites/go-kernel-v0427-clean-install-consumption.cjs`.

Simulation flow:

- create a fake artifact bundle under an OS temp root using Slice 2 bundle shape.
- copy/map the fake bundle into a separate temp installed layout using package-relative paths only.
- validate the installed manifest, checksum file, executable policy, license metadata/copy/checksum, provenance placeholder, attestation/signing placeholder, module, capability, protocolVersion, helperVersion, packageVersion, and os/arch/libc tuple.
- run direct explicit test-path smoke only; do not use production default resolver or runtime discovery.
- smoke proof uses deterministic health response and minimal `tmuxSnapshotParse` capability response.
- assert no source checkout dependency.
- assert no Go toolchain.
- assert no network.
- assert no lifecycle download.
- assert no install-time build.
- assert no manual helper env.
- clean up OS temp roots.

Installed layout path policy:

- installed helper path uses package-relative future contract shape such as `native/tmuxSnapshotParse/<helperVersion>/<platform>/agentteam-tmuxSnapshotParse`.
- installed manifest path uses `native/tmuxSnapshotParse/<helperVersion>/<platform>/manifest.json`.
- installed checksum path uses `native/tmuxSnapshotParse/<helperVersion>/<platform>/SHA256SUMS`.
- installed provenance path uses `native/tmuxSnapshotParse/<helperVersion>/<platform>/provenance.json`.
- installed license path uses `native/tmuxSnapshotParse/<helperVersion>/<platform>/LICENSE` or `license.json`.
- accepted installed metadata must not contain absolute helper paths, temp root paths, repo/cwd paths, or package internals beyond safe package-relative contract paths.

Positive supported-row smoke:

- supported row: linux x64 glibc temp fixture.
- explicit test path only.
- deterministic health response includes `ok: true`, `module: tmuxSnapshotParse`, `capabilities: [tmuxSnapshotParse]`, `protocolVersion: 1`, `helperVersion`, and `packageVersion`.
- deterministic parser/capability smoke proves minimal `tmuxSnapshotParse` capability only.
- success result is fixture-only and not normal-user availability proof.

Negative fail-closed cases:

- missing helper.
- wrong platform/libc.
- non-executable POSIX helper.
- checksum mismatch.
- stale helper.
- wrong package version.
- wrong helper version.
- wrong protocol version.
- wrong capability.
- missing license metadata/copy/checksum.
- missing provenance placeholder.
- missing attestation placeholder or real signing claim without proof.
- corrupt smoke output.
- attempted default resolver use.
- attempted hidden TS parser fallback.

No-leak behavior:

- failure output must not include helper absolute path.
- failure output must not include temp root.
- failure output must not include repo/cwd.
- failure output must not include stdout/stderr.
- failure output must not include raw manifest/checksum/provenance/license/attestation body.
- failure output must not include stack traces.
- failure output must not include package internals.
- failure output must not include mailbox/report text.

Slice 4 preserves Slice 1-3 boundaries: TS/pi control plane mandatory, Go helper behind TS adapter/ports via subprocess/RPC/stdin-stdout, parser-only `tmuxSnapshotParse`, no native Go pi extension/provider ABI assumption, no real package manager install, no npm tarball behavior, no production resolver/runtime discovery, no default discovery, no default Go, no `go-cutover` or `go-packaged-preview` semantic changes, no TypeScript fallback deletion, no `/team readiness` expansion, no broad Go authority, and no Slice 5 resolver discovery contract.

Slice 4 STOP gates:

- STOP for running `go build`.
- STOP for helper build commands.
- STOP for CI workflow.
- STOP for artifact upload.
- STOP for release assets.
- STOP for real package manager install.
- STOP for npm tarball behavior.
- STOP for npm pack/version/publish.
- STOP for package metadata/files/optionalDependencies/scripts changes.
- STOP for lifecycle hooks.
- STOP for postinstall/download/install-time build.
- STOP for native binaries.
- STOP for tarballs.
- STOP for generated manifests checked into the repo.
- STOP for generated package artifacts.
- STOP for go.mod/go.sum.
- STOP for lockfiles.
- STOP for production resolver implementation.
- STOP for runtime discovery.
- STOP for default discovery.
- STOP for default Go.
- STOP for current `go-cutover` behavior changes.
- STOP for `go-packaged-preview` availability semantics changes.
- STOP for TypeScript fallback deletion.
- STOP for `/team readiness` expansion.
- STOP for broadening Go authority beyond parser-only stdin/stdout `tmuxSnapshotParse`.
- STOP for Slice 5 work without separate approval.

## Slice 5 — Resolver Discovery Contract Without Behavior Change

Slice 5 is docs/tests-only resolver discovery contract work, with simulated discovery helper logic only inside tests. It defines how a future approved resolver could discover a helper from the Slice 3/4 installed layout contract. It does not implement production resolver behavior, runtime discovery, default discovery, default Go, packaged discovery activation, `go-cutover` changes, `go-packaged-preview` semantic changes, TypeScript fallback deletion, hidden fallback rollback, `/team readiness` expansion, package metadata changes, build/CI/artifact/package behavior, or Slice 6 failure rollback/no-leak hardening.

Focused suite: `tests/suites/go-kernel-v0427-resolver-discovery-contract.cjs`.

Future resolver inputs:

- installed root.
- package-relative manifest path.
- package-relative helper path.
- package-relative checksum path.
- package-relative provenance path.
- package-relative license path.
- platform tuple: `os`, `arch`, and linux `libc` where applicable.
- `module` = `tmuxSnapshotParse`.
- `capability` includes `tmuxSnapshotParse`.
- `protocolVersion` matches the TS adapter protocol.
- `helperVersion` matches the installed layout metadata.
- `packageVersion` matches the consuming package version.

Path rules:

- accepted resolver metadata uses package-relative paths only.
- no absolute paths.
- no `..` traversal.
- no backslash-only platform escape.
- no repo/cwd/temp root leakage in accepted metadata.
- future resolver may resolve package-relative paths only under its approved installed root.
- failure output must not leak helper absolute path, installed root, repo/cwd, stdout/stderr, raw manifest/checksum/provenance/license body, stack traces, package internals, mailbox text, or report text.

Platform matching:

- `os` exact match required.
- `arch` exact match required.
- linux `libc` exact match required.
- unsupported os/arch/libc rows fail closed.
- unsupported rows do not prove normal-user availability.
- unsupported rows do not permit default resolver activation, native/default cutover, package/native approval, or TypeScript fallback deletion.

Precedence and behavior boundaries:

- explicit helper path remains highest precedence.
- simulated discovery is future-approved only.
- current default/unset mode must not read packaged layout.
- current disabled mode must not read packaged layout.
- current typescript mode must not read packaged layout.
- current go mode must not read packaged layout.
- current auto mode must not read packaged layout.
- current `go-cutover` must not read packaged layout.
- current `go-packaged-preview` semantics remain unchanged.
- `go-packaged-preview` remains explicit-only and non-default.
- production `core/kernel.ts` behavior remains unchanged.
- packaged discovery is not activated in this slice.

Failure mapping for simulated future discovery:

| Failure | Compact failure kind | Release decision |
| --- | --- | --- |
| missing manifest | `manifest_missing` | fail closed; no default/native/fallback approval |
| missing helper | `helper_missing` | fail closed; no default/native/fallback approval |
| invalid path | `invalid_path` | fail closed; no default/native/fallback approval |
| unsupported platform | `unsupported_platform` | fail closed; no normal-user availability proof |
| checksum mismatch | `integrity_mismatch` | fail closed; no package/default/fallback approval |
| provenance mismatch or missing | `provenance_mismatch` | fail closed; no package/default/fallback approval |
| license mismatch or missing | `license_mismatch` | fail closed; no package/default/fallback approval |
| package/helper version skew | `version_skew` | fail closed; no hidden TS fallback rollback |
| protocol skew | `protocol_skew` | fail closed; no hidden TS fallback rollback |
| capability skew | `capability_skew` | fail closed; no hidden TS fallback rollback |
| non-executable helper | `artifact_not_executable` | fail closed; no default/native approval |
| corrupt smoke output | `helper_smoke_failed` | fail closed; no hidden TS fallback rollback |

Runtime authority invariants:

- `compactReadModelFingerprint` remains non-cutover / TypeScript fallback.
- `tmuxSnapshotParse` remains the only cutover-owned candidate under discussion.
- Go authority stays parser-only behind TS adapter/ports via subprocess/RPC/stdin-stdout.
- Go does not own tmux execution/capture, state, worker lifecycle, task/report governance, PlanRun, full-text boundaries, package/release authority, UI rendering, or command control plane.

Slice 5 preserves Slice 1-4 boundaries: TS/pi control plane mandatory, no native Go pi extension/provider ABI assumption, no production resolver/runtime discovery, no default discovery, no packaged discovery activation in current modes, no `go-packaged-preview` semantic change, no default Go, no TypeScript fallback deletion, no hidden fallback rollback, no `/team readiness` expansion, no package/build/CI/artifact changes, and no Slice 6 failure rollback/no-leak hardening.

Slice 5 STOP gates:

- STOP for production resolver implementation.
- STOP for runtime discovery implementation.
- STOP for default discovery.
- STOP for packaged discovery activation in default/unset/disabled/typescript/go/auto/current `go-cutover`.
- STOP for `go-packaged-preview` semantic changes.
- STOP for helper path precedence changes.
- STOP for default Go.
- STOP for TypeScript fallback deletion.
- STOP for hidden fallback rollback.
- STOP for `/team readiness` expansion.
- STOP for package metadata changes.
- STOP for package files changes.
- STOP for optionalDependencies.
- STOP for package scripts.
- STOP for lifecycle hooks.
- STOP for build commands.
- STOP for running `go build`.
- STOP for CI workflow.
- STOP for artifact upload.
- STOP for release assets.
- STOP for npm pack/version/publish.
- STOP for native binaries.
- STOP for tarballs.
- STOP for generated artifacts checked into the repo.
- STOP for generated manifests checked into the repo.
- STOP for generated package artifacts.
- STOP for broadening Go authority beyond parser-only stdin/stdout `tmuxSnapshotParse`.
- STOP for Slice 6 work without separate approval.

## Slice 6 — Failure Rollback No-Leak Hardening

Slice 6 is docs/tests-only failure, rollback, unsupported-platform, and no-leak hardening for the v0.4.27 install-consumption layer. It applies v0.4.25/v0.4.26 compact fail-closed diagnostics and rollback policy to future clean-install consumption evidence. It does not implement production diagnostics, production resolver behavior, runtime discovery, default discovery, normal-user UI, model-callable tools, ambient `/team` diagnostics, `/team readiness` expansion, default Go, TypeScript fallback deletion, hidden TS fallback rollback, package/build/CI/artifact/native metadata, or Slice 7 guardrail consolidation.

Focused suite: `tests/suites/go-kernel-v0427-consumption-failure-rollback-no-leak.cjs`.

Compact failure vocabulary for install consumption:

| Failure kind | Status / marker | Remediation | Blocker outcome |
| --- | --- | --- | --- |
| `artifact_missing` | `unavailable` / `fail-closed` | regenerate or republish the missing consumed artifact | block normal-user availability, default/native approval, and fallback deletion |
| `metadata_invalid` | `unavailable` / `fail-closed` | regenerate package-relative manifest/checksum/provenance/license metadata | block package/default/native/fallback approval |
| `integrity_mismatch` | `unavailable` / `fail-closed` | reject artifact and regenerate checksum/provenance/license integrity evidence | block package/default/native/fallback approval |
| `artifact_not_executable` | `unavailable` / `fail-closed` | restore executable policy in a corrected artifact release | block default/native approval |
| `unsupported_platform` | `unavailable` / `fail-closed` | keep unsupported row unavailable unless support policy is narrowed and approved | block normal-user availability, default/native approval, and fallback deletion |
| `version_skew` | `unavailable` / `fail-closed` | align helperVersion/packageVersion with the installed layout contract | block hidden TS fallback rollback and fallback deletion |
| `protocol_skew` | `unavailable` / `fail-closed` | align protocolVersion with the TS adapter protocol | block hidden TS fallback rollback and fallback deletion |
| `capability_skew` | `unavailable` / `fail-closed` | reject helper unless `tmuxSnapshotParse` capability/module match | block hidden TS fallback rollback and broad Go authority |
| `license_missing` | `unavailable` / `fail-closed` | regenerate license metadata/copy/checksum | block package/native/default approval |
| `provenance_missing` | `unavailable` / `fail-closed` | regenerate provenance placeholder/evidence metadata | block package/native/default approval |
| `install_layout_invalid` | `unavailable` / `fail-closed` | regenerate package-relative installed layout and reject unsafe paths | block clean-install consumption approval |
| `package_unavailable` | `unavailable` / `fail-closed` | publish corrected package/tag/deprecation/default-disable policy before retry | block normal-user availability and default/native approval |
| `smoke_corrupt_output` | `unavailable` / `fail-closed` | reject corrupt explicit smoke output and publish corrected artifact evidence | block hidden TS fallback rollback and fallback deletion |

Compact diagnostic shape:

- allowed fields: `module`, `capability`, `status`, `resultMarker`, `failureKind`, `remediation`, `releaseDecision`, `blockerStatus`, and `rollbackPolicy`.
- `status` is `unavailable` for every failure case.
- `resultMarker` is `fail-closed` for every failure case.
- `releaseDecision` blocks clean-install consumption, normal-user availability, package/native/default approval, and fallback deletion until the owner accepts corrected evidence.
- `blockerStatus` remains `blocked` until corrected release/package/metadata/provenance/license/smoke evidence is reviewed.
- `rollbackPolicy` is corrected release/tag/package/deprecation/default-disable policy.
- compact diagnostics are reviewer/test-visible contract output only, not normal-user UI.

No-leak expectations:

- diagnostics and test-visible outputs must not include helper absolute path.
- diagnostics and test-visible outputs must not include temp root or installed root.
- diagnostics and test-visible outputs must not include repo/cwd.
- diagnostics and test-visible outputs must not include stdout/stderr.
- diagnostics and test-visible outputs must not include raw manifest/checksum/provenance/license body.
- diagnostics and test-visible outputs must not include raw package internals.
- diagnostics and test-visible outputs must not include stack traces.
- diagnostics and test-visible outputs must not include mailbox/report text.

Unsupported-platform policy:

- unsupported os/arch/libc rows fail closed.
- unsupported rows continue blocking normal-user availability.
- unsupported rows continue blocking default resolver activation, native/default cutover, package/native approval, and fallback deletion.
- unsupported rows may only become supported after support policy is narrowed and explicitly approved with generated artifacts, package ownership, clean-install proof, diagnostics, and release rollback policy.

Rollback policy:

- rollback remains corrected release/tag/package/deprecation/default-disable policy.
- rollback is not hidden runtime TypeScript fallback after cutover.
- explicit future consumption smoke/cutover failure must not silently use hidden TS parser fallback.
- TypeScript fallback remains present for existing non-cutover paths; it is not deleted by this slice.
- `compactReadModelFingerprint` remains TypeScript fallback / non-cutover.
- `tmuxSnapshotParse` remains the only cutover-owned candidate under discussion.

Slice 6 preserves Slice 1-5 boundaries: TS/pi control plane mandatory, parser-only Go authority behind TS adapter/ports, no native Go pi extension/provider ABI assumption, no production resolver/runtime/default discovery behavior changes, no `go-cutover` or `go-packaged-preview` semantic changes, no normal-user UI/readiness/tooling expansion, no package/build/CI/artifact/native metadata/files, no hidden TS fallback rollback, and no Slice 7 package/native/default/fallback/readiness guardrail consolidation.

Slice 6 STOP gates:

- STOP for normal-user UI.
- STOP for model-callable tools.
- STOP for ambient `/team` diagnostics.
- STOP for `/team readiness` expansion.
- STOP for production diagnostics changes.
- STOP for production resolver implementation.
- STOP for runtime discovery implementation.
- STOP for default discovery.
- STOP for packaged discovery activation.
- STOP for default Go.
- STOP for TypeScript fallback deletion.
- STOP for hidden TS fallback rollback after cutover.
- STOP for hidden TS parser fallback for explicit future consumption smoke/cutover failure.
- STOP for `go-cutover` behavior changes.
- STOP for `go-packaged-preview` semantic changes.
- STOP for package metadata changes.
- STOP for package files changes.
- STOP for optionalDependencies.
- STOP for package scripts.
- STOP for lifecycle hooks.
- STOP for helper build commands.
- STOP for running `go build`.
- STOP for CI workflow.
- STOP for artifact upload.
- STOP for release assets.
- STOP for npm pack/version/publish.
- STOP for native binaries.
- STOP for tarballs.
- STOP for generated artifacts checked into the repo.
- STOP for generated manifests checked into the repo.
- STOP for generated package artifacts.
- STOP for go.mod/go.sum.
- STOP for lockfiles.
- STOP for broadening Go authority beyond parser-only stdin/stdout `tmuxSnapshotParse`.
- STOP for Slice 7 work without separate approval.

## Slice 7 — Package Native Guardrails and Readiness Containment

Slice 7 is docs/tests-only package/native/readiness/runtime guardrail consolidation. It centralizes the v0.4.27 evidence that the clean-install consumption checkpoint has not changed package metadata, native artifact packaging, runtime modes/defaults, readiness/UI/tooling surfaces, or Go authority. It does not add final checkpoint doc/guard, final GO/STOP summary, Slice 8 work, production code, package/native artifacts, package metadata, CI/build/release behavior, runtime resolver/default behavior, readiness expansion, or UI/tool changes.

Focused suite: `tests/suites/go-kernel-v0427-package-native-guardrails.cjs`.

Package and npm guardrails:

- `package.json` version remains `0.6.8`.
- no `npm version`.
- no `npm publish`.
- no npm pack publish output or package metadata approval.
- no `optionalDependencies`.
- no native package metadata such as `agentteamGoHelper`.
- no package `files` inclusion for helper/native/generated artifacts.
- no package scripts for helper build/install/download/package/version/publish behavior.
- no lifecycle hooks.
- no postinstall/download/install-time build.
- no package version change.
- no package/native approval.

Module and lockfile guardrails:

- no lockfiles: `package-lock.json` or `npm-shrinkwrap.json`.
- no `go.mod`.
- no `go.sum`.
- no Go module files under `kernel/go/agentteam-kernel/`.

Repository artifact guardrails:

- no checked-in native binaries.
- no checked-in tarballs.
- no checked-in generated artifacts.
- no checked-in generated manifests.
- no checked-in checksum artifacts.
- no checked-in provenance artifacts.
- no checked-in attestation artifacts.
- no checked-in generated package artifacts.
- `.agentteam-artifacts/` and similar local prototype output, if mentioned, must remain ignored/excluded and must not be committed.
- tests may use OS temp fixtures only.

Build, CI, and release guardrails:

- no helper build commands.
- no running `go build`.
- no CI workflow implementation.
- no GitHub Actions artifact upload.
- no GitHub release assets.
- no active artifact storage.
- no npm companion package.
- no main package native inclusion.

Runtime and mode guardrails:

- default/unset remains safe disabled/TypeScript.
- no default Go.
- no production resolver implementation.
- no production default discovery.
- no runtime discovery activation.
- no packaged discovery activation.
- no `go-cutover` semantic changes.
- no `go-packaged-preview` semantic changes.
- `go-packaged-preview` remains explicit-only and non-default.
- TypeScript fallback is not deleted.
- `compactReadModelFingerprint` remains TypeScript fallback / non-cutover.
- `tmuxSnapshotParse` remains the only cutover-owned candidate under discussion.
- production `core/kernel.ts` behavior remains unchanged.

UI, tool, and readiness guardrails:

- `/team readiness` remains transitional reviewer tooling only.
- no new `/team readiness` options or subcommands.
- no new `/team` native/package/resolver diagnostics command.
- no model-callable native/readiness tools.
- no ambient `/team` panel diagnostics.
- no normal-user UI expansion.
- no runtime control plane expansion.
- readiness output remains non-proof and not normal-user native availability evidence.

Go authority guardrails:

- Go helper remains parser-only `tmuxSnapshotParse` candidate.
- Go remains behind TS adapter/ports via subprocess/RPC/stdin-stdout.
- Go does not own tmux execution/capture.
- Go does not own state writes.
- Go does not own worker lifecycle.
- Go does not own task/report governance.
- Go does not own PlanRun.
- Go does not own full-text boundaries.
- Go does not own package/release authority.
- Go does not own UI rendering.
- Go does not own command control plane.
- no native Go pi extension/provider ABI assumption.

Slice 7 preserves Slice 1-6 boundaries: docs/tests-only, GitHub-only evidence, no package/native/default/fallback/readiness approval, no production resolver/runtime/default discovery behavior change, no generated package/native outputs, no normal-user availability proof, no TypeScript fallback deletion, no hidden TS fallback rollback, no broader Go authority, and no Slice 8 final checkpoint work.

Slice 7 STOP gates:

- STOP for final checkpoint doc.
- STOP for final checkpoint guard.
- STOP for v0.4.27 final GO/STOP summary.
- STOP for Slice 8 work without separate approval.
- STOP for production code changes.
- STOP for package metadata changes.
- STOP for package files changes.
- STOP for package version change.
- STOP for `optionalDependencies`.
- STOP for package scripts.
- STOP for lifecycle hooks.
- STOP for postinstall/download/install-time build.
- STOP for npm version.
- STOP for npm publish.
- STOP for npm pack package output/approval.
- STOP for helper build commands.
- STOP for running `go build`.
- STOP for CI workflow.
- STOP for GitHub Actions artifact upload.
- STOP for GitHub release assets.
- STOP for native binaries.
- STOP for tarballs.
- STOP for generated artifacts/manifests/checksum/provenance/attestation/package artifacts.
- STOP for lockfiles.
- STOP for go.mod/go.sum.
- STOP for default Go.
- STOP for production resolver/default discovery.
- STOP for `go-cutover` or `go-packaged-preview` semantic changes.
- STOP for TypeScript fallback deletion.
- STOP for `/team readiness` expansion.
- STOP for model-callable native/readiness tools.
- STOP for ambient `/team` diagnostics.
- STOP for normal-user UI expansion.
- STOP for runtime control plane expansion.
- STOP for broadening Go authority beyond parser-only stdin/stdout `tmuxSnapshotParse`.
- STOP for commit/tag/push.

## Slice 8 — Final Checkpoint

Final checkpoint doc: `docs/perf/v0.4.27-generated-artifact-clean-install-consumption-checkpoint.md`.

Focused suite: `tests/suites/go-kernel-v0427-consumption-checkpoint-docs.cjs`.

Slice 8 is docs/tests-only final checkpoint review for accumulated GitHub-only v0.4.27 evidence. It summarizes Slice 1-7 evidence, GO/STOP decisions, validation matrix, remaining blockers, and unchanged runtime/package/default/native/fallback/readiness invariants. It does not implement release/package/build/default behavior, production resolver behavior, generated artifacts, CI/upload/storage/release assets, package metadata/files/scripts, normal-user UI/tool/runtime diagnostics, commit/tag/push, or v0.4.28 implementation work.

## Future v0.4.27 Evidence Areas

Future v0.4.27 review may use the final checkpoint for leader/user approval. Future implementation work belongs to separately approved v0.4.28 or later planning, not this checkpoint.

## Out of Scope

The following remain out of scope and STOP until separately approved:

- production clean-install consumption implementation.
- production runtime resolver behavior changes.
- package manager install behavior changes.
- npm version/publish.
- package version change.
- package.json metadata changes.
- optionalDependencies.
- lifecycle hooks/downloads.
- package scripts.
- lockfiles.
- go.mod/go.sum.
- helper build commands.
- running `go build`.
- CI workflow implementation.
- active GitHub Actions artifact storage.
- GitHub release assets.
- npm companion packages.
- main package inclusion.
- checked-in native binaries.
- tarballs.
- generated manifests/artifacts/package artifacts.
- default Go.
- current `go-cutover` behavior changes.
- `go-packaged-preview` availability semantics changes.
- TypeScript fallback deletion.
- hidden TS fallback rollback after cutover.
- `compactReadModelFingerprint` cutover.
- broad Go authority beyond parser-only stdin/stdout `tmuxSnapshotParse`.
- native Go pi extension assumption.
- `/team readiness` expansion.
- ambient `/team` diagnostics.
- model-callable native/readiness tools.
- normal-user native availability claims before proof is accepted.
- Slice 2 work without separate approval.

## Runtime, Package, and Readiness Invariants

Runtime invariants remain unchanged:

- default/unset remains disabled/TypeScript.
- disabled, typescript, go, auto, current `go-cutover`, and `go-packaged-preview` behavior remains unchanged.
- `go-packaged-preview` remains explicit-only and non-default.
- current `go-cutover` remains helper-path based and unchanged.
- packaged helper discovery does not run in default/disabled/typescript/go/auto/current `go-cutover`.
- `tmuxSnapshotParse` remains the only cutover-owned module under discussion.
- `compactReadModelFingerprint` remains TypeScript fallback / non-cutover.
- production runtime resolver behavior remains unchanged.
- production `core/kernel.ts` behavior remains unchanged.
- TS/pi control plane remains mandatory.
- Go helper remains parser-only stdin/stdout `tmuxSnapshotParse` behind TS adapter/ports via subprocess/RPC/stdin-stdout.

Package/native invariants remain unchanged:

- `package.json` version remains `0.6.8`.
- no package/native artifacts/metadata changes.
- no `optionalDependencies`.
- no lifecycle hooks/downloads.
- no package scripts for helper build/install/download/package/version/publish behavior.
- no lockfiles.
- no `go.mod`/`go.sum`.
- no checked-in native binaries.
- no tarballs.
- no generated manifests/artifacts.
- no generated package artifacts.
- no real package inclusion or native artifact approval.

Readiness invariants remain unchanged:

- `/team readiness` remains transitional reviewer tooling only.
- no `/team readiness` options or subcommands are added.
- no ambient `/team` panel diagnostics are rendered.
- no model-callable readiness/native tools are added.
- readiness output is not normal-user native availability proof.

## STOP Gates

STOP future v0.4.27 work if any of these appear without explicit owner approval:

- production clean-install consumption implementation.
- production runtime resolver behavior changes.
- package manager install behavior changes.
- package.json change.
- package version change.
- npm version.
- npm publish.
- optionalDependencies.
- lifecycle hooks/downloads.
- package scripts.
- helper build commands.
- running `go build`.
- CI workflow implementation.
- active GitHub Actions artifact storage.
- GitHub release assets.
- npm companion packages.
- main package inclusion.
- lockfiles.
- go.mod/go.sum.
- checked-in native binaries.
- tarballs.
- generated manifests/artifacts/package artifacts.
- default Go enablement.
- current `go-cutover` behavior changes.
- `go-packaged-preview` availability semantics changes.
- TypeScript fallback deletion.
- hidden TS fallback rollback after cutover.
- `compactReadModelFingerprint` cutover.
- broader Go authority.
- native Go pi extension assumption.
- readiness expansion.
- ambient `/team` diagnostics.
- model-callable native/readiness tool.
- normal-user native availability claims before proof is accepted.
- Slice 2 work without separate approval.

## Slice 1 Validation Plan

Required Slice 1 validation:

- syntax check: `node --check tests/suites/go-kernel-v0427-clean-install-consumption-contract-docs.cjs`
- focused guard: `node tests/run.cjs go-kernel-v0427-clean-install-consumption-contract-docs`
- package/native sanity scan inside the focused guard.
- repo artifact/tarball/generated manifest/package artifact scan inside the focused guard.
- runtime/default/readiness invariant scan inside the focused guard.
- `git diff --check`

Recommended broader validation when practical:

- `node tests/run.cjs`
- `npm run typecheck`
- `npm run -s check:boundaries`

Future v0.4.27 checkpoint validation must include:

- generated artifact consumption fixture contract guard.
- clean installed-layout input mapping guard.
- package-relative resolver precondition guard.
- direct helper smoke from consumed artifact guard.
- fail-closed/no-leak diagnostics guard.
- offline/cache/no-network expectation guard.
- rollback/default-disable/deprecation policy guard.
- package/native/default/fallback/readiness invariant guardrail consolidation.
- package/native sanity scan.
- repo artifact/tarball/generated manifest/package artifact scan.
- `node tests/run.cjs`.
- `npm run typecheck`.
- `git diff --check`.

## Slice 2 Validation Plan

Required Slice 2 validation:

- syntax check: `node --check tests/suites/go-kernel-v0427-artifact-bundle-contract.cjs`
- focused guard: `node tests/run.cjs go-kernel-v0427-artifact-bundle-contract`
- Slice 1 guard still passes: `node tests/run.cjs go-kernel-v0427-clean-install-consumption-contract-docs`
- package/native sanity scan inside the focused guard.
- repo artifact/tarball/generated bundle/manifest/package artifact scan inside the focused guard.
- OS temp bundle fixture cleanup inside the focused guard.
- `git diff --check`

Recommended broader validation when practical:

- `node tests/run.cjs`
- `npm run typecheck`
- `npm run -s check:boundaries`

## Slice 3 Validation Plan

Required Slice 3 validation:

- syntax check: `node --check tests/suites/go-kernel-v0427-install-layout-matrix-docs.cjs`
- focused guard: `node tests/run.cjs go-kernel-v0427-install-layout-matrix-docs`
- Slice 1 guard still passes: `node tests/run.cjs go-kernel-v0427-clean-install-consumption-contract-docs`
- Slice 2 guard still passes: `node tests/run.cjs go-kernel-v0427-artifact-bundle-contract`
- package/native sanity scan inside the focused guard.
- repo artifact/tarball/generated manifest/package artifact scan inside the focused guard.
- `git diff --check`

Recommended broader validation when practical:

- `node tests/run.cjs`
- `npm run typecheck`
- `npm run -s check:boundaries`

## Slice 4 Validation Plan

Required Slice 4 validation:

- syntax check: `node --check tests/suites/go-kernel-v0427-clean-install-consumption.cjs`
- focused guard: `node tests/run.cjs go-kernel-v0427-clean-install-consumption`
- Slice 1 guard still passes: `node tests/run.cjs go-kernel-v0427-clean-install-consumption-contract-docs`
- Slice 2 guard still passes: `node tests/run.cjs go-kernel-v0427-artifact-bundle-contract`
- Slice 3 guard still passes: `node tests/run.cjs go-kernel-v0427-install-layout-matrix-docs`
- package/native sanity scan inside the focused guard.
- repo artifact/tarball/generated bundle/manifest/package artifact scan inside the focused guard.
- OS temp bundle and installed-layout cleanup inside the focused guard.
- `git diff --check`

Recommended broader validation when practical:

- `node tests/run.cjs`
- `npm run typecheck`
- `npm run -s check:boundaries`

## Slice 5 Validation Plan

Required Slice 5 validation:

- syntax check: `node --check tests/suites/go-kernel-v0427-resolver-discovery-contract.cjs`
- focused guard: `node tests/run.cjs go-kernel-v0427-resolver-discovery-contract`
- Slice 1 guard still passes: `node tests/run.cjs go-kernel-v0427-clean-install-consumption-contract-docs`
- Slice 2 guard still passes: `node tests/run.cjs go-kernel-v0427-artifact-bundle-contract`
- Slice 3 guard still passes: `node tests/run.cjs go-kernel-v0427-install-layout-matrix-docs`
- Slice 4 guard still passes: `node tests/run.cjs go-kernel-v0427-clean-install-consumption`
- package/native sanity scan inside the focused guard.
- repo artifact/tarball/generated bundle/manifest/package artifact scan inside the focused guard.
- current kernel mode and packaged preview invariant scan inside the focused guard.
- `git diff --check`

Recommended broader validation when practical:

- `node tests/run.cjs`
- `npm run typecheck`
- `npm run -s check:boundaries`

## Slice 6 Validation Plan

Required Slice 6 validation:

- syntax check: `node --check tests/suites/go-kernel-v0427-consumption-failure-rollback-no-leak.cjs`
- focused guard: `node tests/run.cjs go-kernel-v0427-consumption-failure-rollback-no-leak`
- Slice 1 guard still passes: `node tests/run.cjs go-kernel-v0427-clean-install-consumption-contract-docs`
- Slice 2 guard still passes: `node tests/run.cjs go-kernel-v0427-artifact-bundle-contract`
- Slice 3 guard still passes: `node tests/run.cjs go-kernel-v0427-install-layout-matrix-docs`
- Slice 4 guard still passes: `node tests/run.cjs go-kernel-v0427-clean-install-consumption`
- Slice 5 guard still passes: `node tests/run.cjs go-kernel-v0427-resolver-discovery-contract`
- failure vocabulary/no-leak simulated diagnostics scan inside the focused guard.
- unsupported-platform rollback policy scan inside the focused guard.
- no hidden TS fallback/cutover source invariant scan inside the focused guard.
- package/native sanity scan inside the focused guard.
- repo artifact/tarball/generated bundle/manifest/package artifact scan inside the focused guard.
- `git diff --check`

Recommended broader validation when practical:

- `node tests/run.cjs`
- `npm run typecheck`
- `npm run -s check:boundaries`

## Slice 7 Validation Plan

Required Slice 7 validation:

- syntax check: `node --check tests/suites/go-kernel-v0427-package-native-guardrails.cjs`
- focused guard: `node tests/run.cjs go-kernel-v0427-package-native-guardrails`
- Slice 1 guard still passes: `node tests/run.cjs go-kernel-v0427-clean-install-consumption-contract-docs`
- Slice 2 guard still passes: `node tests/run.cjs go-kernel-v0427-artifact-bundle-contract`
- Slice 3 guard still passes: `node tests/run.cjs go-kernel-v0427-install-layout-matrix-docs`
- Slice 4 guard still passes: `node tests/run.cjs go-kernel-v0427-clean-install-consumption`
- Slice 5 guard still passes: `node tests/run.cjs go-kernel-v0427-resolver-discovery-contract`
- Slice 6 guard still passes: `node tests/run.cjs go-kernel-v0427-consumption-failure-rollback-no-leak`
- package/native guardrail scan inside the focused guard.
- repo artifact/tarball/generated/checksum/provenance/attestation/package artifact scan inside the focused guard.
- runtime/mode invariant scan inside the focused guard.
- UI/tool/readiness containment scan inside the focused guard.
- Go authority invariant scan inside the focused guard.
- Slice 8/final checkpoint absence scan inside the focused guard.
- `git diff --check`

Recommended broader validation when practical:

- `node tests/run.cjs`
- `npm run typecheck`
- `npm run -s check:boundaries`

## Slice 8 Validation Plan

Required Slice 8 validation:

- syntax check: `node --check tests/suites/go-kernel-v0427-consumption-checkpoint-docs.cjs`
- focused guard: `node tests/run.cjs go-kernel-v0427-consumption-checkpoint-docs`
- Slice 1 guard still passes: `node tests/run.cjs go-kernel-v0427-clean-install-consumption-contract-docs`
- Slice 2 guard still passes: `node tests/run.cjs go-kernel-v0427-artifact-bundle-contract`
- Slice 3 guard still passes: `node tests/run.cjs go-kernel-v0427-install-layout-matrix-docs`
- Slice 4 guard still passes: `node tests/run.cjs go-kernel-v0427-clean-install-consumption`
- Slice 5 guard still passes: `node tests/run.cjs go-kernel-v0427-resolver-discovery-contract`
- Slice 6 guard still passes: `node tests/run.cjs go-kernel-v0427-consumption-failure-rollback-no-leak`
- Slice 7 guard still passes: `node tests/run.cjs go-kernel-v0427-package-native-guardrails`
- package/native sanity scan inside the focused guard.
- no checked-in generated output scan inside the focused guard.
- runtime/default/readiness invariant scan inside the focused guard.
- `git diff --check`

Recommended broader validation when practical:

- `node tests/run.cjs`
- `npm run typecheck`
- `npm run -s check:boundaries`
- default bench: `npm run --silent bench:team-panel-tmux`
- packaged preview bench: `PI_AGENTTEAM_KERNEL=go-packaged-preview npm run --silent bench:team-panel-tmux`

## Final Recommendation

Slice 1 recommendation remains: Proceed only with v0.4.27 Slice 1 docs/tests-only generated artifact clean-install consumption owner contract review after leader/user approval.

Slice 2 recommendation remains: Proceed only with v0.4.27 Slice 2 docs/tests-only artifact bundle contract review after leader/user approval.

Slice 3 recommendation remains: Proceed only with v0.4.27 Slice 3 docs/tests-only future package/install layout matrix review after leader/user approval.

Slice 4 recommendation remains: Proceed only with v0.4.27 Slice 4 tests/docs/temp-fixture clean-install consumption simulation review after leader/user approval.

Slice 5 recommendation remains: Proceed only with v0.4.27 Slice 5 docs/tests-only resolver discovery contract review after leader/user approval.

Slice 6 recommendation remains: Proceed only with v0.4.27 Slice 6 docs/tests-only failure rollback no-leak hardening review after leader/user approval.

Slice 7 recommendation remains: Proceed only with v0.4.27 Slice 7 docs/tests-only package/native/readiness/runtime guardrail consolidation review after leader/user approval.

Proceed only with GitHub-only v0.4.27 Generated Artifact Clean-Install Consumption Gate checkpoint review after leader/user approval.

Do not implement production clean-install consumption, production diagnostics, production resolver behavior, runtime discovery, default discovery, packaged discovery activation, real package manager install, npm tarball behavior, normal-user UI, model-callable tools, ambient `/team` diagnostics, helper build commands, `go build`, CI workflows, upload/storage, active artifact storage, GitHub release assets, npm companion packages, package metadata/files/optionalDependencies/scripts/version changes, lifecycle hooks, postinstall/download/install-time build, npm version/publish/pack, native binaries, tarballs, generated artifacts/manifests/checksum/provenance/attestation checked into the repo, generated package artifacts, go.mod/go.sum, lockfiles, default Go, `go-cutover` or `go-packaged-preview` semantic changes, helper path precedence changes, TypeScript fallback deletion, hidden TS fallback rollback, `/team readiness` expansion, native Go pi extension assumptions, broader Go authority, commit/tag/push before leader/user approval, or v0.4.28 implementation work in this checkpoint slice.
