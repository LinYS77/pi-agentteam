# v0.6.37 v0.5 Release Readiness Burn-down Checkpoint

> Scope: final v0.6.37 docs/tests-only checkpoint for v0.5 release-readiness burn-down. This checkpoint summarizes Slice 1–7 local evidence, final GO/STOP status, remaining blockers, validation status, recommended next decisions, and unchanged package/runtime/default-Go/native/release/signing/platform boundaries.

## Result

Result: v0.6.37 is a docs/tests/fixtures release-readiness burn-down checkpoint ready for leader review.

The final v0.5 release-readiness result remains `ready:false`.

This checkpoint is GO only for reviewing local docs/tests/fixtures governance evidence.

This checkpoint is STOP for v0.5 release-ready approval, tag/release work, npm version/publish, package release, default-Go/default resolver, native/package helper delivery, signing/security attestations, second-platform support, fallback deletion, runtime optimization implementation, manual RC pass claims, p95 pass claims, broad `npm test` green claims, and any unresolved pane-health waiver.

v0.6.37 does not approve v0.5 release readiness.

v0.6.37 does not create, push, move, or imply a tag or release.

v0.6.37 does not run or approve `npm version`, `npm publish`, package release, install source approval, package metadata expansion, release assets, release bundles, or package artifacts.

v0.6.37 does not approve default Go, default resolver, native helper/package delivery, normal-user native availability, `go-cutover` defaulting, `go-packaged-preview` defaulting, signing, cosign, SLSA, security attestation, second-platform support, or TypeScript fallback deletion.

v0.6.37 does not implement runtime optimization, command/tool/readiness expansion, UI behavior changes, workflow behavior changes, or production TypeScript/Go runtime behavior changes.

The Slice 5 manual RC smoke checklist remains `defined-not-executed`.

The Slice 3 p95 release gates remain `defined-not-yet-proven`.

Broad `npm test` green is not claimed; the known `tests/suites/tools-state.cjs:577` pane-health mismatch remains unresolved until separately triaged.

`package.json` remains version `0.6.8`.

## Slice 1–8 Evidence Summary

Accepted local v0.6.37 slice evidence:

- Slice 1 — P0 readiness ledger: status `ledger-defined`; `docs/perf/v0.6.37-v0.5-release-readiness-burndown.md`, `tests/fixtures/kernel/v0637/p0ReadinessLedger.cjs`, and `tests/suites/go-kernel-v0637-v05-p0-readiness-ledger.cjs` record P0 seams, existing evidence, release impact, required proof, and STOP gates.
- Slice 2 — Performance baseline inventory: status `baseline-inventory-only`; `tests/fixtures/kernel/v0637/performanceBaselineInventory.cjs` and `tests/suites/go-kernel-v0637-v05-performance-baseline-inventory.cjs` inventory deterministic benchmark commands, output schemas, metadata, and no-leak expectations without p95 pass/fail interpretation.
- Slice 3 — p95 release gate definitions: status `defined-not-yet-proven`; `tests/fixtures/kernel/v0637/p95ReleaseGates.cjs` and `tests/suites/go-kernel-v0637-v05-p95-release-gates.cjs` define v0.5 p95 thresholds, fallback rules, evidence metadata, and pass/fail recording requirements.
- Slice 4 — Focused hot-path burn-down candidates: status `proposed-not-started`; `tests/fixtures/kernel/v0637/hotPathBurnDownCandidates.cjs` and `tests/suites/go-kernel-v0637-v05-hot-path-burndown-candidates.cjs` rank implementation candidates and recommend `panel-unchanged-state-render-suppression` first.
- Slice 5 — Manual RC smoke checklist: status `defined-not-executed`; `tests/fixtures/kernel/v0637/manualRcSmokeChecklist.cjs` and `tests/suites/go-kernel-v0637-v05-manual-rc-smoke-checklist.cjs` define clean-temp-home manual RC steps.
- Slice 6 — Validation strategy: status `strategy-defined`; `tests/fixtures/kernel/v0637/validationStrategy.cjs` and `tests/suites/go-kernel-v0637-v05-validation-strategy.cjs` define syntax, direct guard, broad regression, type/boundary/diff, performance evidence, and manual RC validation policy.
- Slice 7 — Task/report/PlanRun release reliability map: status `mapped-not-proven`; `tests/fixtures/kernel/v0637/taskReportPlanRunReliability.cjs` and `tests/suites/go-kernel-v0637-v05-task-report-planrun-reliability.cjs` map worker report, leader review, full-text boundary, PlanRun review/pause, peer handoff, and worker delegation requirements.
- Slice 8 — Final release-readiness checkpoint: status `checkpoint-complete-not-release-ready`; `docs/perf/v0.6.37-v0.5-release-readiness-burndown-checkpoint.md`, `tests/fixtures/kernel/v0637/finalReleaseReadinessCheckpoint.cjs`, and `tests/suites/go-kernel-v0637-v05-final-readiness-checkpoint-docs.cjs` summarize final GO/STOP state, blockers, validation status, and next decisions without release-ready approval.

## GO Matrix

GO for:

| GO item | Scope | Evidence | Limit |
| --- | --- | --- | --- |
| `local-docs-tests-governance-evidence` | Local docs/tests/fixtures governance evidence for leader review only. | `docs/perf/v0.6.37-v0.5-release-readiness-burndown.md`; this checkpoint doc; `tests/fixtures/kernel/v0637/*.cjs`; `tests/suites/go-kernel-v0637-v05-*.cjs`. | Does not approve v0.5 release readiness, tag, npm publish, runtime optimization, default-Go/native/package/signing/platform work, or manual RC pass. |
| `focused-guards-direct-pass` | Focused Slice 1–8 guards can be validated by direct require-based invocation with `helpers.extRoot=process.cwd()`. | `node --check` for v0637 `.cjs` fixtures/suites; direct require-based invocation of Slice 1–8 guards. | Focused guards prove docs/fixtures/checkpoint invariants only; `tests/run.cjs <suite>` is not focused proof. |
| `burn-down-map-complete` | The v0.6.37 v0.5 readiness burn-down map is complete enough for leader review of known P0 seams, gates, candidates, manual RC plan, validation strategy, and reliability map. | Slice 1 P0 ledger; Slice 2 baseline inventory; Slice 3 p95 gate definitions; Slice 4 candidate matrix; Slice 5 manual RC checklist; Slice 6 validation strategy; Slice 7 reliability map; Slice 8 checkpoint. | Completion of the map is not completion of release evidence, p95 proof, broad regression triage, or manual RC execution. |
| `ts-pi-facade-authority-preserved` | Package/runtime governance facts remain unchanged and TypeScript/pi remains the product and control-plane facade. | `package.json` name `pi-agentteam`; version `0.6.8`; `package.json#pi.extensions` exactly `['./index.ts']`; stable `/team` command and `agentteam_*` tool surface. | Go remains bounded helper/kernel work only where separately authorized; no default-Go/default resolver approval is implied. |

## STOP Matrix

STOP for:

| STOP item | Reason |
| --- | --- |
| `not-v05-release-ready` | v0.6.37 is a readiness burn-down/checkpoint, not a v0.5 release-ready approval. |
| `no-tag-release-git-push` | No git tag creation/movement/push, GitHub release, release asset, or git push is authorized. |
| `no-npm-version-publish-package-release` | No npm version, npm publish, package release, install source approval, package metadata expansion, package artifact, or release bundle is permitted. |
| `no-default-go-native-resolver` | No default-Go, default resolver, native package/helper delivery, `go-cutover` defaulting, `go-packaged-preview` defaulting, or normal-user native availability claim is authorized. |
| `no-signing-security-second-platform` | No signing, cosign, SLSA, security attestation, signing material, platform matrix expansion, macOS, Windows, arm64, musl, or second-platform support is authorized. |
| `no-fallback-deletion` | No TypeScript fallback deletion, `compactReadModelFingerprint` cutover, or default resolver fallback deletion is authorized. |
| `no-runtime-optimization-implementation` | No production TypeScript/Go/runtime behavior, command/tool/readiness, workflow, or UI behavior change is authorized by Slice 8. |
| `no-manual-rc-execution-claim` | The Slice 5 manual RC checklist remains `defined-not-executed`; no pass/fail smoke evidence is claimed. |
| `no-p95-pass-claim` | The Slice 3 p95 gates remain `defined-not-yet-proven`; no benchmark/manual evidence proves pass/fail. |
| `no-broad-npm-test-green-claim` | Broad `npm test` green is not claimed; known `tests/suites/tools-state.cjs:577` pane-health mismatch remains unresolved unless separately triaged. |
| `no-unresolved-pane-health-waiver` | No waiver is invented for the pane-health mismatch or any broad regression failure. |

## Remaining Blockers

Explicit blockers remain:

- `manual-rc-not-executed`: Manual RC smoke checklist is defined but not executed; release readiness needs clean/backed-up `PI_AGENTTEAM_HOME` execution and compact evidence.
- `p95-gates-not-proven`: p95 gates are defined but not proven with preserved benchmark/manual artifacts, environment metadata, threshold comparison, fallback comparison if used, and reviewer decision.
- `broad-npm-test-pane-health-mismatch`: Prior broad `npm test` failed at `tests/suites/tools-state.cjs:577` with actual `pane lost` vs expected `initial task busy via bridge delivery`; no green claim or waiver exists.
- `hot-path-improvements-not-implemented`: Slice 4 candidates are `proposed-not-started`; no runtime optimization or p95 improvement is implemented.
- `release-tag-decisions-leader-gated`: Release, tag, npm, package, signing, platform, default-Go/default-resolver, fallback deletion, and native decisions remain leader/user gated.
- `default-go-native-remains-blocked`: Default-Go/default resolver/native/package work remains out of scope and blocked by prior governance requirements.
- `task-report-planrun-final-proof-required`: Slice 7 reliability requirements are mapped but still require final validation and manual RC observations before release readiness.

## Recommended Next Decisions

Recommended leader decisions after accepting the checkpoint:

- `triage-broad-npm-test-mismatch`: Triage `tests/suites/tools-state.cjs:577` pane-health mismatch under a separate task before any broad green or release-ready claim.
- `execute-manual-rc-clean-home`: Execute the Slice 5 manual RC smoke checklist in a clean temp or backed-up `PI_AGENTTEAM_HOME`, preserving compact sanitized evidence only.
- `collect-p95-evidence`: Collect fresh p95 benchmark/manual artifacts for Slice 3 gates with environment metadata and reviewed pass/fail decisions.
- `start-first-hot-path-candidate`: If implementation work is authorized, start `panel-unchanged-state-render-suppression` first under a separate runtime-change task with characterization tests.
- `defer-release-tag-npm-native-default-go`: Keep tag/release/npm/native/default-Go/default-resolver/package/signing/second-platform/fallback-deletion blocked until separately authorized.

## Package / Runtime Invariants

Package/runtime boundaries remain unchanged:

- `package.json` name remains `pi-agentteam`.
- `package.json` version remains `0.6.8`.
- `package.json` type remains `module`.
- `package.json#pi.extensions` remains exactly `['./index.ts']`.
- TypeScript/pi remains the product and control-plane facade.
- Go remains bounded helper/kernel work only where explicitly authorized by prior checkpoints; Slice 8 does not authorize default Go or runtime mode change.
- The stable command surface remains `/team`.
- The stable tool surface remains `agentteam_create`, `agentteam_spawn`, `agentteam_send`, `agentteam_receive`, `agentteam_task`, and `agentteam_planrun`.
- No `main`, `exports`, `types`, native metadata, optional native dependencies, bundled dependencies, lifecycle hooks, native install scripts, or package-manager native delivery metadata are added.
- No root lockfile, Go module, native archive/binary, signing material, generated release manifest, checksum, provenance, attestation, release bundle, or release asset is added.

## Validation Status

Focused validation for Slice 8:

- `node --check tests/fixtures/kernel/v0637/finalReleaseReadinessCheckpoint.cjs`.
- `node --check tests/suites/go-kernel-v0637-v05-final-readiness-checkpoint-docs.cjs`.
- Direct require-based invocation of `tests/suites/go-kernel-v0637-v05-final-readiness-checkpoint-docs.cjs`.
- Re-run Slice 1, Slice 2, Slice 3, Slice 4, Slice 5, Slice 6, and Slice 7 focused guards after doc edits.
- `git diff --check`.

Recommended review context:

- Slice 1 guard: `tests/suites/go-kernel-v0637-v05-p0-readiness-ledger.cjs`.
- Slice 2 guard: `tests/suites/go-kernel-v0637-v05-performance-baseline-inventory.cjs`.
- Slice 3 guard: `tests/suites/go-kernel-v0637-v05-p95-release-gates.cjs`.
- Slice 4 guard: `tests/suites/go-kernel-v0637-v05-hot-path-burndown-candidates.cjs`.
- Slice 5 guard: `tests/suites/go-kernel-v0637-v05-manual-rc-smoke-checklist.cjs`.
- Slice 6 guard: `tests/suites/go-kernel-v0637-v05-validation-strategy.cjs`.
- Slice 7 guard: `tests/suites/go-kernel-v0637-v05-task-report-planrun-reliability.cjs`.
- Slice 8 guard: `tests/suites/go-kernel-v0637-v05-final-readiness-checkpoint-docs.cjs`.

Do not use `tests/run.cjs <suite>` as focused proof because the runner can ignore the suite-name argument and run unrelated suites.

`npm test` is optional broad regression coverage for this docs-only checkpoint. If not run, say so. If run and the known `tests/suites/tools-state.cjs:577` pane-health mismatch appears, report it exactly.

No npm pack/install, Go build, native artifact build, hosted workflow command, `gh`, network command, npm version, npm publish, git tag, git push, release command, package command, manual RC execution, p95 benchmark pass claim, or production runtime command is part of Slice 8 validation.

## Final Recommendation

Proceed with leader review of v0.6.37 as a docs/tests/fixtures-only v0.5 release-readiness burn-down checkpoint.

Do not proceed beyond this final checkpoint to v0.5 release-ready approval, tag/release work, npm package work, manual RC execution, runtime optimization implementation, runner behavior changes, native/default-Go/default-resolver work, package/signing/security work, second-platform work, fallback deletion, command/tool/readiness expansion, or broad Go authority without a separate explicit assignment and leader/user approval.
