# v0.6.59 Go targetForPaneId Facade Cutover

Result: v0.6.59 cuts over the TypeScript `targetForPaneId(paneId)` facade/default path to the existing Go-backed `resolvePaneBinding(paneId)` / `workerLifecycle.inspectPane` path.

This slice closes the direct synchronous `display-message` fallback gap for target lookup:

```text
tmux/core.ts targetForPaneId(paneId) -> resolvePaneBinding(paneId)?.target ?? null -> Go workerLifecycle.inspectPane
```

## Runtime Behavior

- `tmux/core.ts` `targetForPaneId(paneId)` now returns `resolvePaneBinding(paneId)?.target ?? null`.
- The TypeScript `runTmuxNoThrow(['display-message', '-p', '-t', paneId, '#{session_name}:#{window_id}'])` fallback for `targetForPaneId()` is removed.
- `targetForPaneId(paneId)` preserves the existing public API: `string | null`.
- Helper failure, invalid pane id, pane-not-found, missing target, and empty pane id all fail closed to `null` via `resolvePaneBinding()`.
- `resolvePaneBinding()`, `paneExists()`, `inspectPane()`, and `listAgentTeamPanes()` remain on their existing Go-backed facade seams.

## Async Binding Decision

`resolvePaneBindingAsync(paneId, signal)` is intentionally not cut over in this slice, and is later cut over by v0.6.61.

Reason at v0.6.59 time: it accepted `AbortSignal` and awaited two async tmux calls that received the signal. The kernel adapter was synchronous/per-call and had no cancellable async helper seam, so replacing the async implementation with a synchronous wrapper around `resolvePaneBinding()` would have silently dropped meaningful cancellation behavior.

Decision superseded by v0.6.61: `resolvePaneBindingAsync` now delegates to the cancellable `inspectWorkerPaneAsync(paneId, signal)` kernel adapter seam.

## Preserved Boundaries

- `captureCurrentPaneBinding()` is cut over separately by v0.6.60, not by this slice.
- `windowExists()` and `firstPaneInWindow()` remain TypeScript window helper paths.
- `listAgentTeamPanes()` remains delegated to `createAgentTeamKernelAdapter().listAgentTeamPanes()` and still filters labeled panes only.
- Mutating worker lifecycle remains TypeScript-owned: no `wakePane`, `syncPaneLabels`, `createTeammatePane`, `killPane`, `clearPaneLabel`, send-keys, label mutation, split-window, new-window, kill-pane, select-pane, or respawn-pane migration.
- State repository, task/report/PlanRun governance, team panel view-model, release/package verification, and package/native ownership remain unmigrated.
- Native artifact path and binary name remain unchanged.

## Package And Release Guards

No package/release/native action is authorized or needed in this facade-only slice:

- no Go source change in v0.6.59 itself; v0.6.60 later adds a narrow current-pane binding operation.
- no native helper rebuild in v0.6.59 itself; v0.6.60 later rebuilds the existing helper path.
- no `npm version`.
- no `npm publish`.
- no tag or GitHub release.
- no package lockfiles.
- no `go.mod` or `go.sum`.
- no lifecycle hooks or postinstall downloads.
- no native artifact path or binary rename.

`package.json` remains `0.6.8`.

## Evidence

- `docs/perf/v0.6.59-go-target-for-pane-facade-cutover.md`
- `tests/fixtures/kernel/v0659/goTargetForPaneFacadeCutover.cjs`
- `tests/suites/go-kernel-v0659-go-target-for-pane-facade-cutover.cjs`

## Validation Scope

The v0.6.59 guard suite verifies:

- `targetForPaneId(paneId)` delegates to `resolvePaneBinding(paneId)?.target ?? null`.
- the direct TypeScript `display-message` fallback is removed from `targetForPaneId()`.
- success returns the Go-backed binding target; helper failure, missing target, and empty pane id return `null`.
- `resolvePaneBindingAsync()` is cut over separately by v0.6.61 and remains signal-aware through the cancellable kernel adapter seam.
- `captureCurrentPaneBinding()` is cut over separately by v0.6.60; window helpers remain TypeScript-owned.
- Go source and native helper metadata are unchanged by this facade-only slice; v0.6.60 later updates them for the current-pane operation.
- Go adds no mutating tmux commands.
