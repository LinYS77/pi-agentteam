# v0.6.92 v0.7 Clean-temp Manual RC / Operator-seam Refresh

Result: v0.6.92 refreshes the clean-temp manual RC/operator-seam readiness evidence using the existing deterministic temp-home RC harness.

Release target remains exactly: `v0.7.0 = core refactor + performance baseline + bug burn-down release`.

Slice status: `manual-rc-operator-seam-refreshed-not-release-ready`.

manual RC/operator-seam gate status: `operator-seam-refreshed-pass`.

Overall v0.7 status remains `not-release-ready` because bug burn-down ledger, release checklist/governance review, evidence reconciliation, and continued no raw artifact policy remain outstanding.

`package.json` remains `0.6.8`.

This slice did not perform a true interactive pi/TUI/operator/model pass. Evidence below is a deterministic temp-home RC harness with stubbed pi/tmux and real registered `/team config` + `agentteam_*` tool seams.

No `--keep-home` or `--allow-non-empty-home` was passed.

## Harness Evidence

Only sanitized summary facts are recorded here. Raw harness JSON output remains under `/tmp` and is not checked in.

| Field | Value |
| --- | --- |
| Harness | `v0638-temp-home-rc-harness` |
| Command | `node scripts/verify-v0638-temp-home-rc-harness.cjs --out /tmp/pi-agentteam-v0692-temp-home-rc.json` |
| Status | `passed` |
| Output SHA256 | `db96d1153fcae7e46cfb38511a34f9ab9cdbd23bfbbd655d4c2a6f85a264c51f` |
| Temp home prefix | `/tmp/pi-agentteam-v0638-rc-harness.` |
| Cleanup | `cleanupResult=removed` |
| Isolation | `safePrefix=true`, `underRepo=false`, `liveHomeEnvRestored=true`, `autoBridgeEnvRestored=true` |
| Counts | `commandCount=4`, `toolCount=21`, `checkCount=8`, `spawnedPaneCount=1`, `killedPaneCount=0` |

## Covered Command Seams

The harness covered these registered `/team config` command seams:

- `team config show`
- `team config init`
- `team config validate`
- `team config migrate --dry-run`

## Covered Tool / Scenario Checklist

| Scenario/check | Status |
| --- | --- |
| `unsafe-name-rejection` | pass |
| `worker-receive-boundary` | pass |
| `report-done-report-only` | pass |
| `leader-receive-report-attention` | pass |
| `report-blocked-report-only` | pass |
| `team-panel-compact-model` | pass |
| `legacy-teams-dash-absent` | pass |
| `release-governance-absence` | pass |

The registered tool seam path included sanitized `agentteam_create`, `agentteam_spawn`, `agentteam_task`, `agentteam_send`, and `agentteam_receive` coverage across create, unsafe-name denial, spawn, task create/assign/show/reports/report/close, task-bound assignment, worker receive, report_done, leader receive, report_blocked, leader block/unblock, and final close flows.

## Explicit Coverage Limitations

- real `/team` TUI observation remains an operator procedure.
- real model/provider worker execution was not performed.
- No true interactive pi/TUI/operator/model evidence is claimed beyond this deterministic harness/operator-seam coverage.

## Accepted Prior Gates Referenced

- v0.6.91 clean-temp p95 refresh is accepted; evidence: `docs/perf/v0.6.91-v07-clean-temp-p95-refresh.md`.
- T046 tools-state pane-health blocker fix is accepted; evidence: `tests/suites/tools-state.cjs`.
- v0.6.90 readiness inventory remains the current not-ready framing; evidence: `docs/perf/v0.6.90-v07-readiness-burndown-refresh.md`.

## Isolation and No-leak Conclusions

- The harness used a clean temporary home under `/tmp/pi-agentteam-v0638-rc-harness.`.
- The harness reported `cleanupResult=removed`; the checked temp home no longer existed after cleanup.
- The temp home was outside the repository and started empty.
- Live `PI_AGENTTEAM_HOME` and auto-bridge environment variables were restored.
- No raw harness JSON output files are checked in.
- No raw logs, raw timing records, raw smoke output, terminal logs, screenshots, full mailbox/report bodies, worker transcripts, state archives, secrets, raw hosted records, or tarballs/temp homes are checked in.

Do not check in raw harness JSON output files, raw logs, raw timing records, raw smoke output, terminal logs, screenshots, full mailbox/report bodies, worker transcripts, state archives, secrets, raw hosted records, tarballs/temp homes.

## Remaining v0.7 Gates

This slice only addresses the clean-temp manual RC/operator-seam evidence gate. The following remain outstanding before any v0.7 decision:

- bug burn-down ledger remains outstanding;
- release checklist/governance review remains outstanding;
- evidence reconciliation remains outstanding;
- continued no raw artifact policy remains in force.

## Preserved Boundaries

- worker delivery remains bridge-only.
- Go `send-keys` / active `wakePane` remain unauthorized.
- state/task/PlanRun/mailbox/governance, team panel/UI, and release/package control-plane remain blocked.
- No runtime/source behavior changes are made.
- No Go source, tmux runtime, native helper, package, release, npm, tag, lockfile, `go.mod`, `go.sum`, lifecycle hook, postinstall/download/native dependency, hidden fallback, or native path/binary change is made.

## Evidence Files

- `tests/fixtures/kernel/v0692/v07CleanTempManualRcRefresh.cjs`
- `tests/suites/go-kernel-v0692-v07-clean-temp-manual-rc-refresh.cjs`

## Validation Plan

- `node scripts/verify-v0638-temp-home-rc-harness.cjs --out /tmp/pi-agentteam-v0692-temp-home-rc.json`
- `sha256sum /tmp/pi-agentteam-v0692-temp-home-rc.json`
- `node --check tests/fixtures/kernel/v0692/v07CleanTempManualRcRefresh.cjs`
- `node --check tests/suites/go-kernel-v0692-v07-clean-temp-manual-rc-refresh.cjs`
- `node tests/run.cjs go-kernel-v0692-v07-clean-temp-manual-rc-refresh`
- focused regression: v0.6.91 and tools-state as practical
- `npm test`
- `npm run typecheck`
- `npm run -s check:boundaries`
- `git diff --check`
- `node -p "require('./package.json').version"`
- `sha256sum -c native/tmuxSnapshotParse/0.3.0-read-model-shadow/linux-x64-glibc/SHA256SUMS`
- status/diff guard confirming only docs/tests/.gitignore/roadmap changes
