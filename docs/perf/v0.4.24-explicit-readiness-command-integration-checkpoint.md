# v0.4.24 Explicit Readiness Command Integration Checkpoint

> Scope: Slice 5 final GitHub-only explicit readiness command integration checkpoint review. This summarizes Slice 1-4 evidence for the transitional `/team readiness` reviewer command. It does not expand `/team readiness`, remove `/team readiness`, add ambient `/team` UI, add model-callable tools, change runtime resolver/default behavior, change package/native/default behavior, change `package.json`, add optionalDependencies, add lifecycle hooks/downloads/package scripts, add lockfiles, add Go modules, add native artifacts, add tarballs, add generated manifests/artifacts, run npm versioning, run npm publication, make Go default, change current `go-cutover`, change `go-packaged-preview` availability semantics, delete the TypeScript parser fallback, broaden Go authority, commit, tag, or push.

## Decision

Final v0.4.24 checkpoint recommendation:

- GO only for GitHub-only v0.4.24 explicit readiness command integration checkpoint after leader/user approval.
- STOP for expanding `/team readiness` into a permanent user-facing feature.
- STOP for additional readiness subcommands/options without explicit user approval.
- STOP for ambient `/team` UI/panel diagnostics.
- STOP for model-callable tools.
- STOP for runtime control-plane behavior.
- STOP for npm/default/native cutover.
- STOP for real package inclusion or native artifact approval.
- STOP for `package.json` metadata or version changes.
- STOP for `optionalDependencies`, lifecycle hooks/downloads, package scripts, lockfiles, Go modules, native artifacts, tarballs, generated manifests, or generated artifacts.
- STOP for treating diagnostics/readiness as normal-user native availability proof.
- STOP for default Go enablement.
- STOP for current `go-cutover` behavior changes.
- STOP for `go-packaged-preview` availability semantics changes.
- STOP for TypeScript fallback deletion.
- STOP for broader Go authority beyond parser-only stdin/stdout `tmuxSnapshotParse`.

This checkpoint says the explicit `/team readiness` command integration is ready to record in GitHub history after leader/user approval as transitional reviewer tooling only. It does not approve productizing the command, normal-user native availability, ambient UI, native/default/package behavior, npm publication, or fallback removal.

## Context and Links

Prior checkpoint:

- v0.4.23 compact native failure diagnostics checkpoint: `docs/perf/v0.4.23-compact-native-failure-diagnostics-checkpoint.md`

v0.4.24 artifacts:

- Contract/seam/implementation/sunset doc: `docs/perf/v0.4.24-explicit-readiness-command-integration.md`
- Readiness command handler: `commands/readiness.ts`
- Team command router: `commands/team.ts`
- Slice 1 contract guard: `tests/suites/go-kernel-v0424-readiness-command-contract-docs.cjs`
- Slice 2 seam guard: `tests/suites/go-kernel-v0424-readiness-command-seam-docs.cjs`
- Slice 3 integration guard: `tests/suites/go-kernel-v0424-readiness-command-integration.cjs`
- Slice 4 sunset guard: `tests/suites/go-kernel-v0424-readiness-command-sunset-docs.cjs`
- Slice 5 checkpoint guard: `tests/suites/go-kernel-v0424-readiness-command-checkpoint-docs.cjs`

## Slice 1-4 Summary

| Slice | Result | Checkpoint implication |
| --- | --- | --- |
| Slice 1 contract | Defines an explicit opt-in reviewer readiness surface only, with safe output fields, forbidden leaks, read-only behavior, runtime invariants, and package/native STOP gates. | `/team readiness` must remain opt-in reviewer tooling, not ambient UI or package/default approval. |
| Slice 2 seam selection | Chooses the smallest `/team readiness` seam before panel open, following `/team config` subcommand dispatch and avoiding tools/panel/package seams. | Future command work stays in the explicit command path and avoids model-callable tools or `/team` rendering. |
| Slice 3 implementation | Adds minimal read-only deterministic `/team readiness` command using v0.4.23 safe helpers from `core/kernelDiagnostics.ts`; normal `/team` still opens the panel. | The command exists only as a contained reviewer notification path and does not mutate state or runtime behavior. |
| Slice 4 containment | Classifies `/team readiness` as transitional reviewer tooling, not a long-term product feature; defines containment rules and sunset/deletion/merge paths. | Future work should not expand the command; it should delete, merge, or hide it after separately approved diagnostics/default work matures. |

## Runtime, Command, and Package State

Runtime behavior remains unchanged:

- default/unset remains disabled/TypeScript.
- disabled, typescript, go, and auto behavior remains unchanged.
- current `go-cutover` remains unchanged, explicit/local-only, and helper-path based.
- `go-packaged-preview` remains explicit-only, non-default, and not normal-user availability proof.
- `go-packaged-preview` availability semantics remain unchanged.
- packaged helper discovery does not run in default, disabled, typescript, go, auto, or current `go-cutover`.
- `tmuxSnapshotParse` is the only cutover-owned module.
- `compactReadModelFingerprint` remains TypeScript fallback / non-cutover.
- runtime `/team` panel remains quiet; no ambient diagnostics rendering, no releaseDecision rendering, no platformHint/freshnessHint rendering, no remediation rendering, and no helper path/stdout/stderr/raw cutoverReason/package internals rendering.
- TypeScript/pi remains the control plane for tmux capture, worker lifecycle, state/repository, task/report governance, PlanRun, full-text boundaries, package metadata, release operations, UI rendering, and command routing.

Command behavior is contained:

- `/team readiness` is a single explicit subcommand only.
- no additional readiness subcommands/options are approved.
- no model-callable readiness tool is registered.
- no ambient `/team` panel diagnostics are rendered.
- no state writes, mailbox/report full-text reads, task/report governance mutation, tmux execution/capture, pane reconcile/kill, worker lifecycle mutation, runtime resolver/default behavior, or broad Go authority are added.
- `/team readiness` is transitional reviewer/readiness tooling only, not a long-term product feature.

Package/native behavior remains unchanged:

- `package.json` version remains `0.6.8`.
- `package.json#files` excludes `kernel/` and native/helper/generated artifact paths.
- no `optionalDependencies`.
- no native companion package metadata in the main package.
- no lifecycle hooks: `preinstall`, `install`, `postinstall`, `prepare`, `prepublish`, `prepublishOnly`, `publish`, or `postpublish`.
- no helper build/install/download/package/version/publish scripts.
- no `package-lock.json` or `npm-shrinkwrap.json`.
- no root or helper `go.mod`.
- no root or helper `go.sum`.
- no checked-in `.exe`, `.dll`, `.so`, `.dylib`, helper binary, native artifact, generated manifest artifact, generated package artifact, or tarball.
- no native artifact package files.
- no `kernel/` package inclusion.
- no `npm version`, `npm publish`, commit, tag, or push is part of this checkpoint.

## Readiness Sunset and Go Mainline

`/team readiness` remains transitional reviewer/readiness tooling only. It is not a long-term product feature and not part of the long-term Go performance replacement goal.

Go mainline remains core hot-path replacement: replace proven deterministic hot-path modules with Go-owned implementations after cutover gates, not feature expansion. Future work returns to Go core replacement, generated artifacts, clean install proof, module cutover gate, and separately approved fallback deletion/default cutover plan.

Sunset paths remain:

- delete `/team readiness` after formal native diagnostics/default path matures.
- or merge `/team readiness` into a separately approved diagnostics UX.
- or keep `/team readiness` only as a developer/reviewer-only hidden/internal command if explicitly approved.

Deletion criteria remain:

- generated artifacts and clean-install proof exist.
- unsupported-platform remediation and rollback are accepted.
- normal-user diagnostics UX exists if needed.
- TypeScript fallback deletion/default cutover decision is separately approved.

## Remaining Blockers Before Actual Default/Native/Fallback Deletion

STOP future default/native cutover, native package behavior, or fallback deletion until later explicit owner slices resolve all of these blockers:

- generated artifacts/checksums/provenance/license/executable validation.
- clean install smokes across supported platforms.
- unsupported-platform remediation.
- rollback story for bad metadata, bad helper artifact, checksum/provenance mismatch, unsupported platform, broken diagnostics, package unpublish/deprecation, and default resolver disablement.
- normal-user diagnostics UX if needed.
- user approval for default/native/fallback deletion.
- package release ownership for naming, versioning, provenance, attestation, checksum, license, supported-platform matrix, publication, deprecation, and rollback.
- parser failure policy proven in any future normal-user default path.
- normal-user native availability proven independently; diagnostics/readiness is not normal-user native availability proof.
- Go helper authority remains parser-only/stdin-stdout `tmuxSnapshotParse` and does not gain tmux execution/capture, state/repository writes, network/listeners, worker lifecycle, PlanRun/governance, full-text access, package/release authority, ambient UI, model-callable tool surface, or control-plane behavior.

## Validation Matrix

Required final validation for this checkpoint:

- focused Slice 1 guard: `node tests/run.cjs go-kernel-v0424-readiness-command-contract-docs`
- focused Slice 2 guard: `node tests/run.cjs go-kernel-v0424-readiness-command-seam-docs`
- focused Slice 3 guard: `node tests/run.cjs go-kernel-v0424-readiness-command-integration`
- focused Slice 4 guard: `node tests/run.cjs go-kernel-v0424-readiness-command-sunset-docs`
- focused Slice 5 checkpoint guard: `node tests/run.cjs go-kernel-v0424-readiness-command-checkpoint-docs`
- syntax check for the checkpoint guard: `node --check tests/suites/go-kernel-v0424-readiness-command-checkpoint-docs.cjs`
- `node tests/run.cjs`
- `npm run typecheck`
- `npm run -s check:boundaries`
- `git diff --check`
- default bench: `npm run --silent bench:team-panel-tmux`
- preview bench: `PI_AGENTTEAM_KERNEL=go-packaged-preview npm run --silent bench:team-panel-tmux`
- package/native sanity scan

Expected validation posture:

- focused v0.4.24 suites pass without adding command expansion, tool registration, panel rendering, package/native artifacts, or runtime behavior changes.
- full suite passes with normal `/team` behavior unchanged and `/team readiness` explicit only.
- default bench metadata stays TypeScript/default with no Go enablement.
- preview bench metadata reports `requestedMode:"go-packaged-preview"`, remains TypeScript when no helper is packaged/injected, keeps `fallbacks:0`, and does not run read-model shadow.
- package/native sanity remains unchanged from v0.4.23/v0.4.24 guardrails.

## Final Recommendation

Proceed only with a GitHub-only v0.4.24 explicit readiness command integration checkpoint after leader/user approval.

Do not proceed with expanding `/team readiness`, adding readiness subcommands/options, ambient `/team` UI/panel diagnostics, model-callable tools, runtime control-plane behavior, npm/default/native cutover, real package inclusion, native artifact approval, `package.json` metadata or version changes, `optionalDependencies`, lifecycle hooks, helper download/build/install scripts, package scripts, lockfiles, Go modules, native binaries, tarballs, generated manifests, generated package artifacts, treating diagnostics/readiness as normal-user native availability proof, default Go enablement, current `go-cutover` behavior changes, `go-packaged-preview` availability semantics changes, TypeScript fallback deletion, broader Go authority, commit, tag, or push as part of this checkpoint.
