# v0.6.42 Spawn Bookkeeping p95 Evidence

> Scope: focused v0.6.42 evidence for app-owned worker spawn bookkeeping p95. This uses a clean temporary `PI_AGENTTEAM_HOME` harness and does not execute manual RC, change package metadata, enable default-Go/native, tag, publish, or claim v0.7 release readiness.

## Result

Result: v0.6.42 adds focused worker spawn bookkeeping p95 coverage through clean-temp file-backed state and app-owned profiling segments.

Final result remains `ready:false`. The `spawn-bookkeeping-p95` gate passes in this focused run, but this slice does not re-execute manual RC or prove every release p95 gate.

This checkpoint is GO for reviewer inspection of the spawn bookkeeping harness, clean-temp isolation contract, visible-pane semantics stubs, launch provenance inheritance checks, no-leak guard, and preserved local `/tmp` sanitized timing artifact.

This checkpoint is STOP for release/tag/git push/npm version/npm publish/native/default-Go/package/signing/second-platform/fallback-deletion work, TypeScript/pi facade authority violations, raw terminal logs, screenshots, raw state archive or full-text evidence check-in, manual RC pass claims, all-p95-gates claims, and claiming v0.7 release readiness.

STOP for release/tag/git push/npm version/npm publish/native/default-Go/package/signing/second-platform/fallback-deletion work.

## Harness

Focused command:

```bash
PI_AGENTTEAM_PROFILE=1 node scripts/verify-v0642-spawn-bookkeeping-p95.cjs --out /tmp/pi-agentteam-v0642-spawn-bookkeeping-p95-latest.json
```

The verifier calls `scripts/lib/v0642-spawn-bookkeeping-p95-harness.cjs`, which:

- Creates a clean temporary `PI_AGENTTEAM_HOME` under `/tmp/pi-agentteam-v0642-spawn-bookkeeping-p95.*` and rejects unsafe/non-empty homes by default.
- Transpiles the TypeScript extension to an isolated temporary dist with test stubs for pi/TUI dependencies.
- Seeds a compact file-backed team with a leader pane binding and repeatedly calls the real `spawnWorkerMember` implementation.
- Stubs external tmux pane creation, pane process readiness, and bridge lease publication so the measured gate excludes provider/LLM/operator time, terminal rendering, external tmux latency, and bridge wait latency.
- Records app-owned spawn bookkeeping segments via `PI_AGENTTEAM_PROFILE=1` and the profiling summary.
- Verifies visible-pane semantics, bridge-ready state, worker session file provenance, and inherited launch cwd without raw transcripts or terminal logs.
- Keeps sanitized timing JSON under `/tmp` and checks that no raw terminal logs, screenshots, state archives, full mailbox/report bodies, worker transcripts, secrets, or raw hosted records are emitted.
- Restores the original `PI_AGENTTEAM_HOME` and `PI_AGENTTEAM_PROFILE` after the run and removes the temp home unless `--keep-home` is explicitly used.

## Coverage

Fixture profile:

| Profile | Shape | Warmup | Measured | Gate threshold |
| --- | --- | ---: | ---: | ---: |
| `file-backed-worker-spawn-bookkeeping-stubbed-external-boundaries` | Clean temp file-backed team; real `spawnWorkerMember`; stubbed tmux/process/bridge external boundaries; p95 uses summed app-owned bookkeeping segments per spawn. | `2` | `12` | `<= 100ms` |

Covered bookkeeping segments:

- `validate-config-classify`
- `build-prompt-command`
- `reserve-worker-state`
- `write-session-context`
- `commit-pane-created`
- `resolve-pane-binding-commit`
- `final-worker-status`

`initial-delivery-bookkeeping` is instrumented but not required by this idle-spawn fixture because initial delivery is explicitly out of the spawn bookkeeping fixture path.

## Gate Status

| Gate | Status | Metric | Threshold | Evidence |
| --- | --- | --- | --- | --- |
| `spawn-bookkeeping-p95` | `pass` | `workerSpawn.bookkeepingMs.p95` observed `21.995ms` over `12` measured spawns | `<= 100ms` | `/tmp/pi-agentteam-v0642-spawn-bookkeeping-p95-latest.json`, SHA-256 `59d3f80112dd378426473eb74057087aa54d8fdbe428f822db8ca25e5d309a53` |
| `visible-pane-semantics` | `pass` | Pane created, pane id returned, and bridge-ready state observed for measured spawns | Preserve visible-pane/bridge semantics | Sanitized harness summary |
| `launch-provenance-inheritance` | `pass` | Worker pane cwd inherits leader cwd and launch command includes worker session file | Preserve launch provenance | Sanitized harness summary |
| `manual-rc-smoke` | `not-covered-by-this-slice` | Real operator RC observation is outside this p95 harness. | Manual checklist evidence | Not executed here. |

## Runtime Instrumentation

`core/profiling.ts`, `runtime/profiling.ts`, and `tools/workerSpawnService.ts` now expose and record spawn bookkeeping profile events.

This is intentionally narrow:

- The profiling events record compact segment names, durations, ok flags, worker name, and role only.
- Spawn lifecycle behavior, visible pane creation, bridge readiness handling, worker FSM transitions, launch command/session context semantics, and persisted state format are preserved.
- Task/report/message governance, report-only semantics, and mailbox/report full-text boundaries are not touched.
- No package, Go, native, default resolver, fallback deletion, tag, release, or publish behavior changes are included.

## No-Leak Check

No raw terminal logs, screenshots, raw state archives, raw full mailbox/report bodies, worker transcripts, secrets, raw hosted records, or raw timing JSON are checked in.

The guard searches for and preserves the following no-leak policy markers in controlled fixture/script/doc files only:

- `V0642_SPAWN_BOOKKEEPING_FULL_TEXT_SENTINEL_DO_NOT_LEAK`
- `MailboxMessage.text`
- `TaskReport.text`
- `worker transcript`
- `terminal raw log`
- `screenshot`
- `state archive`
- `raw state archive`
- `BEGIN PRIVATE KEY`
- `raw hosted record`

Raw local timing output is a sanitized `/tmp` artifact. It records compact spawn segment durations, profile counters, threshold summaries, and no-leak/governance booleans; it does not record worker transcripts, screenshots, raw terminal logs, or raw state bodies.

## TypeScript/pi Facade Authority

TypeScript/pi facade authority is preserved:

- `package.json` remains version `0.6.8`.
- `package.json#pi.extensions` remains `['./index.ts']`.
- The runtime instrumentation is limited to TypeScript spawn bookkeeping profiling.
- No Go runtime, default-Go/native/package/fallback deletion work is approved or implemented.

## Validation

Focused validation for this evidence checkpoint:

- `node --check scripts/lib/v0642-spawn-bookkeeping-p95-harness.cjs`.
- `node --check scripts/verify-v0642-spawn-bookkeeping-p95.cjs`.
- `node --check tests/fixtures/kernel/v0642/spawnBookkeepingP95.cjs`.
- `node --check tests/suites/go-kernel-v0642-spawn-bookkeeping-p95.cjs`.
- Direct require-based invocation of `tests/suites/go-kernel-v0642-spawn-bookkeeping-p95.cjs` with `helpers.extRoot=process.cwd()`.
- Focused harness: `PI_AGENTTEAM_PROFILE=1 node scripts/verify-v0642-spawn-bookkeeping-p95.cjs --out /tmp/pi-agentteam-v0642-spawn-bookkeeping-p95-latest.json`.
- Broad regression: `npm test`.
- TypeScript: `npm run typecheck`.
- Boundary guard: `npm run -s check:boundaries`.
- Whitespace: `git diff --check`.

## Final Recommendation

Do not claim v0.7 release readiness from this artifact. Use it only as the v0.6.42 focused spawn bookkeeping p95 coverage and app-owned profiling evidence slice. Remaining release-readiness gaps still include manual RC/release governance and any separately authorized release/tag/package/default-Go/native decisions.
