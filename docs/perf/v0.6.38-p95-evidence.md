# v0.6.38 p95 Evidence

> Scope: v0.6.38 evidence-only collection for existing deterministic Slice 3 p95 gates. Raw benchmark JSON is preserved under `/tmp/pi-agentteam-v0638-p95/` and is not checked in.

## Result

Result: v0.6.38 collected p95 evidence from the existing deterministic state/read-model and team-panel/tmux benches.

This checkpoint is GO for reviewer inspection of preserved raw JSON paths, SHA-256 hashes, no-leak checks, and threshold comparisons for gates that existing benches can cover.

This checkpoint is STOP for release/tag/git push/npm version/npm publish/native/default-Go/package/signing/second-platform/fallback-deletion work, production runtime behavior changes, raw benchmark JSON check-in, and claiming v0.5 release readiness.

Final result remains `ready:false`.

Reasons:

- Existing benches cover five Slice 3 gates as passing numeric/policy evidence.
- Existing benches cover `unchanged-state-no-repeated-request-render` as failing current semantic evidence because requestRender activity is still present during cache-hit/no-diff measured refreshes.
- Five Slice 3 gates remain `not-covered-by-existing-bench` and need future focused harnesses or manual evidence.
- Manual operator RC is still separate from this p95 evidence artifact.

## Environment Metadata

| Field | Value |
| --- | --- |
| Date | `2026-06-23T02:17:53.663Z` |
| Git | `dc13417` |
| Node | `v24.9.0` |
| npm | `11.7.0` |
| OS | `Linux root123-AS-4124GS-TNR 5.19.0-32-generic #33~22.04.1-Ubuntu SMP PREEMPT_DYNAMIC Mon Jan 30 17:03:34 UTC 2 x86_64 x86_64 x86_64 GNU/Linux` |
| CPU | `AMD EPYC 7402 24-Core Processor`, `96` logical CPUs, `x64`, `linux` |
| `PI_AGENTTEAM_PROFILE` | `1` |
| `AGENTTEAM_BENCH_FIXTURE` | `baseline` |
| `PI_AGENTTEAM_HOME` | Bench-created temporary home under `os.tmpdir`; removed by bench scripts. |
| Fixture iterations | warmup `1`, measured `5` for both benches. |

## Commands And Raw Artifacts

Commands run from repo root:

```bash
node --version
npm --version
git rev-parse --short HEAD
uname -a
PI_AGENTTEAM_PROFILE=1 AGENTTEAM_BENCH_FIXTURE=baseline node tests/bench/team-read-model-baseline.cjs > /tmp/pi-agentteam-v0638-p95/team-read-model-baseline.json
PI_AGENTTEAM_PROFILE=1 AGENTTEAM_BENCH_FIXTURE=baseline node tests/bench/team-panel-tmux-refresh-v0415.cjs > /tmp/pi-agentteam-v0638-p95/team-panel-tmux-refresh-baseline.json
sha256sum /tmp/pi-agentteam-v0638-p95/*.json > /tmp/pi-agentteam-v0638-p95/SHA256SUMS
```

Raw artifacts:

| Artifact | Parse | SHA-256 |
| --- | --- | --- |
| `/tmp/pi-agentteam-v0638-p95/env-metadata.json` | `ok` | `ae8a7fe090f8467a14e18767cb297b8a4a8b571078fe12a27713595fd3f9ac5f` |
| `/tmp/pi-agentteam-v0638-p95/team-read-model-baseline.json` | `ok` | `1eb60acdbe7af022de9ea810ce19e402b801dd82828bf0a096c974e1775ab69a` |
| `/tmp/pi-agentteam-v0638-p95/team-panel-tmux-refresh-baseline.json` | `ok` | `882294237465b9f93d47bce85dc3d61832251d2a041625eda865d40c33b7eb12` |

Raw JSON files are local evidence only. Do not check them in unless a later task explicitly authorizes a sanitized artifact format.

## No-Leak Check

Searched raw JSON artifacts for known full-body markers and boundary names. All searches were absent:

- `BENCH_STATE_READ_MODEL_FULL_BODY_SENTINEL_SHOULD_NOT_LEAK`
- `BENCH_PANEL_TMUX_V0415_FULL_BODY_SENTINEL_SHOULD_NOT_LEAK`
- `MailboxMessage.text`
- `TaskReport.text`
- `FULL_BODY_SENTINEL`
- `PROFILE_FULL_BODY_SENTINEL`
- `V0638_RC_FULL_TEXT_SENTINEL_DO_NOT_LEAK`

No raw full mailbox/report bodies, worker transcripts, screenshots, state archives, secrets, or terminal logs are checked in by this artifact.

## Covered Gate Table

| Gate | Status | Metric / policy | Threshold | Observed value | Source |
| --- | --- | --- | --- | --- | --- |
| `attached-team-warm-refresh-data-load-p95` | `pass` | `attached.panel.dataLoadMs.p95` | `<= 100ms` | `7ms` | `/tmp/pi-agentteam-v0638-p95/team-panel-tmux-refresh-baseline.json` |
| `attached-team-warm-refresh-render-p95` | `pass` | `attached.panel.renderMs.p95` | `<= 16ms` | `2ms` | `/tmp/pi-agentteam-v0638-p95/team-panel-tmux-refresh-baseline.json` |
| `attached-team-warm-refresh-tmux-command-count` | `pass` | `attached.tmux.commandCount` | `<= 1` command per measured attached refresh batch | `0` | `/tmp/pi-agentteam-v0638-p95/team-panel-tmux-refresh-baseline.json` |
| `global-team-warm-refresh-data-load-p95` | `pass` | `global.panel.dataLoadMs.p95` | `<= 200ms` | `17ms` | `/tmp/pi-agentteam-v0638-p95/team-panel-tmux-refresh-baseline.json` |
| `global-team-warm-refresh-snapshot-policy` | `pass` | `global.tmux.commandNames` and normalized `global.tmux.commandCount` | one `list-panes` snapshot per measured global refresh; no per-team/per-member tmux fan-out | `5` commands over `5` measured iterations; command names `list-panes` | `/tmp/pi-agentteam-v0638-p95/team-panel-tmux-refresh-baseline.json` |
| `unchanged-state-no-repeated-request-render` | `fail` | `requestRenderCount`, `cacheHitCount`, `diffChangedCount` under stable measured refreshes | no repeated requestRender growth for unchanged state after the initial mounted panel | attached `requestRenderCount=12`, `cacheHitCount=5`, `diffChangedCount=0`; global `requestRenderCount=6`, `cacheHitCount=5`, `diffChangedCount=0` | `/tmp/pi-agentteam-v0638-p95/team-panel-tmux-refresh-baseline.json` |

The failing unchanged-state row is a threshold interpretation of current evidence, not a runtime change. A later implementation candidate such as `panel-unchanged-state-render-suppression` or a narrower no-input unchanged-state harness is needed before this gate can be reconsidered.

## Supporting State / Read-Model Evidence

State/read-model baseline fields are supporting evidence for the performance baseline, not standalone proof for future action/lock/debounce/spawn gates:

| Field | Observed value | Source |
| --- | --- | --- |
| `panel.dataLoadMs.p95` | `4ms` | `/tmp/pi-agentteam-v0638-p95/team-read-model-baseline.json` |
| `panel.readModelMs.p95` | `2ms` | `/tmp/pi-agentteam-v0638-p95/team-read-model-baseline.json` |
| `fsStore.byKind.read.p95` | `0.18957996368408203ms` | `/tmp/pi-agentteam-v0638-p95/team-read-model-baseline.json` |
| `fsStore.byKind.parse.p95` | `0.671779990196228ms` | `/tmp/pi-agentteam-v0638-p95/team-read-model-baseline.json` |
| `fsStore.byKind.lock.count` | `0` | `/tmp/pi-agentteam-v0638-p95/team-read-model-baseline.json` |
| `tmux.commandCount` | `0` | `/tmp/pi-agentteam-v0638-p95/team-read-model-baseline.json` |

Because lock count is `0`, this run does not prove the `fsstore-lock-wait-p95` gate.

## Not-Covered Gates

| Gate | Status | Reason |
| --- | --- | --- |
| `task-message-report-action-normal-p95` | `not-covered-by-existing-bench` | Existing benches do not time create/assign/send/receive/report_done/report_blocked app-owned action handling under a normal mailbox fixture. |
| `task-message-report-action-large-mailbox-p95` | `not-covered-by-existing-bench` | Existing benches do not time task/message/report actions under large mailbox/report indexes. |
| `fsstore-lock-wait-p95` | `not-covered-by-existing-bench` | Existing state/read-model bench has `fsStore.byKind.lock.count=0`; Slice 3 requires non-zero representative lock timing before pass/fail. |
| `data-change-render-debounce-rate` | `not-covered-by-existing-bench` | Existing benches do not provide timestamped semantic data-change render/requestRender rate evidence. |
| `spawn-bookkeeping-p95` | `not-covered-by-existing-bench` | Existing benches do not time AgentTeam-owned spawn bookkeeping separately from external pi/tmux/LLM startup. |

## Validation

Focused validation for this evidence checkpoint:

- `node --check tests/fixtures/kernel/v0638/p95Evidence.cjs`.
- `node --check tests/suites/go-kernel-v0638-p95-evidence.cjs`.
- Direct require-based invocation of `tests/suites/go-kernel-v0638-p95-evidence.cjs` with `helpers.extRoot=process.cwd()`.
- `git diff --check`.

`npm test` is optional broad regression coverage and is not required for this evidence-only checkpoint.

## Final Recommendation

Do not claim v0.5 release readiness from this artifact. The immediate performance follow-up should be the first runtime candidate, `panel-unchanged-state-render-suppression`, or a narrower unchanged-state panel harness if the leader wants more characterization before implementation. Missing harnesses are still required for task/message/report action p95, fsStore lock wait p95, data-change debounce rate, and spawn bookkeeping p95. Manual operator RC remains a separate blocker.
