# v0.4.26 Go Helper Artifact Generation Pipeline Prototype Checkpoint

> Scope: Slice 1 docs/tests-only pipeline owner contract / release boundary. This starts the v0.4.26 Go Helper Artifact Generation Pipeline Prototype Checkpoint. It defines artifact generation pipeline prototype scope and the GitHub-only release/package boundary. It is evidence only, not npm/package/default/native/fallback approval.

## Decision

v0.4.26 follows v0.4.25 because v0.4.25 proved prerequisites with temp evidence: owner contract, temp artifact/manifest validation, temp clean-install smoke, unsupported-platform rollback/version-skew policy, resolver/default gate, and final checkpoint. The next missing evidence is real artifact generation pipeline design/prototype, not package/default/native release.

Slice 1 recommendation:

- GO only for docs/tests-only pipeline owner contract / release boundary after leader review.
- GO only for GitHub-only evidence planning.
- STOP for helper build command implementation.
- STOP for CI workflow implementation in Slice 1; docs-only references are allowed.
- STOP for generated real artifacts/manifests.
- STOP for npm/package/default/native/fallback approval.
- STOP for later work without separate approval.

## Final Checkpoint

- final v0.4.26 checkpoint: `docs/perf/v0.4.26-go-helper-artifact-pipeline-checkpoint.md`
- final checkpoint guard: `tests/suites/go-kernel-v0426-artifact-pipeline-checkpoint-docs.cjs`

## Prior v0.4.25 Links

- final v0.4.25 checkpoint: `docs/perf/v0.4.25-native-helper-availability-proof-checkpoint.md`
- v0.4.25 owner contract/evidence doc: `docs/perf/v0.4.25-native-helper-availability-proof.md`
- Slice 1 contract guard: `tests/suites/go-kernel-v0425-native-availability-contract-docs.cjs`
- Slice 2 artifact/manifest prototype guard: `tests/suites/go-kernel-v0425-artifact-manifest-prototype.cjs`
- Slice 3 clean-install smoke guard: `tests/suites/go-kernel-v0425-clean-install-smoke.cjs`
- Slice 4 unsupported rollback policy guard: `tests/suites/go-kernel-v0425-unsupported-rollback-policy.cjs`
- Slice 5 resolver/default cutover gate guard: `tests/suites/go-kernel-v0425-resolver-default-cutover-gate.cjs`
- Slice 6 final checkpoint guard: `tests/suites/go-kernel-v0425-native-availability-checkpoint-docs.cjs`

## T013 Runtime Boundary

T013 remains binding:

- TS/pi control plane mandatory.
- Go helper behind TS adapter/ports via subprocess/RPC/stdin-stdout.
- no native Go pi extension/provider ABI assumption.
- Go authority parser-only `tmuxSnapshotParse`.
- `compactReadModelFingerprint` remains TypeScript fallback/non-cutover.
- Go does not own tmux execution/capture, state, worker lifecycle, task/report governance, PlanRun, full-text boundaries, package/release authority, UI rendering, or command control plane.

## In-Scope v0.4.26 Pipeline Areas

Future v0.4.26 slices may define docs/tests/prototype evidence for:

- build matrix definition.
- helper build command policy.
- artifact output path policy.
- local/CI artifact output prototype.
- manifest/checksum/provenance/license/executable generation prototype.
- attestation/signing placeholders.
- direct artifact smoke and clean-install handoff.
- storage/release/rollback policy.
- final checkpoint guardrail.

Slice 1 only defines owner scope and release/package boundary. It does not build helpers, run artifact generation, publish artifacts, or alter runtime/package behavior.

## Slice 2 — Build Matrix and Build Command Policy

Slice 2 is docs/tests-only build matrix and command policy. It specifies how real Go helper binaries would be built per platform in a future artifact pipeline, without building helpers, adding CI workflow, generating artifacts, or changing package/runtime/release behavior.

Candidate/prototype build matrix:

| Target | OS | Arch | Libc | Executable filename | Expected executable permission behavior | Runner assumption | Validation expectation |
| --- | --- | --- | --- | --- | --- | --- | --- |
| linux-x64-glibc | linux | x64 | glibc | `agentteam-tmuxSnapshotParse` | POSIX executable bit required (`0755` prototype expectation) | native runner preferred; cross-compile allowed only with explicit proof | manifest/checksum/provenance/license/executable validation plus direct smoke and clean-install handoff |
| linux-arm64-glibc | linux | arm64 | glibc | `agentteam-tmuxSnapshotParse` | POSIX executable bit required (`0755` prototype expectation) | native runner preferred; cross-compile allowed only with explicit proof | manifest/checksum/provenance/license/executable validation plus direct smoke and clean-install handoff |
| darwin-arm64 | darwin | arm64 | n/a | `agentteam-tmuxSnapshotParse` | POSIX executable bit required (`0755` prototype expectation) | native runner preferred; cross-compile allowed only with explicit proof | manifest/checksum/provenance/license/executable validation plus direct smoke and clean-install handoff |
| darwin-x64 | darwin | x64 | n/a | `agentteam-tmuxSnapshotParse` | POSIX executable bit required (`0755` prototype expectation) | native runner preferred; cross-compile allowed only with explicit proof | manifest/checksum/provenance/license/executable validation plus direct smoke and clean-install handoff |
| win32-x64 | win32 | x64 | n/a | `agentteam-tmuxSnapshotParse.exe` (`.cmd` placeholder allowed only for test fixtures) | Windows executable extension required; POSIX mode is not the portability signal | native runner preferred; cross-compile allowed only with explicit proof | manifest/checksum/provenance/license/executable validation plus direct smoke and clean-install handoff |

Future unsupported rows until proven:

| Target | Status | Policy |
| --- | --- | --- |
| linux-x64-musl | future unsupported until proven | fail-closed; no normal-user support claim |
| linux-arm64-musl | future unsupported until proven | fail-closed; no normal-user support claim |
| win32-arm64 | future unsupported until proven | fail-closed; no normal-user support claim |
| other os/arch/libc targets | future unsupported until proven | fail-closed; no normal-user support claim |

Helper build command policy:

- `go build` may be used only in explicit artifact-generation CI/local prototype after approval.
- `go build` is never allowed in npm lifecycle.
- `go build` is never allowed in package install.
- `go build` is never allowed in runtime resolver.
- `go build` is never allowed in default user path.
- no hidden network fetch.
- no lifecycle download.
- no install-time build.
- no package scripts in this slice.
- no helper build/install/package scripts in this slice.
- no CI workflow implementation in this slice.
- no helper build commands are implemented in this slice.

Go module policy:

- adding go.mod/go.sum remains STOP unless separately approved.
- if future build requires modules, it needs a separate owner decision.
- no go.mod/go.sum are added in Slice 2.

Unsupported platform policy:

- unsupported rows are fail-closed and do not imply normal-user support.
- support claims require CI/toolchain proof and approval.
- unsupported rows do not permit default Go, native/default cutover, fallback deletion, or package/native approval.

Slice 2 preserves the Slice 1 release/package boundary: GitHub-only checkpoint, no npm package metadata by default, GitHub Actions artifacts only as future prototype storage after approval, GitHub release assets as future explicit release-policy gate, npm companion packages as future package-owner gate, and no postinstall/download/install-time build policy remains binding.

## Slice 3 — Local/CI Artifact Output Policy and Prototype

Slice 3 is docs/tests and temp/ignored-output policy/prototype only. It defines where future generated helper artifacts may be written during local/CI prototype runs and proves source/package inclusion does not occur. It does not build real helpers, add CI workflow, generate real native artifacts/manifests, or change package/runtime/release behavior.

Local output policy:

- OS temp root is preferred for tests.
- Optional ignored local directory may be named `.agentteam-artifacts/` for future local prototypes.
- `.agentteam-artifacts/` must be ignored and excluded from package files before any local prototype uses it.
- Generated outputs must never be committed.
- Test-local fixtures may create output trees only under OS temp dirs and must clean them up.
- Output paths must be outside source package inclusion or under ignored/temp roots.

CI output policy:

- CI workspace outputs and GitHub Actions artifact upload may be future prototype storage after explicit approval.
- No GitHub release assets in Slice 3.
- No npm package inclusion.
- No install/runtime download path.
- No CI workflow is added in Slice 3.
- Release assets remain a future explicit release-policy gate.

Conceptual artifact file list per build-matrix row:

| File kind | Safe package-relative filename concept | Policy |
| --- | --- | --- |
| helper executable | `agentteam-tmuxSnapshotParse` or `agentteam-tmuxSnapshotParse.exe` | generated only in temp/ignored future output roots; never committed |
| manifest JSON | `manifest.json` | generated only in temp/ignored future output roots; never committed |
| checksum file | `SHA256SUMS` | generated only in temp/ignored future output roots; never committed |
| provenance metadata | `provenance.json` | generated only in temp/ignored future output roots; never committed |
| license metadata/copy | `LICENSE` or `license.json` | generated/copied only in temp/ignored future output roots; never committed as generated output |
| optional attestation placeholder | `attestation.intoto.jsonl` | placeholder only; future signing/attestation approval required |

Cleanup and no-source-inclusion behavior:

- Test output roots must be OS temp dirs or ignored local dirs created/cleaned at runtime.
- Local prototype output roots must be removed before commit.
- Repository scans must reject checked-in native binaries, tarballs, generated manifests, generated artifacts, and generated package artifacts.
- Package scans must reject `.agentteam-artifacts/`, native/helper/generated output dirs, tarballs, binaries, generated manifests, and generated package artifacts.
- Candidate output file names must be package-relative and safe: no absolute paths, no `..`, no path traversal, and no backslash-only platform escape.

Repository/package no-artifact scan policy:

- No checked-in native binaries.
- No checked-in tarballs.
- No checked-in generated manifests/artifacts.
- No checked-in generated package artifacts.
- No package files entry for `.agentteam-artifacts/`.
- No package files entry for native/helper/generated outputs.
- No CI workflow/release upload/npm publish/package scripts in Slice 3.

Slice 3 preserves the Slice 1 release/package boundary and Slice 2 build command policy: GitHub-only checkpoint, no npm package metadata by default, GitHub Actions artifacts only as future prototype storage after approval, GitHub release assets as future explicit release-policy gate, npm companion packages as future package-owner gate, no postinstall/download/install-time build policy, and `go build` only in explicit artifact-generation CI/local prototype after approval.

## Slice 4 — Manifest / Checksum / Provenance / License / Executable Generator Prototype

Slice 4 is test-local generator prototype only. It moves from v0.4.25 manifest validation to generating manifest/checksum/provenance/license/executable metadata from artifact output files under OS temp dirs in tests.

Focused suite: `tests/suites/go-kernel-v0426-manifest-provenance-generator.cjs`.

Generator prototype evidence:

- Creates fake helper files only under OS temp dirs at test runtime.
- Generates metadata only under OS temp dirs and cleans it.
- Generated metadata is not committed and not release metadata.
- Generated metadata is not GitHub release metadata, npm package metadata, or package inclusion metadata.
- Attestation/signing fields are placeholders only and must not be represented as real signing.
- No package/native/default/fallback behavior is approved.

Generated metadata fields:

- `schemaVersion`.
- `packageName` / `packageVersion` (`pi-agentteam` / `0.6.8`).
- `module` = `tmuxSnapshotParse`.
- `helperVersion`.
- `protocolVersion` = `1`.
- `capabilities` including `tmuxSnapshotParse`.
- OS/arch/libc.
- artifact filename/path safe package-relative concept.
- file size.
- SHA-256.
- executable flag / POSIX mode or Windows executable extension policy.
- sourceRevision placeholder.
- workflow/run identity placeholder.
- toolchain identity placeholder.
- generatedAt policy.
- license metadata/copy checksum.
- attestation/signing placeholders that are explicitly not real signing.

Validation expectations:

- manifest fields are present.
- checksum/size match helper file.
- executable policy matches platform.
- license/provenance fields are present.
- no path traversal/absolute paths.
- generated manifest would satisfy v0.4.25 validation shape for the same module/protocol/capability.
- missing provenance fails closed.
- missing license fails closed.
- checksum mismatch fails closed.
- size mismatch fails closed.
- unsafe artifact path fails closed.
- wrong module/protocol/capability fails closed.
- real signing/attestation claim without proof fails closed.
- failure output must not leak absolute helper path, temp root, repo/cwd, stdout/stderr, raw manifest/provenance/license body, stack traces, or package internals.

Slice 4 preserves Slice 1-3 boundaries: GitHub-only evidence, no npm package metadata by default, no helper build commands, no CI workflow, no real artifacts/manifests, no release assets, no npm package inclusion, no install/runtime download path, no postinstall/download/install-time build, and `go build` only in explicit artifact-generation CI/local prototype after future approval.

Slice 4 STOP gates:

- STOP for helper build commands.
- STOP for running `go build`.
- STOP for CI workflow.
- STOP for real repo artifacts/manifests.
- STOP for checked-in native binaries.
- STOP for tarballs.
- STOP for generated package artifacts.
- STOP for GitHub release assets.
- STOP for npm package inclusion.
- STOP for package metadata changes.
- STOP for package scripts.
- STOP for go.mod/go.sum.
- STOP for default Go.
- STOP for current `go-cutover` behavior changes.
- STOP for `go-packaged-preview` availability semantics changes.
- STOP for TypeScript fallback deletion.
- STOP for hidden TS parser fallback rollback after cutover.
- STOP for broadening Go authority beyond parser-only stdin/stdout `tmuxSnapshotParse`.
- STOP for `compactReadModelFingerprint` cutover.
- STOP for native Go pi extension assumption.
- STOP for `/team readiness` expansion.
- STOP for later work without separate approval.

## Slice 5 — Artifact Smoke and Clean-Install Handoff

Slice 5 is test-local artifact smoke / clean-install handoff only. It shows how generated-temp artifact outputs from the v0.4.26 pipeline prototype can be smoke-tested and handed off to a future clean-install/package proof.

Focused suite: `tests/suites/go-kernel-v0426-artifact-smoke-handoff.cjs`.

Smoke proof:

- Direct smoke uses generated-temp artifact under explicit local/test path only.
- The fake/generated-temp helper is invoked/read in a controlled test-local way.
- Health response is deterministic.
- `tmuxSnapshotParse` response or parser capability smoke is deterministic.
- No default resolver activation.
- No hidden TS parser fallback on explicit smoke failure.
- All generated/smoked files are under OS temp dirs at test runtime and cleaned.

Handoff proof:

- Generated artifact metadata can feed future clean-install proof.
- Generated-temp artifact metadata is copied/mapped into a temp installed layout like v0.4.25 clean-install simulation.
- Package install remains simulated.
- Companion package metadata remains future work.
- Optional dependency installation remains future work.
- npm tarball behavior remains future work.
- User install path remains future work.
- Default resolver discovery remains simulated/future.
- Generated artifact is prototype input to future clean-install proof, not normal-user availability proof.
- No source checkout dependency for installed-layout handoff.
- No Go toolchain, network, lifecycle download, install-time build, or manual helper env in the simulated handoff.

Simulated-vs-real distinction:

- Real prototype evidence: explicit local/test path smoke of a fake generated-temp helper, deterministic health/capability smoke, metadata handoff into an OS-temp installed layout, and fail-closed/no-leak negative cases.
- Still simulated/future: package manager install, companion package metadata, optional dependency installation, npm tarball behavior, user install path, GitHub release assets, package inclusion, default resolver discovery, and normal-user native availability.
- No package/native/default/fallback behavior is approved.

Negative cases fail closed for:

- corrupt health JSON.
- missing `tmuxSnapshotParse` capability.
- wrong protocol/helper version.
- checksum mismatch.
- missing installed layout mapping.
- attempted default resolver use.
- attempted hidden TS parser fallback.

Slice 5 preserves Slice 1-4 boundaries: GitHub-only evidence, no helper build commands, no `go build`, no CI workflow, no real repo artifacts/manifests, no release assets, no package metadata changes, no package scripts, no go.mod/go.sum, no default Go, no TypeScript fallback deletion, no broad Go authority, no native Go pi extension assumption, and no `/team readiness` expansion.

Slice 5 STOP gates:

- STOP for helper build commands.
- STOP for running `go build`.
- STOP for CI workflow.
- STOP for real repo artifacts/manifests.
- STOP for checked-in native binaries.
- STOP for tarballs.
- STOP for generated package artifacts.
- STOP for GitHub release assets.
- STOP for npm package inclusion.
- STOP for package metadata changes.
- STOP for package scripts.
- STOP for go.mod/go.sum.
- STOP for production runtime resolver behavior.
- STOP for default resolver activation.
- STOP for default Go.
- STOP for current `go-cutover` behavior changes.
- STOP for `go-packaged-preview` availability semantics changes.
- STOP for TypeScript fallback deletion.
- STOP for hidden TS parser fallback rollback after cutover.
- STOP for broadening Go authority beyond parser-only stdin/stdout `tmuxSnapshotParse`.
- STOP for `compactReadModelFingerprint` cutover.
- STOP for native Go pi extension assumption.
- STOP for `/team readiness` expansion.
- STOP for later work without separate approval.

## Slice 6 — Storage, Release, and Rollback Policy

Slice 6 is docs/tests storage/release/rollback policy only. It decides how generated artifact outputs may be stored before package publication exists, and it continues rollback/deprecation/default-disable/version-skew policy without relying on hidden TS fallback after cutover.

Focused suite: `tests/suites/go-kernel-v0426-storage-release-policy-docs.cjs`.

Storage decision matrix:

| Storage channel | Current v0.4.26 policy | Evidence boundary |
| --- | --- | --- |
| OS temp/local outputs for tests | allowed only as test-local outputs and cleaned | test-local prototype evidence only; no repo/package inclusion |
| ignored local prototype directory | future only after explicit approval and excluded from package files | `.agentteam-artifacts/` remains ignored/local concept, not package content |
| CI workflow workspace outputs | future only after approval | no CI workflow is added in Slice 6 |
| GitHub Actions artifacts for prototype review | future prototype storage after approval | review-only, limited retention, not release asset, not install source, not normal-user availability proof |
| GitHub release assets | STOP until explicit release-policy approval | no active GitHub release assets are defined, uploaded, or approved |
| npm companion packages | STOP until package-owner approval | companion package path remains future option only |
| main package inclusion | STOP | no native helper/package inclusion is approved |
| postinstall/download/install-time build | prohibited | no postinstall/download/install-time build remains binding |

Future GitHub Actions artifact retention/access expectations:

- review-only.
- limited retention.
- not release asset.
- not install source.
- not normal-user availability proof.
- not package publication proof.
- not default/native/fallback approval.

Rollback/deprecation/default-disable scenarios:

- bad generated artifact.
- bad manifest/checksum/provenance/license.
- bad helper smoke.
- stale helper.
- unsupported platform.
- broken diagnostics.
- bad storage upload.
- accidental release asset.
- package deprecation/unpublish.
- bad future default resolver.

Version-skew policy:

- package version must match.
- helper version must match.
- protocol must match.
- module must match.
- capability must match.
- platform must match.
- checksum must match.
- skew fails closed.
- no hidden TS fallback as rollback after cutover.
- rollback is corrected release/tag/package/deprecation/default-disable policy.

Slice 6 preserves Slice 1-5 boundaries: TS/pi control plane mandatory, Go helper behind TS adapter/ports via subprocess/RPC/stdin-stdout, Go authority parser-only `tmuxSnapshotParse`, `compactReadModelFingerprint` remains non-cutover / TypeScript fallback, no native Go pi extension assumption, no helper build commands, no `go build`, no CI workflow, no real repo artifacts/manifests, no active artifact storage, no GitHub release assets, no npm companion package approval, no main package inclusion, no package metadata changes, no package scripts, no go.mod/go.sum, no production runtime resolver behavior changes, no `core/kernel.ts` production behavior changes, no default resolver activation, no default Go, no current `go-cutover` behavior changes, no `go-packaged-preview` availability semantics changes, no TypeScript fallback deletion, no hidden TS parser fallback rollback after cutover, no broad Go authority, no `/team readiness` expansion, and no Slice 7.

Slice 6 STOP gates:

- STOP for helper build commands.
- STOP for running `go build`.
- STOP for CI workflow.
- STOP for real repo artifacts/manifests.
- STOP for checked-in native binaries.
- STOP for tarballs.
- STOP for generated package artifacts.
- STOP for active GitHub Actions artifact storage without explicit approval.
- STOP for GitHub release assets until explicit release-policy approval.
- STOP for npm companion packages until package-owner approval.
- STOP for main package inclusion.
- STOP for postinstall/download/install-time build.
- STOP for package metadata changes.
- STOP for package scripts.
- STOP for go.mod/go.sum.
- STOP for production runtime resolver behavior.
- STOP for production `core/kernel.ts` behavior changes.
- STOP for default resolver activation.
- STOP for default Go.
- STOP for current `go-cutover` behavior changes.
- STOP for `go-packaged-preview` availability semantics changes.
- STOP for TypeScript fallback deletion.
- STOP for hidden TS parser fallback rollback after cutover.
- STOP for broadening Go authority beyond parser-only stdin/stdout `tmuxSnapshotParse`.
- STOP for `compactReadModelFingerprint` cutover.
- STOP for native Go pi extension assumption.
- STOP for `/team readiness` expansion.
- STOP for later work without separate approval.

## Slice 7 — Final Checkpoint and Guardrail Consolidation

Slice 7 is final GitHub-only checkpoint docs/tests consolidation. It summarizes Slice 1-6 Go helper artifact generation pipeline prototype evidence, validation matrix, GO/STOP decision, remaining blockers, and unchanged runtime/package/default/native/fallback/readiness invariants.

Focused suite: `tests/suites/go-kernel-v0426-artifact-pipeline-checkpoint-docs.cjs`.

Final checkpoint doc: `docs/perf/v0.4.26-go-helper-artifact-pipeline-checkpoint.md`.

Slice 7 preserves Slice 1-6 boundaries: docs/tests only, no helper build commands, no `go build`, no CI workflow, no real repo artifacts/manifests, no active GitHub Actions artifact storage, no GitHub release assets, no npm companion packages, no main package inclusion, no package metadata changes, no package scripts, no go.mod/go.sum, no production runtime resolver behavior changes, no `core/kernel.ts` production behavior changes, no default Go, no `go-cutover` behavior changes, no `go-packaged-preview` semantics changes, no TypeScript fallback deletion, no hidden TS parser fallback rollback after cutover, no `compactReadModelFingerprint` cutover, no broad Go authority, no native Go pi extension assumption, no `/team readiness` expansion, no commit/tag/push, and no later work.

Slice 7 final checkpoint recommendation: proceed only with GitHub-only v0.4.26 Go helper artifact generation pipeline prototype checkpoint review after leader/user approval.

## Out of Scope

The following are out of scope and remain STOP until separately approved:

- npm version/publish.
- package metadata changes.
- optionalDependencies/lifecycle hooks/downloads/package scripts.
- lockfiles/go.mod/go.sum unless explicitly approved later.
- checked-in binaries/tarballs/generated manifests/artifacts.
- GitHub release assets without release-policy approval.
- npm package inclusion.
- default Go.
- `go-cutover`/`go-packaged-preview` semantic changes.
- TypeScript fallback deletion.
- native Go pi extension.
- `/team readiness` expansion.
- helper build commands.
- production runtime resolver behavior.
- production `core/kernel.ts` behavior changes.
- real package inclusion/native artifact approval.
- hidden TS parser fallback rollback after cutover.
- broad Go authority beyond parser-only stdin/stdout `tmuxSnapshotParse`.

## Release and Package Boundary

Release/package boundary for v0.4.26 Slice 1:

- GitHub-only checkpoint.
- no npm package metadata by default.
- GitHub Actions artifacts may be future prototype storage after approval.
- GitHub release assets are future explicit release-policy gate.
- npm companion packages are future package-owner gate.
- no postinstall/download/install-time build policy remains binding.
- no checked-in native binaries, tarballs, generated manifests/artifacts, or generated package artifacts.
- no GitHub release assets in Slice 1.
- no real package inclusion or native artifact approval.
- no package/default/native/fallback behavior approval.

## Build Matrix Placeholder

Candidate build matrix rows for future prototype discussion only:

| Target | Status | Notes |
| --- | --- | --- |
| linux-x64-glibc | candidate placeholder | no support claim yet |
| linux-arm64-glibc | candidate placeholder | no support claim yet |
| darwin-arm64 | candidate placeholder | no support claim yet |
| darwin-x64 | candidate placeholder | no support claim yet |
| win32-x64 | candidate placeholder | no support claim yet |
| linux-x64-musl | future unsupported until proven | no support claim yet |
| linux-arm64-musl | future unsupported until proven | no support claim yet |
| win32-arm64 | future unsupported until proven | no support claim yet |
| other os/arch/libc targets | future unsupported until proven | no support claim yet |

This build matrix placeholder does not approve supported platforms, generated artifacts, GitHub release assets, npm package inclusion, default Go, or fallback deletion.

## STOP Gates

STOP future v0.4.26 work if any of these appear without explicit owner approval:

- helper build command implementation.
- CI workflow implementation in Slice 1.
- generated real artifacts/manifests.
- checked-in native binaries.
- tarballs.
- generated package artifacts.
- GitHub release assets.
- npm package inclusion.
- npm version/publish.
- package version change.
- package.json metadata changes.
- optionalDependencies.
- lifecycle hooks/downloads.
- helper build/install/package scripts.
- lockfiles.
- go.mod/go.sum.
- production runtime resolver behavior changes.
- production `core/kernel.ts` behavior changes.
- default Go enablement.
- current `go-cutover` behavior changes.
- `go-packaged-preview` availability semantics changes.
- TypeScript fallback deletion.
- hidden TS parser fallback rollback after cutover.
- `compactReadModelFingerprint` cutover.
- broad Go authority beyond parser-only stdin/stdout `tmuxSnapshotParse`.
- native Go pi extension assumption.
- `/team readiness` expansion.
- normal-user native availability claims.
- native/default cutover approval.
- fallback deletion approval.
- native packaging approval.
- package metadata changed claims.
- checked-in artifacts allowed claims.
- later work without separate approval.

## Slice 1 Validation Plan

Required Slice 1 validation:

- syntax check: `node --check tests/suites/go-kernel-v0426-artifact-pipeline-contract-docs.cjs`
- Slice 2 syntax check: `node --check tests/suites/go-kernel-v0426-build-matrix-policy-docs.cjs`
- Slice 3 syntax check: `node --check tests/suites/go-kernel-v0426-artifact-output-policy.cjs`
- Slice 4 syntax check: `node --check tests/suites/go-kernel-v0426-manifest-provenance-generator.cjs`
- Slice 5 syntax check: `node --check tests/suites/go-kernel-v0426-artifact-smoke-handoff.cjs`
- Slice 6 syntax check: `node --check tests/suites/go-kernel-v0426-storage-release-policy-docs.cjs`
- Slice 7 checkpoint syntax check: `node --check tests/suites/go-kernel-v0426-artifact-pipeline-checkpoint-docs.cjs`
- focused guard: `node tests/run.cjs go-kernel-v0426-artifact-pipeline-contract-docs`
- Slice 2 focused guard: `node tests/run.cjs go-kernel-v0426-build-matrix-policy-docs`
- Slice 3 focused guard: `node tests/run.cjs go-kernel-v0426-artifact-output-policy`
- Slice 4 focused guard: `node tests/run.cjs go-kernel-v0426-manifest-provenance-generator`
- Slice 5 focused guard: `node tests/run.cjs go-kernel-v0426-artifact-smoke-handoff`
- Slice 6 focused guard: `node tests/run.cjs go-kernel-v0426-storage-release-policy-docs`
- Slice 7 checkpoint focused guard: `node tests/run.cjs go-kernel-v0426-artifact-pipeline-checkpoint-docs`
- package/native sanity scan inside the focused guard.
- repo artifact/tarball/generated manifest/package artifact scan inside the focused guard.
- `git diff --check`

Recommended broader validation when practical:

- `node tests/run.cjs`
- `npm run typecheck`
- `npm run -s check:boundaries`
- `npm run --silent bench:team-panel-tmux`
- `PI_AGENTTEAM_KERNEL=go-packaged-preview npm run --silent bench:team-panel-tmux`

## Future v0.4.26 Validation Matrix

Future slices should add validation for:

- build matrix definition guard.
- helper build command policy guard.
- artifact output path policy guard.
- local artifact output prototype under temp or ignored output roots only.
- CI artifact output prototype after explicit approval.
- manifest/checksum/provenance/license/executable generation prototype.
- attestation/signing placeholder guard.
- direct artifact smoke guard.
- clean-install handoff guard.
- storage/release/rollback policy guard.
- package/native sanity scan.
- repo artifact/tarball/generated manifest/package artifact scan.
- `node tests/run.cjs`.
- `npm run typecheck`.
- `git diff --check`.

## Final Recommendation

Proceed only with GitHub-only v0.4.26 Go helper artifact generation pipeline prototype checkpoint review after leader/user approval.

Do not implement helper build commands, run `go build`, add CI workflows, activate GitHub Actions artifact storage, define GitHub release assets, add npm companion packages, include native artifacts in the main package, change package metadata, add package scripts, change production resolver behavior, enable default Go, delete TypeScript fallback, expand `/team readiness`, assume native Go pi extension surfaces, commit/tag/push, or start later work in this implementation slice.
