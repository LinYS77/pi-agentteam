# v0.6.57 Go paneExists Facade Cutover

Result: v0.6.57 cuts over the TypeScript `paneExists(paneId)` facade/default path to the already Go-backed `inspectPane(paneId)` facade.

This slice closes the direct `display-message` fallback gap for the boolean pane existence helper:

```text
tmux/core.ts paneExists(paneId) -> Go-backed inspectPane(paneId) -> boolean exists result
```

## Runtime Behavior

- `tmux/core.ts` `paneExists(paneId)` now returns `Boolean(paneId && inspectPane(paneId).exists)`.
- The TypeScript `runTmuxNoThrow(['display-message', '-p', '-t', paneId, '#{pane_id}'])` fallback for this facade is removed.
- `paneExists(paneId)` preserves the existing boolean public API.
- Helper failure, invalid pane id, and pane-not-found all fail closed to `false`.
- Kernel-level diagnostics remain compact on the inspect adapter result; the public facade returns only `true` or `false`.

## Preserved Boundaries

- `resolvePaneBinding()` is cut over separately by v0.6.58, `targetForPaneId()` by v0.6.59, and `captureCurrentPaneBinding()` by v0.6.60; `resolvePaneBindingAsync()` remains TypeScript `display-message`-owned.
- `windowExists()` and `firstPaneInWindow()` remain TypeScript window helper paths.
- `inspectPane()` and `listAgentTeamPanes()` remain delegated through their Go-backed facade seams.
- Mutating worker lifecycle remains TypeScript-owned: no `wakePane`, `syncPaneLabels`, `createTeammatePane`, `killPane`, `clearPaneLabel`, send-keys, label mutation, split-window, new-window, kill-pane, select-pane, or respawn-pane migration.
- State repository, task/report/PlanRun governance, team panel view-model, release/package verification, and package/native ownership remain unmigrated.
- No Go source or native helper rebuild is required for this facade-only cutover.

## Package And Release Guards

No package/release action is authorized in this slice:

- no `npm version`.
- no `npm publish`.
- no tag or GitHub release.
- no package lockfiles.
- no `go.mod` or `go.sum`.
- no lifecycle hooks, postinstall downloads, or runtime `go build`.
- no native artifact path or binary rename.

`package.json` remains `0.6.8`.

## Evidence

- `docs/perf/v0.6.57-go-pane-exists-facade-cutover.md`
- `tests/fixtures/kernel/v0657/goPaneExistsFacadeCutover.cjs`
- `tests/suites/go-kernel-v0657-go-pane-exists-facade-cutover.cjs`

## Validation Scope

The v0.6.57 guard suite verifies:

- the TypeScript `paneExists(paneId)` facade delegates to the Go-backed `inspectPane(paneId)` facade.
- the direct TypeScript `display-message` fallback is removed from `paneExists()`.
- facade success and failure return boolean `true`/`false` only.
- `inspectPane()` and `listAgentTeamPanes()` stay cut over to Go.
- `resolvePaneBinding()`, `targetForPaneId()`, and `captureCurrentPaneBinding()` are later cut over by v0.6.58-v0.6.60; `resolvePaneBindingAsync()` and window helpers remain TypeScript-owned.
- Go does not add mutating tmux commands.
- package/native path, package version, and release surfaces remain unchanged.
