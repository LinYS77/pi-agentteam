# v0.6.28 Final Prep and v0.6.29 Entry

> Scope: short docs/tests-only final-prep checkpoint before the formal v0.6.29 builder/resolver refactor. This records version namespace correction and the v0.6.29 entry decision. It does not implement builder, resolver, smoke, package, release, default, readiness, UI, or runtime behavior.

## Version Namespace Correction

- `v0.4.27` is a legacy/misnamed GitHub tag.
- the canonical roadmap checkpoint is `v0.6.27`.
- `v0.4.27` and `v0.6.27` point to commit `bc25c3c`.
- do not delete or move `v0.4.27`.
- do not amend `bc25c3c`.
- historical v0427 files/tests are not batch-renamed.
- from v0.6.28/v0628 onward, new checkpoint docs/tests use the corrected v0.6.x / v06xx namespace.
- `package.json` remains `0.6.8`.
- roadmap checkpoint tags and npm package version remain separate.

## v0.6.28 Final-Prep Decision

v0.6.28 is the last small preparation version before the formal implementation refactor.

v0.6.29 is GO for real local/reviewer-controlled Go helper artifact builder and explicit preview manifest resolver.

The first target module is `tmuxSnapshotParse` because it is the only existing cutover-owned candidate, and Go helper/adapter/diagnostics scaffolding already exists.

## v0.6.29 Entry Plan Summary

v0.6.29 may implement the following with separate approval:

- real local helper artifact builder: reviewer/CI utility may run `GO111MODULE=off go build` and output only to OS temp or ignored `.agentteam-artifacts/`.
- real artifact validation/smoke: real helper runs JSON-RPC `health` and `tmuxSnapshotParse`.
- explicit manifest resolver module, for example future `core/kernelPackagedResolver.ts`, used only on explicit preview paths.
- integrate resolver into `go-packaged-preview` only when explicit manifest/root input is set.
- keep default/go/auto/current `go-cutover` from discovering packaged layout.
- real clean-install preview smoke: real artifact → temp installed layout → explicit resolver → adapter preview parse.

## v0.6.28 STOP

STOP in v0.6.28 for:

- implementing v0.6.29 builder/resolver/smoke.
- running `go build`.
- adding CI workflow.
- adding artifact upload.
- adding release assets.
- changing package metadata.
- adding `optionalDependencies`.
- adding package scripts.
- adding lifecycle hooks.
- adding postinstall/download/install-time build.
- adding go.mod/go.sum.
- adding lockfiles.
- checking in native binaries.
- checking in tarballs.
- checking in generated artifacts/manifests.
- default Go.
- default resolver.
- TypeScript fallback deletion.
- `go-cutover` behavior changes.
- `go-packaged-preview` semantic changes beyond future plan text.
- `/team readiness` expansion.
- normal-user UI/tool/runtime diagnostics.
- npm version/publish.
- commit/tag/push.

## Validation

Required v0.6.28 validation:

- `node --check tests/suites/go-kernel-v0628-final-prep-entry-guard.cjs`
- focused guard: `tests/suites/go-kernel-v0628-final-prep-entry-guard.cjs`
- `git diff --check`
- `node tests/run.cjs` when feasible.

Do not run npm version/publish/pack, do not run `go build`, and do not commit/tag/push in v0.6.28 final prep.
