# v0.6.69 Go Detached First Pane Cutover

Result: v0.6.69 cuts over the `tmux/windows.ts` detached `ensureSwarmWindow()` leader pane selection from direct TypeScript `list-panes` parsing to existing Go-backed `firstPaneInWindow(initialTarget, signal)`.

This slice removes only the detached first-pane selection fallback after `initialTarget` is known. It does not move post-creation window lookup, session/window creation, labels, mutating lifecycle, state, task/report/PlanRun, UI, or release/package behavior into Go:

```text
tmux/windows.ts ensureSwarmWindow() detached branch
  -> firstPaneInWindow(initialTarget, signal)
  -> workerLifecycle.listPanesInWindow
  -> compact first pane id
  -> resolvePaneBindingAsync(leaderPaneId, signal)
  -> compact target from Go-backed binding
```

## Runtime Behavior

- `tmux/windows.ts` now calls `firstPaneInWindow(initialTarget, signal)` as the sole detached leader pane source after `initialTarget` is known.
- The direct TypeScript `runTmuxAsync(['list-panes', '-t', initialTarget, '-F', '#{pane_id}'], undefined, signal)` parsing is removed from the detached branch.
- If `firstPaneInWindow(initialTarget, signal)` cannot provide a pane id, `ensureSwarmWindow()` throws compact `Error('Failed to resolve agentteam leader pane')`.
- `resolvePaneBindingAsync(leaderPaneId, signal)` remains the Go-backed target source after leader pane selection.
- Successful detached behavior still returns `{ session, window, target, leaderPaneId }`.
- Raw stdout, raw stderr, cwd, stack, helper paths, mailbox/report bodies, worker transcript bodies, and full helper diagnostics are not exposed.

## Preserved Boundaries

- Post-creation `list-windows -F '#{window_id}\t#{window_name}'`, `new-session`, `new-window`, marking, labels, kill, state/task/UI/release/package remain TypeScript-owned.
- `markWindowAsAgentTeam()` and `refreshWindowPaneLabels()` remain TypeScript-owned.
- `createTeammatePane()`, pane splitting, pane labels, `wakePane()`, and `killPane()` remain TypeScript-owned.
- The inside-tmux current binding fallback remains the v0.6.67 `captureCurrentPaneBinding()` reuse cutover.
- The detached leader target fallback remains the v0.6.68 `resolvePaneBindingAsync()` reuse cutover.
- `sessionExists()` remains the v0.6.66 Go-backed session existence facade.
- `findAgentTeamWindowTarget()` remains the v0.6.65 Go-backed marked-window discovery facade.
- The native helper path and binary name remain unchanged.

No Go source or native artifact rebuild is required for this slice. `package.json` remains `0.6.8`.

## Guard Evidence

The v0.6.69 guard suite verifies:

- `ensureSwarmWindow()` detached leader pane selection uses `firstPaneInWindow(initialTarget, signal)`.
- the direct TypeScript `list-panes -t initialTarget -F '#{pane_id}'` parsing is removed.
- missing first pane throws only the compact `Failed to resolve agentteam leader pane` error.
- no hidden TypeScript `list-panes` fallback remains for the same behavior.
- post-creation `list-windows`, session/window creation, marking, and label refresh remain TypeScript-owned.
- `firstPaneInWindow()` remains Go-backed through `workerLifecycle.listPanesInWindow`.
- `resolvePaneBindingAsync()` remains Go-backed through `workerLifecycle.inspectPane` and compact `target`.
- Go source and existing native artifact files are unchanged by this slice.
- package/native path/name/version/release guardrails hold.

Evidence files:

- `tests/fixtures/kernel/v0669/goDetachedFirstPaneCutover.cjs`
- `tests/suites/go-kernel-v0669-go-detached-first-pane-cutover.cjs`
