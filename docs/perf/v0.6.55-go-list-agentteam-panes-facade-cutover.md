# v0.6.55 Go listAgentTeamPanes Facade Cutover

Result: v0.6.55 cuts over the TypeScript `listAgentTeamPanes()` facade/default path to the existing Go `workerLifecycle.listAgentTeamPanes` adapter.

This slice closes the remaining fallback gap for the already implemented read-only pane listing operation:

```text
tmux/core.ts listAgentTeamPanes() -> TypeScript kernel adapter -> Go workerLifecycle.listAgentTeamPanes -> compact labeled pane list
```

## Runtime Behavior

- `tmux/core.ts` `listAgentTeamPanes()` now delegates to `createAgentTeamKernelAdapter().listAgentTeamPanes()`.
- The TypeScript `runTmuxNoThrow(['list-panes', '-a', '-F', ...])` fallback for this facade is removed.
- `listAgentTeamPanes()` returns `result.ok ? result.panes : []`.
- The Go worker lifecycle adapter remains responsible for the v0.6.54 semantics: only panes with non-empty `paneId` and non-empty label (`@agentteam-name`) are returned, and `byPaneId` is built from that filtered list only.
- Returned pane objects remain compact: `paneId`, `target`, `label`, and `currentCommand`.

## Failure Policy

When the Go helper is missing, unavailable, incompatible, or returns a failed worker lifecycle result, the public facade returns `[]`.

Kernel-level diagnostics remain compact on the adapter result and do not leak raw stdout, stderr, cwd, stack traces, state archives, mailbox bodies, report bodies, or worker transcripts.

## Preserved Boundaries

- `listAgentTeamPanesFromSnapshot()` remains unchanged and still filters snapshot panes by `item.paneId && item.label`.
- No `inspectPane`, `wakePane`, `syncPaneLabels`, `createTeammatePane`, `killPane`, `clearPaneLabel`, `targetForPaneId`, `captureCurrentPaneBinding`, or `display-message` path is migrated in this slice.
- Mutating worker lifecycle remains TypeScript-owned.
- State repository, task/report/PlanRun governance, team panel view-model, release/package verification, and package/native ownership remain unmigrated.
- No Go source or native helper rebuild is required for this facade-only cutover.

## Package And Release Guards

No package/release action is authorized in this slice:

- no `npm version`.
- no `npm publish`.
- no tag or GitHub release.
- no package lockfiles.
- no `go.mod` or `go.sum`.
- no lifecycle hooks, postinstall downloads, or runtime `go build`.
- no native artifact path or binary rename.

`package.json` remains `0.6.8`.

## Evidence

- `docs/perf/v0.6.55-go-list-agentteam-panes-facade-cutover.md`
- `tests/fixtures/kernel/v0655/goListAgentTeamPanesFacadeCutover.cjs`
- `tests/suites/go-kernel-v0655-go-list-agentteam-panes-facade-cutover.cjs`

## Validation Scope

The v0.6.55 guard suite verifies:

- the TypeScript `listAgentTeamPanes()` facade delegates through the kernel adapter.
- the direct TypeScript `tmux list-panes` fallback is removed from that facade.
- facade success returns the adapter panes and facade failure returns `[]`.
- `listAgentTeamPanesFromSnapshot()` keeps the existing labeled-pane filter.
- `inspectPane` and mutating lifecycle remain TypeScript-owned.
- Go does not add mutating tmux commands.
- package/native path, package version, and release surfaces remain unchanged.
