# v0.6.48 Default-Go Cutover For tmuxSnapshotParse

Result: v0.6.48 enables the approved actual default-Go cutover for `tmuxSnapshotParse` only.

This slice changes the runtime default for the bounded parser module after explicit leader/user approval of the main-package embedded helper layout. It does not make AgentTeam a Go control plane, a native package product, or a release-ready artifact.

## Runtime Behavior

- default/unset and explicit `go` resolve the embedded main-package helper manifest at `native/tmuxSnapshotParse/0.3.0-read-model-shadow/linux-x64-glibc/`.
- `tmux/snapshot.ts` no longer contains a TypeScript runtime parser fallback.
- missing, unsupported, integrity-failed, and incompatible helper paths return an unknown/stale snapshot with compact cutover diagnostics.
- `disabled` and `typescript` remain the rollback/default-disable modes; after TypeScript parser fallback deletion they produce explicit unknown/stale parser diagnostics rather than restoring a hidden TypeScript parser.
- `go-packaged-preview` remains explicit preview-only behavior.
- `compactReadModelFingerprint` remains TypeScript-owned for default/`go` cutover behavior.

## Authority Boundary

Go is parser-only for `tmuxSnapshotParse`. Go still does not execute tmux, capture panes, own pane/session/worker lifecycle, write state, govern task/report/PlanRun, read full-text mailbox/report bodies, render UI, or manage package/release behavior.

TypeScript/pi remains authoritative for tmux command execution and capture, pane/session/worker lifecycle, state/repository writes, task/report/PlanRun governance, mailbox/report full-text boundaries, UI rendering, extension lifecycle, and package/release policy.

## Embedded Helper Surface

The only approved embedded native surface is:

- `native/tmuxSnapshotParse/0.3.0-read-model-shadow/linux-x64-glibc/agentteam-tmuxSnapshotParse`
- `native/tmuxSnapshotParse/0.3.0-read-model-shadow/linux-x64-glibc/manifest.json`
- `native/tmuxSnapshotParse/0.3.0-read-model-shadow/linux-x64-glibc/SHA256SUMS`
- `native/tmuxSnapshotParse/0.3.0-read-model-shadow/linux-x64-glibc/provenance.json`
- `native/tmuxSnapshotParse/0.3.0-read-model-shadow/linux-x64-glibc/LICENSE`
- `native/tmuxSnapshotParse/0.3.0-read-model-shadow/linux-x64-glibc/license.json`
- `native/tmuxSnapshotParse/0.3.0-read-model-shadow/linux-x64-glibc/attestation.intoto.jsonl`

No other native helper locations, package manager native delivery, release assets, signatures, installers, tarballs, lockfiles, `go.mod`, or `go.sum` are introduced.

## Package And Release Guards

No `npm version`, `npm publish`, tag, GitHub release, lockfile, `go.mod`, or `go.sum` is introduced. Package/release/signing/second-platform approval remains false.

This slice only updates `package.json` `files` for the exact approved embedded helper surface. It does not add lifecycle scripts, `bin`, `main`, `exports`, native package metadata fields, optional dependencies, bundled dependencies, or package manager install hooks.

## Evidence

- `tests/fixtures/kernel/v0648/defaultGoCutoverTmuxSnapshot.cjs`
- `tests/suites/go-kernel-v0648-default-go-cutover-tmux-snapshot.cjs`
- `tests/fixtures/kernel/tmux/snapshotCases.cjs`
- `tests/suites/go-kernel-tmux-snapshot-parser.cjs`
- `tests/suites/go-kernel-tmux-cutover-failure-classes.cjs`
- `scripts/lib/go-helper-clean-install-proof.cjs`
- `scripts/lib/pi-extension-install-load-proof.cjs`

## Validation Scope

The v0.6.48 guard suite covers embedded manifest integrity, default/unset and explicit `go` parser success, resolver failure fail-closed behavior, incompatible helper fail-closed behavior, rollback/default-disable modes, TypeScript/pi control-plane boundaries, package surface limits, and no raw evidence leaks.
