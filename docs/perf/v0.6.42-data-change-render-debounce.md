# v0.6.42 Data-Change Render Debounce p95 Evidence

> Scope: focused v0.6.42 evidence for mounted `/team` semantic data-change render debounce rate. This uses a clean temporary `PI_AGENTTEAM_HOME` harness and does not execute manual RC, change package metadata, enable default-Go/native, tag, publish, or claim v0.7 release readiness.

## Result

Result: v0.6.42 adds focused mounted `/team` semantic data-change render debounce coverage and a minimal panel debounce runtime fix.

Final result remains `ready:false`. The `data-change-render-debounce-rate` gate passes in this focused run, but this slice does not re-execute manual RC or prove every release p95 gate.

This checkpoint is GO for reviewer inspection of the mounted panel harness, clean-temp isolation contract, semantic data-change render rate evidence, manual direct refresh/no-op refresh boundaries, no-leak guard, and preserved local `/tmp` sanitized timing artifact.

This checkpoint is STOP for release/tag/git push/npm version/npm publish/native/default-Go/package/signing/second-platform/fallback-deletion work, TypeScript/pi facade authority violations, raw terminal logs, screenshots, raw state archive or full-text evidence check-in, manual RC pass claims, all-p95-gates claims, and claiming v0.7 release readiness.

STOP for release/tag/git push/npm version/npm publish/native/default-Go/package/signing/second-platform/fallback-deletion work.

## Harness

Focused command:

```bash
PI_AGENTTEAM_PROFILE=1 node scripts/verify-v0642-data-change-render-debounce.cjs --out /tmp/pi-agentteam-v0642-data-change-render-debounce-latest.json
```

The verifier calls `scripts/lib/v0642-data-change-render-debounce-harness.cjs`, which:

- Creates a clean temporary `PI_AGENTTEAM_HOME` under `/tmp/pi-agentteam-v0642-data-change-render-debounce.*` and rejects unsafe/non-empty homes by default.
- Writes a clean temp config with `ui.teamPanel.refreshMode='debounced'` and `ui.teamPanel.minRefreshMs=250`.
- Transpiles the TypeScript extension to an isolated temporary dist with test stubs for pi/TUI dependencies.
- Mounts the real `openTeamPanel(ctx, teamName)` attached panel through a fake `ctx.ui.custom` and captures the returned mounted panel object.
- Seeds compact file-backed team state, mutates semantic task data, calls mounted `panel.invalidate()`, and records real `tui.requestRender()` calls.
- Separately verifies manual direct `r` refresh behavior and no-op `r` refresh cache-hit/no-render behavior.
- Keeps sanitized timing JSON under `/tmp` and checks that no raw terminal logs, screenshots, state archives, full mailbox/report bodies, worker transcripts, secrets, or raw hosted records are emitted.
- Restores the original `PI_AGENTTEAM_HOME` and `PI_AGENTTEAM_PROFILE` after the run and removes the temp home unless `--keep-home` is explicitly used.

## Coverage

Fixture profile:

| Profile | Shape | Warmup | Measured | Changes per burst | Debounce | Settle | Gate threshold |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `mounted-attached-team-panel-semantic-burst` | Clean temp attached `/team` panel; compact task data mutates, mounted `invalidate()` fires repeatedly, and harness counts actual `tui.requestRender()` calls. | `1` | `5` | `8` | `250ms` | `320ms` | `<= 4 renders/sec` |

Covered panel behavior:

- Mounted `/team` attached panel input/layout path through `openTeamPanel`.
- Semantic data-change invalidation via mounted `panel.invalidate()`.
- Existing `ui.teamPanel.minRefreshMs` default debounce setting.
- Manual direct `r` refresh remains immediate and still renders when data changed.
- Manual no-op `r` refresh remains a cache hit and does not request render.
- Compact projection/read-model behavior without full mailbox/report body reads.

## Gate Status

| Gate | Status | Metric | Threshold | Evidence |
| --- | --- | --- | --- | --- |
| `data-change-render-debounce-rate` | `pass` | `teamPanel.semanticDataChange.renderRequestsPerSecond.p95` observed `3.021 renders/sec` over `5` measured semantic bursts | `<= 4 renders/sec` | `/tmp/pi-agentteam-v0642-data-change-render-debounce-latest.json`, SHA-256 `1379175288f03d13c0c6b801485afb47859285b08ff730b1c55423d3aa2fb9a3` |
| `manual-direct-r-refresh` | `pass` | Changed-data `r` refresh requested `1` render and recorded `1` diff | Preserve direct refresh behavior | Sanitized harness summary |
| `manual-noop-r-refresh` | `pass` | No-op `r` refresh requested `0` renders and recorded `1` cache hit | Preserve no-op refresh behavior | Sanitized harness summary |
| `spawn-bookkeeping-p95` | `not-covered-by-this-slice` | Spawn app-owned bookkeeping timing required. | `<= 100ms` | Not collected here. |
| `manual-rc-smoke` | `not-covered-by-this-slice` | Real operator RC observation is outside this p95 harness. | Manual checklist evidence | Not executed here. |

Baseline before the runtime fix: mounted panel `invalidate()` was a no-op and render scheduling only coalesced same-tick requests; semantic data-change invalidations did not have configured `ui.teamPanel.minRefreshMs` debounce coverage.

## Runtime Optimization

`teamPanel.ts` now routes mounted semantic invalidations through the existing refresh path with the configured `ui.teamPanel.minRefreshMs` debounce. Manual `r` refresh still calls immediate refresh and cancels any pending semantic debounce.

This is intentionally narrow:

- Panel data loading, compact projection/full-text boundaries, manual direct `r` refresh, no-op cache-hit refresh behavior, and rendering layout are preserved.
- Task/report/message governance, report-only semantics, and mailbox/report full-text boundaries are not touched.
- No package, Go, native, default resolver, fallback deletion, tag, release, or publish behavior changes are included.

## No-Leak Check

No raw terminal logs, screenshots, raw state archives, raw full mailbox/report bodies, worker transcripts, secrets, raw hosted records, or raw timing JSON are checked in.

The guard searches for and preserves the following no-leak policy markers in controlled fixture/script/doc files only:

- `V0642_DATA_CHANGE_RENDER_DEBOUNCE_FULL_TEXT_SENTINEL_DO_NOT_LEAK`
- `MailboxMessage.text`
- `TaskReport.text`
- `worker transcript`
- `terminal raw log`
- `screenshot`
- `state archive`
- `raw state archive`
- `BEGIN PRIVATE KEY`
- `raw hosted record`

Raw local timing output is a sanitized `/tmp` artifact. It records render request counts, compact profile counters, threshold summaries, and no-leak/governance booleans; it does not record raw state bodies, screenshots, or terminal logs.

## TypeScript/pi Facade Authority

TypeScript/pi facade authority is preserved:

- `package.json` remains version `0.6.8`.
- `package.json#pi.extensions` remains `['./index.ts']`.
- The runtime optimization is limited to mounted team panel semantic invalidation scheduling in the TypeScript UI path.
- No Go runtime, default-Go/native/package/fallback deletion work is approved or implemented.

## Validation

Focused validation for this evidence checkpoint:

- `node --check scripts/lib/v0642-data-change-render-debounce-harness.cjs`.
- `node --check scripts/verify-v0642-data-change-render-debounce.cjs`.
- `node --check tests/fixtures/kernel/v0642/dataChangeRenderDebounce.cjs`.
- `node --check tests/suites/go-kernel-v0642-data-change-render-debounce.cjs`.
- Direct require-based invocation of `tests/suites/go-kernel-v0642-data-change-render-debounce.cjs` with `helpers.extRoot=process.cwd()`.
- Focused harness: `PI_AGENTTEAM_PROFILE=1 node scripts/verify-v0642-data-change-render-debounce.cjs --out /tmp/pi-agentteam-v0642-data-change-render-debounce-latest.json`.
- Broad regression: `npm test`.
- TypeScript: `npm run typecheck`.
- Boundary guard: `npm run -s check:boundaries`.
- Whitespace: `git diff --check`.

## Final Recommendation

Do not claim v0.7 release readiness from this artifact. Use it only as the v0.6.42 focused data-change render debounce p95 coverage and minimal panel debounce evidence slice. Remaining release-readiness gaps still include spawn bookkeeping p95, manual RC/release governance, and any separately authorized release/tag/package/default-Go/native decisions.
