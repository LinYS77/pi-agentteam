# v0.6.61 Go resolvePaneBindingAsync Facade Cutover

Result: v0.6.61 cuts over the TypeScript `resolvePaneBindingAsync(paneId, signal)` facade/default path to the existing Go-backed `workerLifecycle.inspectPane` operation through a cancellable async kernel adapter seam.

This slice closes the deferred async binding gap from v0.6.59/v0.6.60 without wrapping the synchronous `resolvePaneBinding()` facade:

```text
tmux/core.ts resolvePaneBindingAsync(paneId, signal)
  -> createAgentTeamKernelAdapter().inspectWorkerPaneAsync(paneId, signal)
  -> workerLifecycle.inspectPane
  -> { paneId, target } | null
```

## Runtime Behavior

- `tmux/core.ts` `resolvePaneBindingAsync(paneId, signal)` now delegates to `createAgentTeamKernelAdapter().inspectWorkerPaneAsync(paneId, signal)`.
- The TypeScript `runTmuxNoThrowAsync(['display-message', '-p', '-t', paneId, ...], undefined, signal)` fallback calls are removed from `resolvePaneBindingAsync()`.
- `core/kernel.ts` adds a cancellable async helper seam using `spawn` with stdin JSON-RPC, bounded stdout collection, timeout handling, and `AbortSignal` propagation.
- The async helper process forwards only `PATH`, `TMUX`, and `TMUX_PANE`, matching the synchronous helper environment boundary.
- Successful adapter results map to `{ paneId, target }` when Go `workerLifecycle.inspectPane` returns compact `target`.
- Empty pane id, helper unavailable/failure, pane-not-found, unsafe response shape, missing target, pre-aborted signal, and in-flight abort all fail closed to `null` at the public facade.
- Abort policy: pre-aborted and in-flight aborted `AbortSignal` resolve `null` at `resolvePaneBindingAsync()` and record only compact `helper-spawn-error`/`aborted` diagnostics inside the adapter; they do not throw or leak process output.

## Preserved Boundaries

- `resolvePaneBinding()` remains the synchronous Go-backed inspect adapter facade.
- `targetForPaneId()` remains `resolvePaneBinding(paneId)?.target ?? null` from v0.6.59.
- `captureCurrentPaneBinding()` remains the v0.6.60 Go-backed current-pane operation and keeps its `isInsideTmux()` guard.
- `inspectPane()`, `paneExists()`, and `listAgentTeamPanes()` remain Go-backed as before.
- `windowExists()` and `firstPaneInWindow()` are cut over separately by v0.6.62 through a cancellable async Go `listPanesInWindow` seam.
- Mutating worker lifecycle remains TypeScript-owned: no `wakePane`, `syncPaneLabels`, `createTeammatePane`, `killPane`, `clearPaneLabel`, send-keys, label mutation, split-window, new-window, kill-pane, select-pane, or respawn-pane migration.
- State repository, task/report/PlanRun governance, team panel view-model, release/package verification, and package/native ownership remain unmigrated.
- Go `display-message` remains limited to the existing v0.6.60 no-target current-pane binding operation; no target-based `display-message` is added to Go.
- No Go source or native helper rebuild is required for this TypeScript adapter-seam cutover.

## Package And Release Guards

No package/release action is authorized in this slice:

- no `npm version`.
- no `npm publish`.
- no tag or GitHub release.
- no package lockfiles.
- no `go.mod` or `go.sum`.
- no lifecycle hooks, postinstall downloads, or runtime `go build`.
- no native artifact path or binary rename.

`package.json` remains `0.6.8`.

## Evidence

- `docs/perf/v0.6.61-go-async-pane-binding-facade-cutover.md`
- `tests/fixtures/kernel/v0661/goAsyncPaneBindingFacadeCutover.cjs`
- `tests/suites/go-kernel-v0661-go-async-pane-binding-facade-cutover.cjs`

## Validation Scope

The v0.6.61 guard suite verifies:

- `resolvePaneBindingAsync(paneId, signal)` delegates to the cancellable async Go inspect adapter seam.
- the direct TypeScript async `display-message` fallback is removed from `resolvePaneBindingAsync()`.
- success returns `{ paneId, target }`.
- empty pane id, helper failure, missing target, invalid response, pre-aborted signal, and in-flight abort return `null`.
- async helper diagnostics remain compact and do not leak raw stdout, stderr, cwd, stack traces, state archives, mailbox/report bodies, worker transcripts, or full helper paths.
- sync facades remain unchanged and Go-backed.
- window helpers are cut over separately by v0.6.62 through a cancellable async Go `listPanesInWindow` seam.
- Go/native helper files remain untouched by this slice.
- package version, native path/name, and release surfaces remain unchanged.
