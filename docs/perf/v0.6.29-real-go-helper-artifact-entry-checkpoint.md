# v0.6.29 Real Go Helper Artifact Entry Checkpoint

> Scope: final v0.6.29 checkpoint for the real local/reviewer-controlled Go helper artifact entry. This is checkpoint docs/tests/guard consolidation only. It summarizes the implemented builder, real artifact validation/smoke, pure explicit manifest resolver, explicit `go-packaged-preview` integration, real clean-install preview smoke, remaining blockers, and unchanged npm/package/default/fallback/readiness boundaries.

## Prior Checkpoint

- v0.6.28 entry decision: `docs/perf/v0.6.28-final-prep-and-v0.6.29-entry.md`

## v0.6.29 Artifacts and Guards

- Slice 1 builder utility: `scripts/lib/go-helper-artifact-builder.cjs`
- Slice 1 builder CLI: `scripts/build-go-helper-artifact.cjs`
- Slice 1 focused suite: `tests/suites/go-kernel-v0629-helper-artifact-builder.cjs`
- Slice 2 real artifact validation/smoke suite: `tests/suites/go-kernel-v0629-real-helper-artifact-build.cjs`
- Slice 3 pure explicit manifest resolver: `core/kernelPackagedResolver.ts`
- Slice 3 focused suite: `tests/suites/go-kernel-v0629-packaged-manifest-resolver.cjs`
- Slice 4 explicit preview integration: `core/kernel.ts`
- Slice 4 focused suite: `tests/suites/go-kernel-v0629-packaged-preview-manifest-integration.cjs`
- Slice 5 real clean-install preview smoke suite: `tests/suites/go-kernel-v0629-real-artifact-clean-install-preview.cjs`
- Slice 6 checkpoint guard: `tests/suites/go-kernel-v0629-real-implementation-checkpoint-docs.cjs`

## Real Implementation Evidence

v0.6.29 proves a bounded real implementation entry for `tmuxSnapshotParse` preview only:

- Real local/reviewer-controlled helper artifact builder exists in `scripts/lib/go-helper-artifact-builder.cjs`, with the reviewer CLI entry `scripts/build-go-helper-artifact.cjs`.
- Real host-platform `GO111MODULE=off go build` evidence is produced only by the approved builder utility and v0629 tests, with output confined to OS temp roots or ignored `.agentteam-artifacts/`.
- Real helper binary metadata is generated and validated: `manifest.json`, `SHA256SUMS`, `provenance.json`, `LICENSE`, `license.json`, and placeholder-only `attestation.intoto.jsonl`.
- Real JSON-RPC smoke covers `health` and `tmuxSnapshotParse` against the generated helper binary.
- Pure explicit packaged manifest resolver exists in `core/kernelPackagedResolver.ts`; it validates package-relative manifest, helper, checksum, provenance, license, attestation, helper version, protocol version, package version, capability, platform, executable bit, and compact failure diagnostics without spawning helpers or changing state.
- Explicit `go-packaged-preview` manifest/root integration exists in `core/kernel.ts` only when explicit `packagedHelperInstallRoot` plus `packagedHelperManifestPath`, or `PI_AGENTTEAM_KERNEL_PACKAGED_HELPER_ROOT` plus `PI_AGENTTEAM_KERNEL_PACKAGED_HELPER_MANIFEST`, is supplied.
- Real clean-install preview smoke exists in `tests/suites/go-kernel-v0629-real-artifact-clean-install-preview.cjs`: real artifact → temp installed layout → explicit manifest resolver → adapter `go-packaged-preview` parse.

## `go build` Allowance

`go build` is now allowed only in approved reviewer/CI utility and v0629 test evidence paths:

- `scripts/build-go-helper-artifact.cjs`
- `scripts/lib/go-helper-artifact-builder.cjs`
- `tests/suites/go-kernel-v0629-helper-artifact-builder.cjs`
- `tests/suites/go-kernel-v0629-real-helper-artifact-build.cjs`
- `tests/suites/go-kernel-v0629-real-artifact-clean-install-preview.cjs`

`go build` remains forbidden in npm lifecycle hooks, package install, runtime resolver/default paths, package scripts, default user paths, and non-approved source areas.

## Unchanged Boundaries

v0.6.29 does not approve package/native/default/fallback/readiness expansion:

- `package.json` remains `0.6.8`; no npm version/publish approval.
- No package metadata, package files, `optionalDependencies`, scripts, lifecycle hooks, postinstall, download, or install-time build path is added.
- No `go.mod`, `go.sum`, npm lockfile, or shrinkwrap is added.
- No checked-in native binaries, generated manifests, checksums, provenance, attestations, tarballs, or artifact bundles are added.
- No CI workflow, upload, release asset, npm companion package, or main package native inclusion is added.
- No default Go and no default resolver are enabled.
- Default/unset, `disabled`, `typescript`, `go`, `auto`, and current `go-cutover` behavior remains unchanged.
- TypeScript fallback is not deleted.
- `compactReadModelFingerprint` remains TypeScript fallback / non-cutover for cutover and packaged-preview paths.
- Go authority remains bounded to parser-only `tmuxSnapshotParse` preview path behind the TypeScript adapter/ports.
- Go does not own tmux execution/capture.
- Go does not own state writes.
- Go does not own worker lifecycle.
- Go does not own task/report governance.
- Go does not own PlanRun.
- Go does not own full-text boundaries.
- Go does not own package/release authority.
- Go does not own UI rendering.
- Go does not own command control plane.
- `/team readiness` is not expanded.

## Non-Approval Statement

v0.6.29 does not prove normal-user native availability.

v0.6.29 does not prove real package-manager install availability.

v0.6.29 does not approve default Go, default resolver activation, package metadata, npm companion packages, main package native inclusion, CI artifact upload/storage, release assets, npm publication, TypeScript fallback deletion, `compactReadModelFingerprint` cutover, broad Go authority, or `/team readiness` expansion.

## Remaining Blockers

Before normal-user native/default/package/fallback work, later versions still need:

- cross-platform matrix evidence beyond the current host-platform build.
- real package-manager install proof beyond temp installed layout preview.
- normal-user native availability proof.
- CI artifact storage and retention approval.
- package metadata, release ownership, and npm/package inclusion approval.
- default resolver/default Go proof and explicit approval.
- fallback deletion approval and rollback/default-disable execution plan.
- cross-platform unsupported-row policy finalization.

## Validation Matrix

v0.6.29 focused validation:

- `node --check tests/suites/go-kernel-v0629-helper-artifact-builder.cjs`
- `node --check tests/suites/go-kernel-v0629-real-helper-artifact-build.cjs`
- `node --check tests/suites/go-kernel-v0629-packaged-manifest-resolver.cjs`
- `node --check tests/suites/go-kernel-v0629-packaged-preview-manifest-integration.cjs`
- `node --check tests/suites/go-kernel-v0629-real-artifact-clean-install-preview.cjs`
- `node --check tests/suites/go-kernel-v0629-real-implementation-checkpoint-docs.cjs`
- focused v0629 suites for builder, real helper artifact build, packaged manifest resolver, packaged preview manifest integration, real artifact clean-install preview, and checkpoint docs.

Broader validation:

- `node tests/run.cjs`
- `git diff --check`
- package/native/runtime guardrail scans.
- no checked-in generated output scans.

## Final Checkpoint Recommendation

Proceed only with v0.6.29 real Go helper artifact entry checkpoint review after leader/user approval.

Do not run npm version/publish, change package version, change package metadata/files/optionalDependencies/scripts, add lifecycle hooks, add postinstall/download/install-time build, add lockfiles, add go.mod/go.sum, check in native binaries/tarballs/generated manifests/checksums/provenance/attestations/artifacts, add CI workflows/uploads/release assets, add npm companion packages, include native artifacts in the main package, activate default Go/default resolver, change default/disabled/typescript/go/auto/current `go-cutover`, delete TypeScript fallback, cut over `compactReadModelFingerprint`, broaden Go authority, expand `/team readiness`, implement package-manager install proof, implement normal-user native availability, or commit/tag/push in this slice.
