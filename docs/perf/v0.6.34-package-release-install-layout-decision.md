# v0.6.34 Package / Release Ownership and Install-Layout Decision Contract

> Scope: v0.6.34 Slice 1 docs/tests-only contract. v0.6.34 starts package/release ownership and install-layout decision shaping after the v0.6.33 Clean-Install Native Helper Consumption Prototype. This slice changes no production runtime, package metadata, default resolver, `go-cutover`, `go-packaged-preview`, readiness, release, signing, tag, or publishing behavior.

## Background References

- `docs/agentteam方案书.md` records the long-term direction: default Go plus TypeScript fallback deletion only after package, install, default, rollback, and security gates are complete.
- `docs/perf/v0.6.33-clean-install-native-helper-consumption-checkpoint.md` records the completed v0.6.33 temp clean-install baseline, verified installed-layout explicit preview consumption, fail-closed/no-leak negatives, package/runtime guardrails, and final checkpoint.
- v0.6.33 moved prerequisite evidence to approximately 45–50% while keeping normal-user native helper availability at 0%.

## Distance-to-Goal After v0.6.33

Current position after v0.6.33:

- Native/default prerequisite evidence is approximately 45–50% complete.
- The normal-user native helper availability claim remains 0%.
- Evidence now covers temp npm clean-install TypeScript/pi facade baseline, verified review artifact copied into temp installed-package layout, explicit `go-packaged-preview` helper consumption, fail-closed/no-leak negatives, and package/runtime guardrails.
- Evidence still does not cover real package-manager native delivery, package/release ownership, install source ownership, default resolver approval, release assets, signing/security ownership, rollback/default-disable policy, TypeScript fallback deletion approval, or platform support beyond `linux-x64-glibc`.

Expected position if v0.6.34 fully completes:

- Native/default prerequisite evidence can move to approximately 55–60%.
- Normal-user native helper availability remains 0%.
- v0.6.34 can shape package/release ownership, install-layout choices, default-disable/rollback responsibilities, and placeholder security policy, but it still does not ship a normal-user native helper path.
- Default Go and TypeScript fallback deletion remain blocked; v0.6.34 is prerequisite shaping only, not runtime cutover approval.

## Route Evaluation A–F

- Route A — package/release ownership plus install-layout decision docs/tests/fixtures: main route. It defines owners, responsibilities, install-layout decision criteria, and non-applied review fixtures without changing package/runtime behavior.
- Route B — real package metadata/native inclusion prototype: forbidden/deferred. v0.6.34 may only describe non-applied fixture/proposal shapes; it must not add `package.json` metadata, native package files, optional native dependencies, companion packages, lifecycle hooks, postinstall downloads, install-time builds, or package inclusion.
- Route C — second platform matrix: deferred. No macOS, Windows, arm64, musl, or additional platform support is claimed in v0.6.34.
- Route D — default resolver dry-run/default-disable plan: constrained docs/tests only. It may define future responsibilities and failure-policy expectations, but it must not implement production default resolver, package discovery, default Go, or `go-packaged-preview` default behavior.
- Route E — signing/security ownership plan: placeholder policy only. It may assign future decision responsibilities and required evidence types, but it must not claim real signing, cosign, SLSA, security attestation, release asset, or install source approval.
- Route F — hosted tag backlog: tag policy only. It must not locally query, trigger, fetch, or record hosted workflow state; no `gh`, token, network, raw hosted records, raw API payloads, or downloaded artifacts are allowed.

## Selected Route

The selected v0.6.34 route is Route A: package/release ownership plus install-layout decision docs/tests/fixtures.

Constrained support is allowed only from:

- Route B as non-applied package metadata/layout fixture/proposal text.
- Route D as docs/tests-only default-disable and future resolver responsibility planning.
- Route E as placeholder security/signing ownership policy with no real signing proof.
- Route F as tag/backlog policy text with no hosted workflow query, trigger, fetch, or raw record.

v0.6.34 should not jump directly to default resolver/default Go because the package/release owner, install source owner, rollback/default-disable owner, and security owner are not accepted. It should not jump directly to a second platform matrix because the first normal-user install path is still not owned or released. It should not jump directly to signing/security because there is no release asset/install source to sign, and no cosign/SLSA ownership has been approved.

## Scope and STOP Gates

v0.6.34 Slice 1 is a GitHub-only checkpoint contract for package/release ownership and install-layout decision shaping.

STOP gates:

- `package.json` remains `0.6.8`.
- No `npm version` and no `npm publish`.
- No package metadata, package files native entries, `optionalDependencies`, native dependencies, lifecycle hooks, `postinstall`, download, or install-time build.
- No `package-lock.json`, `npm-shrinkwrap.json`, `go.mod`, or `go.sum`.
- No native binaries, generated artifacts, tarballs, release assets, raw hosted records, raw API payloads, downloaded bundles, generated manifests, checksums, provenance, attestations, or verifier output.
- No default Go, default resolver, production package discovery, or package-manager native delivery.
- No current `go-cutover` semantic changes.
- No `go-packaged-preview` default behavior.
- No TypeScript fallback deletion and no hidden fallback rollback policy change.
- `compactReadModelFingerprint` remains TypeScript fallback / non-cutover.
- `/team readiness` is not expanded.
- No broad Go authority over tmux execution/capture, worker lifecycle, task/report governance, PlanRun, package/release, UI rendering, command control plane, full-text boundaries, state, mailbox, or report access.
- No tag, push, hosted workflow query, hosted workflow trigger, `gh`, token, or network action by a worker.

## No-Availability Wording

Allowed wording for v0.6.34 is limited to ownership decision contract, install-layout decision shaping, non-applied fixture/proposal, placeholder policy, review-only, GitHub-only, and prerequisite evidence.

Do not describe v0.6.34 as normal-user availability, package-manager native delivery, package/release approval, install source approval, release asset approval, default resolver approval, default Go approval, fallback deletion approval, signing approval, cosign proof, SLSA proof, or second-platform support.

The normal-user native helper availability claim remains 0% until real package-manager native delivery, install source ownership, package/release ownership, default resolver/default-disable ownership, rollback policy, signing/security ownership, platform policy, and user-facing availability approval are complete.

## Slice 2 — Distribution Option Matrix and Owner Responsibilities

Slice 2 documents future native helper distribution options and ownership responsibilities. Every option below is decision-only and requires future explicit leader/user approval before implementation.

| Option | Current v0.6.34 status | Package metadata owner | Artifact build owner | Checksum/provenance/license/attestation owner | Package naming/versioning owner | Platform matrix/support owner | Install behavior owner | Unsupported-platform policy owner | Rollback/deprecation owner | Security/signing signoff owner | Allowed user-facing claim / forbidden claim |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Main package inclusion | deferred — decision-only; requires future explicit leader/user approval before implementation | future package/release owner must approve any `package.json#files` native entries and main package size policy | future artifact build owner must produce reproducible helper outputs before inclusion | future artifact integrity owner must own checksums, provenance, license copy/metadata, and placeholder or real attestation policy | future package/release owner must align helper layout with main `pi-agentteam` versioning | future platform owner must define supported OS/arch/libc rows before inclusion | future install behavior owner must prove clean npm/pi install without lifecycle downloads or install-time build | future platform owner must define fail-closed unsupported-platform diagnostics | future release owner must define rollback/default-disable and deprecation handling | future security owner must sign off before any real signing/security claim | Allowed claim: proposed main-package layout candidate only. Forbidden claim: bundled native helper approval, implementation, release, or availability. |
| npm companion native package(s) | proposed — decision-only; requires future explicit leader/user approval before implementation | future package/release owner must define optional companion metadata without changing current main package metadata in this slice | future artifact build owner must produce per-platform package contents from verified build outputs | future artifact integrity owner must own checksums, provenance, license copy/metadata, and attestation handoff between packages | future package naming/versioning owner must define companion package names, semver coupling, and skew policy | future platform owner must define package-per-platform matrix and support policy | future install behavior owner must define optional install behavior without postinstall downloads or install-time native builds | future platform owner must define unsupported companion absence behavior | future release owner must define companion rollback, yanking, and deprecation rules | future security owner must sign off on package provenance and any signing flow | Allowed claim: proposed companion package strategy only. Forbidden claim: companion native package approval, publication, installation, release, or availability. |
| GitHub release asset(s) | deferred — decision-only; requires future explicit leader/user approval before implementation | future release owner must define how assets relate to npm package metadata without changing `package.json` in this slice | future artifact build owner must produce release asset bundles from reviewed build pipeline | future artifact integrity owner must own checksums, provenance, license metadata, and attestation publication | future release owner must define asset naming, versioning, retention, and skew policy | future platform owner must define which target assets exist and unsupported rows | future install behavior owner must define how runtime/package resolver may use assets only after a future install-source design approval | future platform owner must define no-asset/unsupported behavior | future release owner must define rollback, asset removal, and default-disable policy | future security owner must sign off on release asset signing/security before claims | Allowed claim: possible future release-asset distribution path only. Forbidden claim: release asset approval, upload, signing/trust, install-source approval, or availability. |
| Generated artifact package/bundle | proposed — decision-only; requires future explicit leader/user approval before implementation | future package/release owner must decide whether generated bundles stay CI/review-only or become package inputs | future artifact build owner must own deterministic generation from review artifact pipeline | future artifact integrity owner must own manifest, checksums, provenance, license, and attestation placeholder/upgrade rules | future artifact owner must define bundle naming/versioning and package/helper skew mapping | future platform owner must define generated target matrix and support expansion gates | future install behavior owner must define copy/verification behavior before any package/runtime use | future platform owner must define unsupported-platform fail-closed policy | future release owner must define bundle deprecation and rollback rules | future security owner must decide whether bundle integrity is enough or signing is required | Allowed claim: proposed generated bundle/package input shape only. Forbidden claim: generated bundle as released package artifact, install source, or normal-user availability proof. |
| Source-only / no native delivery continuation | decision-only fallback option; requires future explicit leader/user approval before implementation | current package owner keeps main package metadata unchanged unless a future slice changes it | no artifact build owner for normal-user delivery; reviewer/local source build remains optional evidence only | no shipped integrity owner beyond source/review docs; no release attestation claim | current package versioning remains `0.6.8` in this slice; future versioning remains separate | future platform owner records no normal-user native platform support | future install behavior owner keeps TypeScript/pi install path as current behavior | future platform owner records unsupported-platform policy as no native helper availability claim | future release owner uses no native rollback path because no native delivery ships | future security owner records no signing/cosign/SLSA claim for native delivery | Allowed claim: continued source-only/review-only posture. Forbidden claim: native helper delivery, availability, or default behavior. |

Recommendation / decision framing:

- Preferred future candidate for further design is npm companion native package(s) plus generated artifact package/bundle inputs, because it separates native platform contents from the TypeScript/pi facade while preserving package ownership and artifact integrity seams.
- This preference requires future explicit leader/user approval before implementation.
- v0.6.34 does not approve package metadata, companion package publication, release assets, install source behavior, or normal-user availability.
- Route C platform expansion remains deferred until an owned distribution path exists.
- Route B real implementation remains forbidden/deferred; Slice 2 records decision criteria only, not applied fixtures or production package changes.

Slice 2 STOP reinforcement:

- Main `package.json` remains unchanged with version `0.6.8`.
- No `optionalDependencies`, native dependencies, package files, scripts, lifecycle hooks, `postinstall`, download, or install-time build are added.
- No `npm version`, `npm publish`, or `npm pack` for release is approved.
- No native binaries, generated artifacts, tarballs, release assets, raw hosted records, raw API payloads, or downloaded bundles are checked in.
- No default resolver, default Go, fallback deletion, signing, cosign, or SLSA approval is granted.

Slice 2 guard: `tests/suites/go-kernel-v0634-distribution-option-matrix-docs.cjs` verifies this matrix, non-approved statuses, future approval wording, unchanged package/runtime/workflow invariants, and no checked-in native/generated/tarball/raw records.

## Slice 3 — Non-Applied Package Metadata / Layout Proposal Fixtures

Slice 3 adds test-only, non-applied proposal fixtures in `tests/fixtures/kernel/v0634/nonAppliedPackageLayoutProposals.cjs` plus the focused guard `tests/suites/go-kernel-v0634-non-applied-package-layout-fixtures.cjs`.

The fixture proposals model the five Slice 2 distribution options without applying them to the main package or runtime:

- main-package inclusion proposal.
- companion native package proposal.
- GitHub release asset proposal.
- generated artifact package/bundle proposal.
- source-only/no-native continuation proposal.

Every fixture must carry explicit `proposalOnly: true`, `nonApplied: true`, and `testOnly: true` markers. Every fixture status must remain non-approved (`proposed`, `decision-only`, `deferred`, or equivalent) and every fixture must state that it requires future explicit leader/user approval before implementation.

Fixtures may describe hypothetical package metadata or install layouts only as inert object/string values inside the fixture module. They must not write to the main `package.json`, package files, native metadata, optional dependencies, lifecycle hooks, generated artifact paths, release asset paths, or production resolver inputs.

Slice 3 is not package/release approval. It does not approve package metadata, companion packages, release assets, install source behavior, normal-user availability, default resolver, default Go, TypeScript fallback deletion, signing, cosign, SLSA, or second platform support.

Slice 3 guard requirements:

- all five fixture proposals exist and are proposal-only, non-applied, test-only, non-approved, and future-approval-gated.
- each fixture includes allowed claims and forbidden claims; forbidden claims must cover availability, release, install source, default, signing, and fallback deletion overclaims.
- production sources must not import or read the fixture module or proposal paths.
- main `package.json` remains `0.6.8` with no native metadata, package files, optional native dependencies, native deps, scripts, lifecycle hooks, or unsafe package behavior.
- repository/package surface contains no package locks, `go.mod`, `go.sum`, native binaries, tarballs, generated manifests/provenance/checksums/attestations/verifier output, release assets, hosted records, raw API payloads, or downloaded bundles.
- runtime invariants remain unchanged: `go-packaged-preview` explicit-only, no package discovery/default resolver, and `compactReadModelFingerprint` remains TypeScript fallback / non-cutover.

Slice 3 validation:

- `node --check tests/fixtures/kernel/v0634/nonAppliedPackageLayoutProposals.cjs`.
- `node --check tests/suites/go-kernel-v0634-non-applied-package-layout-fixtures.cjs`.
- direct focused guard suite.
- `git diff --check`.

Do not start Slice 4 install layout resolver contract in Slice 3. Do not implement production resolver/default behavior. Do not edit `package.json`.

## Slice 4 — Future Install Layout Resolver Contract

Slice 4 defines a future package-relative native helper install layout and resolver contract. It is docs/tests only and does not implement production resolver/default discovery.

Future install layout shape:

- Main package proposal root: the package root for `pi-agentteam`, with native helper files under package-relative paths only.
- Companion package proposal root: the package root for a future native companion package, with the same package-relative native helper layout inside that companion package.
- Module path: `native/tmuxSnapshotParse/<helper-version>/<target>/...`.
- Manifest path: `native/tmuxSnapshotParse/<helper-version>/<target>/manifest.json`.
- Helper binary path/name expectation: helper executable lives under `native/tmuxSnapshotParse/<helper-version>/<target>/`; platform-specific executable naming is allowed only as a future owner decision, for example `agentteam-tmuxSnapshotParse` or a platform-suffixed equivalent.
- Required file/categories: manifest, helper executable, checksums, provenance, license copy or license metadata, and placeholder attestation metadata.
- All paths must be package-relative, normalized with `/`, non-empty, non-absolute, no `..`, no backslash, and resolved inside the selected package root.

Target mapping contract:

- Target components are `os`, `arch`, and Linux `libc` when `os=linux`.
- Current review-only row is `linux-x64-glibc`.
- Unsupported rows remain unsupported and carry no availability claim.
- No second platform support claim is made in v0.6.34.
- Route C platform expansion remains deferred until a separately approved package/release/install-source owner accepts the platform matrix.

Version/protocol/capability contract:

- Module is `tmuxSnapshotParse` only.
- Current helper version expectation is `0.3.0-read-model-shadow` unless a future owner-approved version contract changes it.
- Current protocol version expectation is `1`.
- Required capabilities are `health`, `profile`, `tmuxSnapshotParse`, `tmuxSnapshotCapture`, and `compactReadModelFingerprint`, with `businessPathsConnected: false`.
- Package/helper skew policy remains a future package/release owner decision; v0.6.34 does not approve skew tolerance or automatic upgrade/downgrade behavior.

Future resolver precedence contract:

- Explicit helper path remains first precedence.
- Explicit `go-packaged-preview` package-root/manifest injection remains the current preview path and is second precedence after explicit helper path when no explicit helper is supplied.
- A future package resolver may run only after separate approval; it must not be inferred from default runtime state in v0.6.34.
- Default/unset, `disabled`, `typescript`, `go`, `auto`, and current `go-cutover` behavior remain unchanged in v0.6.34.
- `go-packaged-preview` remains explicit-only and non-default.

Failure policy contract:

- Missing helper, unsupported platform, path-unsafe layout, integrity mismatch, missing provenance, missing license/license metadata, invalid attestation placeholder, package/helper version skew, protocol skew, capability skew, non-executable helper, and helper smoke failure must fail closed with compact diagnostics.
- Diagnostics must not leak absolute package roots, repo cwd, helper paths, raw stdout/stderr, raw manifest/provenance/license/attestation bodies, stacks, hosted records, raw API payloads, signing material, cosign claims, or SLSA claims.
- Explicit cutover/preview failures must not hide behind a successful TypeScript `tmuxSnapshotParse` parser fallback.
- `compactReadModelFingerprint` remains TypeScript fallback / non-cutover.

Slice 4 STOP language:

- This contract is a future proposal only.
- It is not production resolver implementation.
- It is not default resolver approval.
- It is not default Go approval.
- It is not package-manager native delivery proof.
- It is not normal-user native helper availability.
- It is not release asset, install source, signing, cosign, SLSA, fallback deletion, or second-platform approval.

Slice 4 guard: `tests/suites/go-kernel-v0634-install-layout-contract.cjs` verifies this contract, no overclaims, preview-gated runtime source invariants, helper precedence, current `go-cutover` behavior, fingerprint TypeScript fallback, no release/npm/download/signing/hosted behavior in runtime/resolver source, and unchanged package/repo artifact invariants.

Slice 4 validation:

- `node --check tests/suites/go-kernel-v0634-install-layout-contract.cjs`.
- direct focused guard suite.
- `git diff --check`.

Do not start Slice 5 rollback/default-disable policy in Slice 4. Do not implement default resolver dry-run or runtime code. Do not modify `package.json`.

## Slice 5 — Rollback / Default-Disable Ownership and Fallback-Deletion Preconditions

Slice 5 defines rollback/default-disable ownership, failure scenarios, and fallback-deletion preconditions. It is docs/tests only and does not implement a runtime default-disable flag, default resolver, default Go, fallback deletion, hidden fallback rollback, or `compactReadModelFingerprint` cutover.

Ownership roles remain future owner / unassigned until leader decision, but the responsibility categories are explicit:

- Package/release owner: owns package release decision, tag/package sequencing, package deprecation/yank policy, npm publish/version gates, and package-surface rollback coordination.
- Install source owner: owns approved install source semantics, package-manager delivery route, install layout source of truth, and install behavior change control.
- Artifact/verifier owner: owns helper artifact integrity, verifier rules, checksum/provenance/license/attestation validation, and no-leak verifier diagnostics.
- Runtime/default resolver owner: owns any future default resolver/default Go design, resolver precedence, fail-closed behavior, and runtime compatibility gates.
- Rollback/default-disable owner: owns default-disable switch design, rollback playbooks, emergency disable procedures, and verification that rollback does not become hidden long-term TypeScript fallback.
- Security owner: owns security incident response, signing/security evidence requirements, cosign/SLSA decision boundaries, and any future security signoff.
- Platform owner: owns supported target matrix, unsupported-platform policy, platform-specific executable expectations, and platform deprecation.
- User-facing communication owner: owns release notes, migration warnings, deprecation notices, normal-user availability wording, and support guidance.

Failure scenarios that a future rollback/default-disable plan must cover:

- bad package metadata.
- bad helper artifact.
- checksum/provenance mismatch.
- unsupported platform.
- broken diagnostics/no-leak violation.
- package unpublish/deprecation/yank.
- security incident.
- default resolver disablement.
- release rollback.
- package/helper skew.
- helper protocol/capability mismatch.

Rollback/default-disable policy:

- Rollback after cutover must be a release/tag/package/deprecation/default-disable policy, not hidden long-term TypeScript fallback.
- Hidden runtime TypeScript fallback rollback remains unapproved.
- Current explicit `go-packaged-preview` and `go-cutover` failure behavior remains fail-closed.
- Default-disable implementation is future work, not v0.6.34.
- Any future default-disable behavior must be explicitly designed, tested, documented, and approved before default Go or fallback deletion.
- Runtime rollback must not silently reinterpret failed native helper execution as successful pane disappearance or successful TypeScript parser output.

Fallback-deletion preconditions remain blocked until all are complete:

- normal-user native availability proven.
- package/release/install ownership approved.
- package-manager native delivery approved/proven.
- default resolver/default Go approved and observed.
- unsupported-platform policy approved.
- diagnostics/no-leak proven.
- rollback/default-disable policy implemented/tested.
- security/signing ownership approved.
- platform matrix/support policy approved.
- leader/user explicit approval.
- at least one checkpoint after default Go approval.
- `compactReadModelFingerprint` is not included; it remains TypeScript fallback / non-cutover unless separately approved.

Slice 5 STOP language:

- No default Go approval.
- No default resolver approval.
- No TypeScript fallback deletion approval.
- No hidden fallback rollback approval.
- No runtime behavior change.
- No `compactReadModelFingerprint` cutover approval.
- No tag, release, npm version, npm publish, package metadata, package files, native deps, lifecycle hooks, release asset, signing, cosign, SLSA, readiness/UI/tool expansion, or hosted workflow action.

Slice 5 guard: `tests/suites/go-kernel-v0634-rollback-default-disable-policy-docs.cjs` verifies ownership roles, failure scenarios, rollback/default-disable policy, fallback-deletion preconditions, STOP language, no overclaims, current kernel fail-closed/fingerprint fallback source invariants, unchanged package/repo artifact invariants, and no readiness/tool/control-plane expansion.

Slice 5 validation:

- `node --check tests/suites/go-kernel-v0634-rollback-default-disable-policy-docs.cjs`.
- direct focused guard suite.
- `git diff --check`.

Do not start Slice 6 security/signing ownership in Slice 5. Do not implement runtime default-disable or default resolver code.

## Slice 6 — Security / Signing Ownership Placeholder Policy and Evidence Boundaries

Slice 6 defines future security/signing ownership and evidence boundaries. It is docs/tests only and does not create real signing, cosign, SLSA, security attestation, release asset, install source, generated signature, generated attestation, release bundle, native artifact, hosted record, or verifier output.

Security/signing ownership roles remain future owner / unassigned until explicit leader/user decision:

- Security owner: owns security acceptance criteria, security signoff policy, vulnerability review, and whether any future signing/security claim is allowed.
- Release owner: owns release asset boundaries, release notes, retention policy, tag/package sequencing, and release rollback coordination.
- Artifact/verifier owner: owns checksum, provenance, license metadata, verifier behavior, compact diagnostics, and no-leak verification.
- Package/install source owner: owns install source approval, package delivery path, package metadata safety, and package-manager install evidence requirements.
- Platform owner: owns supported-platform commitments, unsupported-platform policy, and platform-specific security expectations.
- Incident response owner: owns revocation, rotation, yanking/deprecation, emergency disablement, and security incident communication.
- Key/credential owner: future/unassigned until explicit decision; owns any future signing key, token, credential, KMS, cosign identity, SLSA identity, rotation, and access-control policy.

Required future evidence categories before any real security/signing claim:

- checksum evidence.
- provenance evidence.
- source revision evidence.
- build context evidence.
- license metadata evidence.
- attestation placeholder vs real attestation distinction.
- retention policy evidence.
- artifact/package naming evidence.
- verifier behavior evidence.
- supported-platform commitments.
- key/signature/cosign/SLSA decision if later approved.
- revocation/rotation/incident response evidence.

Placeholder boundaries:

- Existing attestation/signing fields are placeholder/non-real unless a later approved slice provides proof.
- Current artifacts and checkpoint docs are review-only and not signed availability.
- v0.6.34 does not approve signing, cosign, SLSA, or security attestation.
- No security claim can be used to justify default Go, default resolver, package delivery, release asset, fallback deletion, or normal-user availability.
- Placeholder attestation metadata is allowed only as review/test contract language; it is not a signature, not a trust root, not a release attestation, and not user availability evidence.

Evidence/no-leak policy:

- No raw hosted records, raw API payloads, raw verifier JSON, downloaded bundles, generated signatures, generated attestations, release bundles, native artifacts, or verifier output may be checked in.
- Future security diagnostics must be compact/no-leak.
- Future diagnostics must not leak private key material, tokens, credentials, signing payload bodies, raw provenance, raw attestation bodies, helper stdout/stderr, absolute package roots, repo cwd, mailbox/report text, hosted payloads, or stack traces.
- Hosted workflow state must not be queried, triggered, fetched, or recorded by workers in this slice.

Slice 6 STOP language:

- No real signing approval.
- No cosign approval.
- No SLSA approval.
- No security attestation approval.
- No signed availability claim.
- No release asset approval.
- No install source approval.
- No package metadata change.
- No runtime/default resolver/default Go behavior change.
- No `go-cutover` or `go-packaged-preview` behavior change.
- No readiness/UI/tool/runtime diagnostics expansion.
- No workflow permission expansion such as `id-token: write`, `packages: write`, or `contents: write`.
- No `gh attestation`, `gh release`, npm publish/version, curl/wget, postinstall/preinstall, generated signature, generated attestation, release bundle, native artifact, hosted fetch, token, network, commit, tag, or push.

Slice 6 guard: `tests/suites/go-kernel-v0634-security-signing-ownership-docs.cjs` verifies ownership roles, evidence categories, placeholder boundaries, no-leak policy, STOP language, no signing/cosign/SLSA/security overclaims, unchanged review-only workflow permissions/behavior, no generated signature/attestation/release/native/raw records, and unchanged package/runtime/readiness boundaries.

Slice 6 validation:

- `node --check tests/suites/go-kernel-v0634-security-signing-ownership-docs.cjs`.
- direct focused guard suite.
- `git diff --check`.

Do not create or modify GitHub workflow signing behavior in Slice 6. Do not generate any signature or attestation file. Do not start Slice 7 final checkpoint.

## Slice 7 — Final Checkpoint Status

Slice 7 adds `docs/perf/v0.6.34-package-release-install-layout-decision-checkpoint.md` and `tests/suites/go-kernel-v0634-package-release-decision-checkpoint-docs.cjs` only.

The checkpoint summarizes Slice 1–6 evidence, validation matrix, GO/STOP decision, distance after v0.6.34, tag policy, remaining blockers, and unchanged package/runtime/default/readiness/workflow/release/signing boundaries.

If accepted, v0.6.34 moves prerequisite evidence from approximately 45–50% to approximately 55–60%, while the normal-user native helper availability claim remains 0%.

Slice 7 is checkpoint docs/tests only. It does not approve package release, install source, release asset, default Go, default resolver, TypeScript fallback deletion, hidden fallback rollback, `compactReadModelFingerprint` cutover, signing, cosign, SLSA, second platform support, hosted workflow query/fetch/trigger, npm version/publish, tag creation, package metadata, runtime behavior, readiness/UI/tool expansion, or broad Go authority.

## Planned v0.6.34 Slices

- Slice 1 — contract doc/guard.
- Slice 2 — distribution option matrix plus owner responsibilities.
- Slice 3 — non-applied package metadata/layout fixtures.
- Slice 4 — future install layout resolver contract.
- Slice 5 — rollback/default-disable ownership and fallback-deletion preconditions.
- Slice 6 — security/signing ownership placeholder policy.
- Slice 7 — final checkpoint/tag policy.

Each slice must preserve package/runtime/default/readiness/release/signing boundaries unless a later leader assignment explicitly changes the boundary. Blocked future slices must not be implemented early.

## Tag Policy

- v0.6.31, v0.6.32, and v0.6.33 tags remain gated unless exact hosted evidence or an explicit leader/user waiver is supplied.
- v0.6.34 may become a `main` commit after validation and review.
- A v0.6.34 tag should wait for prior gates or an explicit leader/user waiver.
- No worker should create, push, or move tags for v0.6.34.
- No worker should run `npm version`, `npm publish`, `git tag`, `git push`, `gh`, token-based commands, hosted workflow trigger/query, or network validation for v0.6.34 Slice 1.

## Guard Contract

The focused guard for this slice is `tests/suites/go-kernel-v0634-ownership-install-layout-contract-docs.cjs`.

It must verify:

- this doc exists and includes distance-to-goal after v0.6.33, references to `docs/agentteam方案书.md` and `docs/perf/v0.6.33-clean-install-native-helper-consumption-checkpoint.md`, Route A–F evaluation, selected Route A, constrained support routes, STOP gates, no-availability wording, planned slices, and tag policy.
- this doc does not overclaim normal-user availability, package-manager native delivery, package/release approval, install source approval, default Go, default resolver, TypeScript fallback deletion, signing, cosign, SLSA, or second-platform support.
- `.gitignore` allowlists this v0.6.34 doc.
- package/runtime/workflow invariants remain unchanged at a basic level: `package.json` stays `0.6.8`, no optional native deps/scripts/lockfiles/go modules are added, `go-packaged-preview` remains explicit-only, the review workflow remains one `linux-x64-glibc` row with no release/npm/signing behavior, and the repository contains no checked-in native/generated/tarball/raw records.

## Slice 1 Validation

Run:

- `node --check tests/suites/go-kernel-v0634-ownership-install-layout-contract-docs.cjs`.
- direct focused guard suite.
- `git diff --check`.

Do not run npm pack/install, build review artifact commands, hosted workflow queries, `gh`, token/network commands, commits, tags, or pushes in Slice 1.
