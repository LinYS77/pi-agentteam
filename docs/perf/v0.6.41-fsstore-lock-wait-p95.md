# v0.6.41 fsStore Lock-Wait p95 Evidence

> Scope: v0.6.41 focused p95 evidence for fsStore lock acquisition wait only. This uses a clean temporary `PI_AGENTTEAM_HOME` harness and does not execute manual RC, change package metadata, enable default-Go/native, tag, publish, or claim v0.7 release readiness.

## Result

Result: v0.6.41 adds focused fsStore lock-wait p95 coverage through a clean temp `PI_AGENTTEAM_HOME` harness and applies a minimal retry-granularity optimization.

Final result remains `ready:false`. The fsStore lock-wait gate passes in this focused run, but this slice does not re-execute manual RC or prove every release p95 gate.

This checkpoint is GO for reviewer inspection of the focused harness, clean-temp isolation contract, fsStore lock-wait timing evidence, no-leak guard, and preserved local `/tmp` sanitized timing artifact.

This checkpoint is STOP for release/tag/git push/npm version/npm publish/native/default-Go/package/signing/second-platform/fallback-deletion work, TypeScript/pi facade authority violations, raw state archive or full-text evidence check-in, manual RC pass claims, all-p95-gates claims, and claiming v0.7 release readiness.

STOP for release/tag/git push/npm version/npm publish/native/default-Go/package/signing/second-platform/fallback-deletion work.

## Harness

Focused command:

```bash
PI_AGENTTEAM_PROFILE=1 node scripts/verify-v0641-fsstore-lock-wait-p95.cjs --out /tmp/pi-agentteam-v0641-fsstore-lock-wait-p95-latest.json
```

The verifier calls `scripts/lib/v0641-fsstore-lock-wait-p95-harness.cjs`, which:

- Creates a clean temporary `PI_AGENTTEAM_HOME` under `/tmp/pi-agentteam-v0641-fsstore-lock-wait-p95.*` and rejects unsafe/non-empty homes by default.
- Transpiles only the fsStore/profiling modules needed for the fixture into a temporary build directory.
- Uses a child process to hold the real fsStore lock briefly, then measures parent `withFileLock` lock acquisition wait with `PI_AGENTTEAM_PROFILE=1`.
- Performs compact JSON write/read/parse work under the measured lock path so the evidence covers representative file-backed serialization without raw state archives.
- Keeps sanitized timing JSON under `/tmp` and checks that no raw state archives, full mailbox/report bodies, worker transcripts, screenshots, secrets, or raw hosted records are emitted.
- Restores the original `PI_AGENTTEAM_HOME` and `PI_AGENTTEAM_PROFILE` after the run and removes the temp home unless `--keep-home` is explicitly used.

## Coverage

Fixture profile:

| Profile | Shape | Warmup | Measured | Lock holder | Gate threshold |
| --- | --- | ---: | ---: | ---: | ---: |
| `contended-json-read-write` | Clean temp `PI_AGENTTEAM_HOME` with one compact JSON file; a child process holds the fsStore lock while the parent measures `withFileLock` `lockWaitMs` and performs compact read/write/parse work. | `5` | `30` | `8ms` | `<= 25ms` |

Covered fsStore behavior:

- `withFileLock` real lock acquisition wait using the same lock file path convention as production fsStore.
- `writeJsonFile`, `readJsonFile`, JSON parse, and profiling summary counters during measured iterations.
- Sanitized by-call-site summary and lock/read/write/parse counts without raw JSON state bodies.

## Gate Status

| Gate | Status | Metric | Threshold | Evidence |
| --- | --- | --- | --- | --- |
| `fsstore-lock-wait-p95` | `pass` | `fsStore.lockWaitMs.p95` observed `10.644ms` over `30` measured locks | `<= 25ms` | `/tmp/pi-agentteam-v0641-fsstore-lock-wait-p95-latest.json`, SHA-256 `481deab8087569babf1806541a423214a310b446b1c5e72dc0f7724a9a5f0968` |
| `data-change-render-debounce-rate` | `not-covered-by-this-slice` | Mounted `/team` semantic data-change render rate required. | `<= 4 renders/sec` | Not collected here. |
| `spawn-bookkeeping-p95` | `not-covered-by-this-slice` | Spawn bookkeeping segment timing required. | `<= 100ms` | Not collected here. |
| `manual-rc-smoke` | `not-covered-by-this-slice` | Real operator RC observation is outside this p95 harness. | Manual checklist evidence | Not executed here. |

Baseline before the runtime fix: `fsstore-lock-wait-p95` observed `25.343ms > 25ms`. The failure was consistent with the previous fixed `25ms` lock retry sleep granularity.

## Runtime Optimization

`state/fsStore.ts` reduces `LOCK_RETRY_MS` from `25` to `5`.

This is intentionally narrow:

- File lock acquisition, stale-lock cleanup, lock timeout, atomic temp-file write/rename behavior, and persisted JSON format are unchanged.
- Task/report/message governance, report-only semantics, and full-text boundaries are not touched.
- No package, Go, native, default resolver, fallback deletion, tag, release, or publish behavior changes are included.

## No-Leak Check

No raw state archives, raw full mailbox/report bodies, worker transcripts, screenshots, secrets, raw hosted records, or raw timing JSON are checked in.

The guard searches for and preserves the following no-leak policy markers in controlled fixture/script/doc files only:

- `V0641_FSSTORE_LOCK_WAIT_FULL_TEXT_SENTINEL_DO_NOT_LEAK`
- `MailboxMessage.text`
- `TaskReport.text`
- `worker transcript`
- `screenshot`
- `state archive`
- `raw state archive`
- `BEGIN PRIVATE KEY`
- `raw hosted record`

Raw local timing output is a sanitized `/tmp` artifact. It records lock wait durations, compact call-site hints, aggregate profile counts, threshold summaries, and no-leak/governance booleans; it does not record raw state bodies.

## TypeScript/pi Facade Authority

TypeScript/pi facade authority is preserved:

- `package.json` remains version `0.6.8`.
- `package.json#pi.extensions` remains `['./index.ts']`.
- The runtime optimization is limited to fsStore lock retry granularity in the TypeScript file-backed state path.
- No Go runtime, default-Go/native/package/fallback deletion work is approved or implemented.

## Validation

Focused validation for this evidence checkpoint:

- `node --check scripts/lib/v0641-fsstore-lock-wait-p95-harness.cjs`.
- `node --check scripts/verify-v0641-fsstore-lock-wait-p95.cjs`.
- `node --check tests/fixtures/kernel/v0641/fsStoreLockWaitP95.cjs`.
- `node --check tests/suites/go-kernel-v0641-fsstore-lock-wait-p95.cjs`.
- Direct require-based invocation of `tests/suites/go-kernel-v0641-fsstore-lock-wait-p95.cjs` with `helpers.extRoot=process.cwd()`.
- Focused harness: `PI_AGENTTEAM_PROFILE=1 node scripts/verify-v0641-fsstore-lock-wait-p95.cjs --out /tmp/pi-agentteam-v0641-fsstore-lock-wait-p95-latest.json`.
- Broad regression: `npm test`.
- TypeScript: `npm run typecheck`.
- Boundary guard: `npm run -s check:boundaries`.
- Whitespace: `git diff --check`.

## Final Recommendation

Do not claim v0.7 release readiness from this artifact. Use it only as the v0.6.41 focused fsStore lock-wait p95 coverage and retry-granularity optimization evidence slice. Remaining release-readiness gaps still include data-change render debounce rate, spawn bookkeeping p95, manual RC/release governance, and any separately authorized release/tag/package/default-Go/native decisions.
