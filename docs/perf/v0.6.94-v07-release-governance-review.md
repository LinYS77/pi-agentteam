# v0.6.94 v0.7 Release Checklist / Governance Review

Result: v0.6.94 records the v0.7 release checklist/governance posture after accepted p95, manual RC/operator-seam, bug burn-down, and T046 blocker-fix evidence.

Release target remains exactly: `v0.7.0 = core refactor + performance baseline + bug burn-down release`.

Slice status: `release-governance-reviewed-not-release-ready`.

governance gate status: `reviewed-no-release-action-authorized`.

Overall v0.7 status remains `not-release-ready` because evidence reconciliation and continued no raw artifact policy remain outstanding.

`package.json` remains `0.6.8`.

This is a docs/tests/readiness-governance slice, not release mechanics. No release/package action is authorized or performed.

## Required Governance Conclusions

- no `npm version`.
- no `npm publish`.
- no tag creation/push.
- no GitHub release/assets.
- no release asset upload.
- no signing/cosign/SLSA/security attestation.
- no package source approval.
- no package metadata/lifecycle hook/optional native dependency/native dependency/postinstall/download/runtime build flow.
- no release/package control-plane migration.
- current embedded helper path remains `native/tmuxSnapshotParse/0.3.0-read-model-shadow/linux-x64-glibc/`.
- no native helper path/binary rename.
- no second platform claim.
- no default resolver/default Go expansion beyond already accepted explicit cutovers.
- no hidden TypeScript tmux fallback reintroduction.
- worker delivery remains bridge-only.
- Go `send-keys` / active `wakePane` remain unauthorized.
- Mutating lifecycle/state/task/PlanRun/mailbox/governance/team panel/UI/release control-plane migrations remain deferred/blocked unless separately gated.

## True Interactive Operator Coverage Decision

True interactive pi/TUI/operator/model pass was not performed in v0.6.92. The accepted v0.6.92 evidence is deterministic clean-temp operator-seam coverage with stubbed pi/tmux and real registered `/team config` + `agentteam_*` seams.

Governance classification: true interactive pi/TUI/operator/model coverage remains a deferred/manual operator procedure and not a current blocker for this v0.7 readiness track. It can be promoted later by release ownership into a new clean-temp operator task, but this document does not claim that a true interactive pass was performed.

## Checklist Rows

| ID | Category | Status | Decision | Required next action |
| --- | --- | --- | --- | --- |
| `package-version-npm` | package-version-npm | `reviewed-held` | `package.json` remains `0.6.8`; no npm version, npm publish, package source approval, or package metadata/lifecycle/optional-native-dependency flow is authorized or performed. | release owner must authorize separately after evidence reconciliation if a package release is ever approved |
| `git-tag-github-release-assets` | git-release-assets | `reviewed-held` | No git tag creation/push, GitHub release, release asset upload, hosted release query, or release artifact creation is authorized or performed. | no tag/release action until a separate final release approval exists |
| `release-ownership-approval` | release-ownership-approval | `not-approved` | This governance review is not final release approval; v0.7 remains not-release-ready pending evidence reconciliation and continuous no-raw-artifact review. | complete evidence reconciliation and explicit release-owner approval before any release mechanics |
| `rollback-policy` | rollback | `reviewed-no-release-to-rollback` | Rollback posture is preserve current package/native state and avoid release mechanics; no published package/tag/asset rollback is needed because none is created. | if release approval is later granted, define explicit rollback owner and artifact rollback procedure before publishing |
| `native-helper-default-resolver-platform` | native-helper-default-resolver-platform | `reviewed-unchanged` | Embedded helper path remains unchanged; no native helper path/binary rename, second platform claim, default resolver/default Go expansion, native dependency flow, or hidden TypeScript tmux fallback is authorized. | keep existing native posture until separately gated |
| `security-signing-attestation` | security-signing-attestation | `reviewed-not-authorized` | No signing, cosign, SLSA/security attestation, signature generation, or release security artifact is authorized or performed in this slice. | if future release requires signing, create a separate signing/security gate |
| `true-interactive-operator-coverage` | operator-coverage | `deferred-manual-operator-procedure-not-current-blocker` | True interactive operator/model coverage was not performed; it remains deferred/manual and is not a current blocker for this readiness track unless release ownership promotes it. | do not claim true operator coverage; create a new clean-temp operator task only if release owner promotes it |
| `raw-artifact-policy` | raw-artifact-policy | `continuous` | Raw logs, validation output, transcripts, state archives, screenshots, temp homes, hosted records, raw JSON, tarballs, signatures, and release artifacts must stay out of repo. | continue enforcing through evidence reconciliation and any future release decision |
| `evidence-reconciliation` | remaining-gate | `outstanding` | evidence reconciliation remains outstanding and blocks any v0.7 release-ready decision. | create and complete evidence reconciliation gate |
| `bridge-only-worker-delivery` | delivery-boundary | `reviewed-preserved` | Worker delivery remains bridge-only; Go send-keys and active wakePane remain unauthorized and require separate design gates. | none for this readiness track |
| `deferred-runtime-migrations` | deferred-design-gates | `deferred-blocked` | Mutating lifecycle/state/task/PlanRun/mailbox/governance, team panel/UI, and release/package control-plane migrations remain deferred/blocked unless separately gated. | future explicit design gates only; no v0.7 release-governance action |

Every checklist row has `releaseActionAuthorized=false` and `releaseActionPerformed=false` in the machine-readable fixture.

## Accepted Prior Gates Referenced

- v0.6.91 clean-temp p95 refresh is accepted; evidence: `docs/perf/v0.6.91-v07-clean-temp-p95-refresh.md`.
- v0.6.92 deterministic manual RC/operator-seam refresh is accepted; evidence: `docs/perf/v0.6.92-v07-clean-temp-manual-rc-refresh.md`.
- v0.6.93 bug burn-down ledger is accepted; evidence: `docs/perf/v0.6.93-v07-bug-burndown-ledger.md`.
- T046 tools-state pane-health blocker fix is accepted; evidence: `tests/suites/tools-state.cjs` and commit `e423179 Isolate tools-state tmux snapshot fixture`.

## Remaining v0.7 Gates

This slice only addresses release checklist/governance posture. The following remain outstanding before any v0.7 decision:

- evidence reconciliation remains outstanding;
- continued no raw artifact policy remains in force.

## Package / Native Snapshot

- `package.json` remains `0.6.8`.
- Existing native helper root remains `native/tmuxSnapshotParse/0.3.0-read-model-shadow/linux-x64-glibc`.
- Existing native helper binary remains `agentteam-tmuxSnapshotParse`.
- Existing native helper platform remains the existing linux-x64-glibc artifact only; no second platform claim is made.
- No native rebuild, metadata refresh, helper path rename, binary rename, signing, package-manager native dependency flow, or runtime download/build flow is performed.

## Raw Artifact / No-release Policy

Do not check in raw logs, validation output, transcripts, state archives, screenshots, temp homes, raw hosted records, release artifacts, tarballs, signatures, or raw JSON.

No release commands or hosted workflow queries were run in this slice. No `npm version`, `npm publish`, git tag, GitHub release command, release asset upload, signing command, or network/hosted release query is part of this evidence.

## Evidence Files

- `tests/fixtures/kernel/v0694/v07ReleaseGovernanceReview.cjs`
- `tests/suites/go-kernel-v0694-v07-release-governance-review.cjs`

## Validation Plan

- `node --check tests/fixtures/kernel/v0694/v07ReleaseGovernanceReview.cjs`
- `node --check tests/suites/go-kernel-v0694-v07-release-governance-review.cjs`
- `node tests/run.cjs go-kernel-v0694-v07-release-governance-review`
- `node tests/run.cjs go-kernel-v0691-v07-clean-temp-p95-refresh go-kernel-v0692-v07-clean-temp-manual-rc-refresh go-kernel-v0693-v07-bug-burndown-ledger go-kernel-v0694-v07-release-governance-review tools-state`
- `npm test`
- `npm run typecheck`
- `npm run -s check:boundaries`
- `git diff --check`
- `node -p "require('./package.json').version"`
- `sha256sum -c native/tmuxSnapshotParse/0.3.0-read-model-shadow/linux-x64-glibc/SHA256SUMS`
- status/diff guard confirming only docs/tests/.gitignore/roadmap changes
