# v0.6.62 Go Window Pane Lookup Facade Cutover

Result: v0.6.62 cuts over the TypeScript `windowExists(target, signal)` and `firstPaneInWindow(target, signal)` facades to a narrow Go-backed `workerLifecycle.listPanesInWindow` operation through the cancellable async kernel adapter seam.

This slice closes the remaining read-only async window helper gap in `tmux/core.ts` without migrating any window creation, labels, or mutating lifecycle code:

```text
tmux/core.ts windowExists(target, signal)
  -> createAgentTeamKernelAdapter().listPanesInWindowAsync(target, signal)
  -> workerLifecycle.listPanesInWindow
  -> boolean

tmux/core.ts firstPaneInWindow(target, signal)
  -> createAgentTeamKernelAdapter().listPanesInWindowAsync(target, signal)
  -> workerLifecycle.listPanesInWindow
  -> first pane id | null
```

## Runtime Behavior

- `tmux/core.ts` `windowExists(target, signal)` now delegates to `createAgentTeamKernelAdapter().listPanesInWindowAsync(target, signal)` and returns `true` only when the Go-backed lookup succeeds.
- `tmux/core.ts` `firstPaneInWindow(target, signal)` now delegates to `createAgentTeamKernelAdapter().listPanesInWindowAsync(target, signal)` and returns the first compact pane id or `null`.
- The TypeScript `runTmuxNoThrowAsync(['list-panes', '-t', target, '-F', '#{pane_id}'], undefined, signal)` fallback calls are removed from both window helper facades.
- Go uses exactly `tmux list-panes -t <target> -F workerLifecycleWindowPaneFormat` with compact format `#{pane_id}` for `workerLifecycle.listPanesInWindow`.
- Target input is compactly sanitized before helper invocation; empty or unsafe targets fail closed without spawning the helper.
- Empty or unsafe target, helper unavailable/failure, command failure, unsafe response shape, empty pane list for `firstPaneInWindow()`, pre-aborted signal, and in-flight abort all fail closed at the public facades.
- Abort policy: pre-aborted and in-flight aborted `AbortSignal` resolve `false` for `windowExists()` and `null` for `firstPaneInWindow()` with only compact `helper-spawn-error`/`aborted` diagnostics inside the adapter.

## Preserved Boundaries

- `listAgentTeamPanes()` remains the separate label-filtered global `list-panes -a` operation and is not used for window lookup.
- `resolvePaneBindingAsync(paneId, signal)` remains the v0.6.61 Go-backed `inspectWorkerPaneAsync(paneId, signal)` facade.
- `captureCurrentPaneBinding()` remains the v0.6.60 Go-backed current-pane operation and keeps its `isInsideTmux()` guard.
- `inspectPane()`, `paneExists()`, `resolvePaneBinding()`, `targetForPaneId()`, and `listAgentTeamPanes()` remain Go-backed as before.
- Mutating worker lifecycle remains TypeScript-owned: no `wakePane`, `syncPaneLabels`, `createTeammatePane`, `killPane`, `clearPaneLabel`, send-keys, label mutation, split-window, new-window, kill-pane, select-pane, or respawn-pane migration.
- State repository, task/report/PlanRun governance, team panel view-model, release/package verification, and package/native ownership remain unmigrated.
- Go `display-message` remains limited to the existing no-target current-pane binding operation; no target-based `display-message` is added.
- Because Go source changes, the existing embedded helper is rebuilt in the same approved path with refreshed manifest, checksums, provenance, and placeholder attestation.

## Package And Release Guards

No package/release action is authorized in this slice:

- no `npm version`.
- no `npm publish`.
- no tag or GitHub release.
- no package lockfiles.
- no `go.mod` or `go.sum`.
- no lifecycle hooks, postinstall downloads, or runtime `go build`.
- no native artifact path or binary rename.

`package.json` remains `0.6.8`.

## Evidence

- `docs/perf/v0.6.62-go-window-pane-lookup-facade-cutover.md`
- `tests/fixtures/kernel/v0662/goWindowPaneLookupFacadeCutover.cjs`
- `tests/suites/go-kernel-v0662-go-window-pane-lookup-facade-cutover.cjs`

## Validation Scope

The v0.6.62 guard suite verifies:

- `windowExists(target, signal)` delegates to the cancellable async Go window pane lookup seam and returns `true` only on successful lookup.
- `firstPaneInWindow(target, signal)` delegates to the same seam and returns the first compact pane id or `null`.
- the direct TypeScript async target-based `list-panes -t` fallback is removed from both facades.
- success, helper failure, command failure, empty target, unsafe target, empty pane list, invalid response, pre-aborted signal, and in-flight abort preserve fail-closed public behavior.
- async helper diagnostics remain compact and do not leak raw stdout, stderr, cwd, stack traces, state archives, mailbox/report bodies, worker transcripts, or full helper paths.
- Go source contains the explicit target-based `list-panes -t` only for `workerLifecycle.listPanesInWindow` and does not add target-based `display-message` or mutating tmux commands.
- `listAgentTeamPanes()` remains label-filtered and global `list-panes -a` only.
- package version, native path/name, and release surfaces remain unchanged.
