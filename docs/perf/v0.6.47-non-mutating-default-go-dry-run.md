# v0.6.47 Non-Mutating Default-Go Dry-Run

> Scope: real reviewer-only default-Go dry-run runtime/verifier path for the selected `tmuxSnapshotParse` module. This does not enable default Go, does not enable the production default resolver, does not delete the TypeScript fallback, and does not approve package/native/release work.

Result: v0.6.47 implements a real non-mutating default-Go dry-run runtime/verifier path for `tmuxSnapshotParse`.

Final result remains `ready:false`.

## Implemented Path

- CLI: `scripts/verify-v0647-default-go-dry-run.cjs`.
- Harness: `scripts/lib/v0647-default-go-dry-run-harness.cjs`.
- Fixture contract: `tests/fixtures/kernel/v0647/defaultGoDryRunContract.cjs`.
- Guard suite: `tests/suites/go-kernel-v0647-non-mutating-default-go-dry-run.cjs`.

The verifier builds a reviewer helper into an OS temp root, creates a clean package-preview/default-layout fixture under the same temp root, exercises the existing packaged manifest resolver as a future default resolver simulation, and routes `tmuxSnapshotParse` through the Go helper using the existing explicit preview adapter seam.

It returns sanitized structured summary fields including `wouldUseGoForTmuxSnapshotParse:true`, `defaultBehaviorChanged:false`, `fallbackDeleted:false`, and `goAuthority:"tmuxSnapshotParse-parser-only"`.

## Runtime Invariants

- default/unset remains disabled/TypeScript.
- `go-cutover` remains explicit helper-path only.
- `go-packaged-preview` remains explicit preview only and non-default.
- The TypeScript parser fallback remains present.
- `compactReadModelFingerprint` remains TypeScript fallback / non-cutover under cutover/preview paths.
- Production `core/kernel.ts`, `tmux/snapshot.ts`, package metadata, package files, and runtime defaults are not changed by this slice.

## Real Checks

The harness performs these real checks when run on a host with Go available:

- direct helper health smoke with protocol `1`, helper version `0.3.0-read-model-shadow`, required capabilities, and `businessPathsConnected:false`.
- direct helper `tmuxSnapshotParse` smoke against deterministic compact tmux snapshot text.
- explicit `go-cutover` adapter smoke proving the helper can own only parser output when explicitly selected.
- future default resolver dry-run against a clean temp package-preview fixture using manifest/version/protocol/capability checks.
- canonical `tmuxSnapshotParse` parity corpus run through the direct helper and future-default dry-run adapter.
- missing, corrupt, wrong-version, unsupported-platform, and malformed helper cases fail closed without TypeScript parser fallback callback.

## Authority Boundary

Go authority remains parser-only for `tmuxSnapshotParse`:

- Go may parse TypeScript-captured tmux snapshot text.
- Go may return `TmuxSnapshot`-compatible compact pane snapshot output.
- Go may participate in this non-mutating reviewer dry-run verifier.
- Go must not execute tmux, capture panes, own pane/session/worker lifecycle, write state, govern task/report/PlanRun, read full-text mailbox/report bodies, render UI, manage package/release behavior, open network listeners, or become a daemon scheduler.

## No-Leak And Package Invariants

The verifier summary is sanitized only:

- no raw smoke logs, raw timing JSON, raw tmux stdout/stderr, raw state archives, full mailbox/report bodies, worker transcripts, terminal logs, absolute helper paths, or repository paths.
- No raw smoke logs, raw timing JSON, native binaries, tarballs, release assets, lockfiles, `go.mod`, or `go.sum` are introduced.
- temp helper/package-preview files are created under `/tmp` only and cleaned after the run.
- `package.json` version remains `0.6.8`; native metadata, optional dependencies, lifecycle hooks, npm publish/version scripts, and native install/download scripts remain absent.

## Explicit STOP Items

- NO-GO for actual default Go enablement.
- NO-GO for actual default resolver enablement.
- NO-GO for TypeScript fallback deletion.
- NO-GO for package/native release work.
- NO-GO for v0.7 release-ready approval.
- Do not create tags, GitHub releases, release assets, native packages, `npm version`, or `npm publish` from this evidence.

## Validation

Recommended focused validation:

```text
node --check scripts/lib/v0647-default-go-dry-run-harness.cjs
node --check scripts/verify-v0647-default-go-dry-run.cjs
node --check tests/fixtures/kernel/v0647/defaultGoDryRunContract.cjs
node --check tests/suites/go-kernel-v0647-non-mutating-default-go-dry-run.cjs
node scripts/verify-v0647-default-go-dry-run.cjs --json
node tests/run.cjs go-kernel-v0647-non-mutating-default-go-dry-run
npm test
npm run typecheck
npm run -s check:boundaries
git diff --check
```

Use v0.6.47 as implementation evidence for the non-mutating default-Go dry-run path only. Actual default-Go approval remains blocked on explicit leader/user approval, default resolver normal-user availability, package-manager native delivery, rollback/default-disable rehearsal, broad validation, manual RC implications, and release governance.
