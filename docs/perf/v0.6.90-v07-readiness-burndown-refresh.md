# v0.6.90 v0.7 Readiness Burn-down Refresh

Result: v0.6.90 is a docs/tests-only readiness inventory checkpoint after the narrow Go tmux/worker-lifecycle migration line.

Release target remains exactly: `v0.7.0 = core refactor + performance baseline + bug burn-down release`.

Current status: `not-release-ready`. Machine-readable status: `not-release-ready-readiness-inventory-only`.

v0.7.0 remains not release-ready until the remaining readiness evidence below is completed, reviewed, and reconciled. This checkpoint does not perform a release, create a tag, publish to npm, change runtime behavior, rebuild native artifacts, or approve package/release control-plane work.

`package.json` remains `0.6.8`.

Machine-checkable copy:

- `tests/fixtures/kernel/v0690/v07ReadinessBurndownRefresh.cjs`
- `tests/suites/go-kernel-v0690-v07-readiness-burndown-refresh.cjs`

## Accepted Current Head

Recent accepted state now includes:

- v0.6.88 `clearPaneLabelSync` cutover is accepted; evidence: `docs/perf/v0.6.88-go-clear-pane-label-sync-cutover.md`.
- v0.6.89 worker delivery boundary is accepted; evidence: `docs/perf/v0.6.89-go-worker-delivery-boundary-gate.md`.
- worker delivery remains bridge-only TypeScript-owned outbox/bridge orchestration.
- Go `send-keys` / active `wakePane` remain unauthorized.
- TypeScript/pi remains facade/compliance boundary; Go owns only explicit helper/kernel slices.

This means the narrow Go tmux/worker-lifecycle line has current evidence through v0.6.89, including v0.6.88 clear-pane-label-sync runtime reuse and the v0.6.89 bridge-only worker delivery boundary. It does not mean v0.7 release evidence is complete.

## Remaining v0.7 Gates

The following are still required before any v0.7 release decision:

1. **Clean-temp `PI_AGENTTEAM_HOME` p95 evidence refresh** — rerun the relevant p95 evidence in an isolated temp home, record environment metadata, and preserve sanitized summaries/checksums only.
2. **Clean-temp manual RC/operator evidence** — run the manual/operator RC in an isolated temp home, with compact pass/fail/blocked observations and cleanup proof.
3. **Bug burn-down ledger: P0/P1/P2 or equivalent, with deferred/non-goal classification** — classify remaining bugs and explicitly distinguish blockers from deferred/non-goals.
4. **Release checklist and governance review** — review package, tag, npm, release asset, rollback, native/default resolver, and release ownership decisions.
5. **Evidence reconciliation so docs/tests/roadmap/actual behavior agree** — reconcile current docs, machine guards, roadmap entries, and actual repository/runtime behavior.
6. **No raw artifact check-ins** — keep raw evidence out of the repo while preserving sanitized reviewer-facing summaries.

No raw p95/manual RC run is performed in this slice. This is readiness inventory/contract only.

## Raw Artifact / No-Leak Policy

Do not check in raw benchmark JSON, raw smoke logs, terminal logs, screenshots, raw mailbox/report bodies, worker transcripts, state archives, secrets, raw hosted records, tarballs, or raw p95/manual RC output.

Evidence docs may reference clean temp `/tmp/...` paths or checksums only when appropriate and only in sanitized form. This v0.6.90 checkpoint does not claim a fresh manual RC or p95 run.

## Still Unauthorized / Blocked

The following remain out of scope and unauthorized here:

- runtime/source behavior changes;
- Go source changes;
- native rebuild, native metadata refresh, native helper path rename, or binary rename;
- tmux runtime changes;
- state/task/PlanRun/mailbox/governance migration;
- team panel/UI migration;
- release/package control-plane runtime migration;
- default resolver/package/native delivery changes;
- `npm version`, `npm publish`, tags, GitHub releases, or release assets;
- lockfiles, `go.mod`, `go.sum`, lifecycle hooks, postinstall/download/native dependency flow;
- Go `send-keys`, active Go `wakePane`, or terminal transport revival.

state/task/PlanRun/mailbox/governance, team panel/UI, and release/package control-plane remain blocked without future design gates. Terminal/tmux worker wake/send-keys also remains blocked without a separate explicit design gate.

## Package / Native Invariants

- `package.json` version remains `0.6.8`.
- Existing native helper root remains `native/tmuxSnapshotParse/0.3.0-read-model-shadow/linux-x64-glibc`.
- Existing native helper binary remains `agentteam-tmuxSnapshotParse`.
- No native `workerLifecycleWakePane` smoke key is introduced.
- No native rebuild or metadata refresh is performed.

## Reviewer Interpretation

Use this checkpoint as the current v0.7 readiness burn-down inventory after v0.6.89. It is useful for reviewer alignment on what is accepted, what remains, what is blocked, and what raw evidence must stay out of repo.

Do not interpret this checkpoint as release approval, npm publication approval, tag approval, package/native approval, or runtime migration approval.

## Validation Plan

- `node --check tests/fixtures/kernel/v0690/v07ReadinessBurndownRefresh.cjs`
- `node --check tests/suites/go-kernel-v0690-v07-readiness-burndown-refresh.cjs`
- `node tests/run.cjs go-kernel-v0690-v07-readiness-burndown-refresh`
- focused regression: v0.6.89, v0.6.88, v0.6.87, v0.6.86 as practical
- `npm test`
- `npm run typecheck`
- `npm run -s check:boundaries`
- `git diff --check`
- `node -p "require('./package.json').version"`
- `sha256sum -c native/tmuxSnapshotParse/0.3.0-read-model-shadow/linux-x64-glibc/SHA256SUMS`
- status/diff guard confirming only docs/tests/.gitignore/roadmap changes
