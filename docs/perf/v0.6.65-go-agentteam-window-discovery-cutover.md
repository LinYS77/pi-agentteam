# v0.6.65 Go AgentTeam Window Discovery Cutover

Result: v0.6.65 cuts over `tmux/windows.ts` internal `findAgentTeamWindowTarget(sessionName, signal)` from direct TypeScript `list-windows` parsing to a narrow Go-backed `workerLifecycle.findAgentTeamWindowTarget` operation through the cancellable async kernel adapter seam.

This slice removes only the read-only marked-window discovery call. It does not move session/window creation, marking, labels, pane setup, or any mutating lifecycle behavior into Go:

```text
tmux/windows.ts findAgentTeamWindowTarget(sessionName, signal)
  -> createAgentTeamKernelAdapter().findAgentTeamWindowTargetAsync(sessionName, signal)
  -> workerLifecycle.findAgentTeamWindowTarget
  -> target | null
```

## Runtime Behavior

- `tmux/windows.ts` `findAgentTeamWindowTarget(sessionName, signal)` now delegates to `createAgentTeamKernelAdapter().findAgentTeamWindowTargetAsync(sessionName, signal)`.
- The TypeScript `runTmuxNoThrowAsync(['list-windows', '-t', sessionName, '-F', '#{window_id}\t#{@agentteam-window}'], undefined, signal)` fallback is removed from the discovery helper.
- Go uses exactly `tmux list-windows -t <sessionName> -F workerLifecycleAgentTeamWindowFormat` with compact format `#{window_id}\t#{@agentteam-window}` for `workerLifecycle.findAgentTeamWindowTarget`.
- A marked agentteam window returns `${sessionName}:${windowId}` as before.
- No marked window, missing session, helper failure, invalid response, empty session name, pre-aborted signal, and in-flight abort fail closed to `null` so TypeScript-owned creation remains in place.
- Public behavior of `ensureSwarmWindow(preferred?, signal?)` is preserved from the caller perspective.
- Raw stdout, raw stderr, cwd, stack, helper paths, mailbox/report bodies, worker transcript bodies, and full paths are not exposed.

## Preserved Boundaries

- `new-session`, `new-window`, list-panes during pane setup, display-message current target fallbacks, marking, labels, kill, state/task/UI/release/package remain TypeScript-owned; `has-session` is superseded by the v0.6.66 `sessionExists` cutover.
- `markWindowAsAgentTeam()` and `refreshWindowPaneLabels()` remain TypeScript-owned.
- `createTeammatePane()`, pane splitting, pane labels, `wakePane()`, and `killPane()` remain TypeScript-owned.
- `waitForPaneAppStart()` remains the v0.6.64 Go-backed inspect polling facade.
- `ensureTmuxAvailable()` remains the v0.6.63 Go-backed tmux availability facade.
- The native helper path and binary name remain unchanged.

Because Go source changes, the existing embedded helper is rebuilt in the same approved path with refreshed manifest, checksums, provenance, and placeholder attestation.

`package.json` remains `0.6.8`.

## Guard Evidence

The v0.6.65 guard suite verifies:

- `findAgentTeamWindowTarget()` delegates to the cancellable async Go operation.
- the direct TypeScript marked-window `list-windows` fallback and stdout parsing are removed from the discovery helper.
- marked window success, no-marked-window failure, missing session, invalid session, invalid response, helper failure, pre-aborted signal, and in-flight abort preserve fail-closed behavior.
- `ensureSwarmWindow()` still owns `new-session`, `new-window`, post-creation window lookup, pane setup list-panes, display-message current target fallback, marking, and label refresh; the former `has-session` check is superseded by v0.6.66.
- Go command surface gained only the approved read-only `list-windows` command and no mutating tmux commands.
- package/native path/name/version/release guardrails hold.

Evidence files:

- `tests/fixtures/kernel/v0665/goAgentTeamWindowDiscoveryCutover.cjs`
- `tests/suites/go-kernel-v0665-go-agentteam-window-discovery-cutover.cjs`
