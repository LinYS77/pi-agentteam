# v0.6.51 Contract Constants And Artifact Naming Gate

Result: v0.6.51 adds a non-runtime structural gate for shared kernel/helper/native artifact contract constants.

This slice reduces drift before the next Go-owned runtime slices. It does not change runtime behavior from v0.6.50.

## Contract Source

`core/kernelContract.ts` is the TypeScript source of truth for package/helper/protocol/capability/native artifact constants.

The guarded contract includes:

- package name/version: `pi-agentteam` / `0.6.8`.
- helper and adapter version: `0.3.0-read-model-shadow`.
- protocol version: `1`.
- required capabilities: `health`, `profile`, `tmuxSnapshotParse`, `tmuxSnapshotCapture`, `compactReadModelFingerprint`.
- `businessPathsConnected:false`.
- current native module: `tmuxSnapshotParse`.
- current capture capability: `tmuxSnapshotCapture`.
- current embedded native root: `native/tmuxSnapshotParse/0.3.0-read-model-shadow/linux-x64-glibc`.
- approved embedded native files under the current root.

`core/kernel.ts` and `core/kernelPackagedResolver.ts` now consume those constants rather than duplicating their own package/helper/protocol/capability values.

## Artifact Naming Decision

The current embedded runtime path remains `native/tmuxSnapshotParse/0.3.0-read-model-shadow/linux-x64-glibc/agentteam-tmuxSnapshotParse`.

The artifact naming decision status is `deferred-current-path-guarded`.

Options documented for a future rename decision:

- keep current `tmuxSnapshotParse` module/path for backward-compatible package layout.
- rename to broader `agentteamKernel`.
- rename to broader `agentteamControlPlaneCore`.

No native module path or binary rename is approved in this slice. A future rename must be a separate migration gate with package file, manifest, resolver, builder, clean-install, rollback, and no-leak coverage.

## Preserved Boundaries

Runtime behavior stays unchanged from v0.6.50.

This slice does not migrate:

- worker lifecycle.
- state repository.
- task/report/PlanRun governance.
- team panel view-model or `/team` TUI shell.
- package/release verification helpers.

TypeScript/pi remains the public facade for `index.ts`, command/tool schemas, hooks, renderers, prompts, diagnostics shaping, and TUI integration.

No hidden TypeScript fallback is restored for `tmuxSnapshotParse` or `tmuxSnapshotCapture`.

## Package And Release Guards

No package/release action is authorized in this slice:

- no `npm version`.
- no `npm publish`.
- no tag or GitHub release.
- no GitHub release assets.
- no package lockfiles.
- no `go.mod` or `go.sum`.
- no lifecycle hooks, postinstall downloads, or runtime `go build`.
- no native path rename.
- no binary rename.

`package.json` remains `0.6.8`.

## Evidence

- `core/kernelContract.ts`
- `tests/fixtures/kernel/v0651/kernelContractArtifactNamingGate.cjs`
- `tests/suites/go-kernel-v0651-contract-artifact-naming-gate.cjs`
- `native/tmuxSnapshotParse/0.3.0-read-model-shadow/linux-x64-glibc/manifest.json`
- `scripts/lib/go-helper-artifact-builder.cjs`

## Validation Scope

Focused guard:

```text
node tests/run.cjs go-kernel-v0651-contract-artifact-naming-gate
```

The v0.6.51 guard verifies:

- central TS contract values match runtime exports.
- package files include only the approved embedded native files.
- manifest, Go source, resolver, and builder stay aligned with the guarded contract.
- current `tmuxSnapshotParse` artifact path remains guarded while broader naming options stay decision-only.
- package version and release surfaces remain unchanged.
- no runtime migration beyond v0.6.50 is claimed or implemented.
