const GO_PANE_LABEL_SETTING_GATE_SCHEMA_VERSION = 1
const GO_PANE_LABEL_SETTING_GATE_THEME = 'v0.6.75 Go pane label setting gate'
const PACKAGE_VERSION = '0.6.8'
const HELPER_VERSION = '0.3.0-read-model-shadow'
const PROTOCOL_VERSION = 1
const CAPABILITY = 'workerLifecycle'
const FUTURE_OPERATION = 'setPaneLabel'
const HELPER_NAME = 'setPaneLabel'
const ORCHESTRATOR_NAME = 'syncPaneLabelsForTeam'
const CLEAR_HELPER_NAME = 'clearPaneLabel'
const RUNTIME_FILE = 'tmux/labels.ts'
const CONTRACT_STATUS = 'gate-only-no-runtime-mutation'
const ACTIVE_OPERATIONS = Object.freeze(['inspectPane', 'listAgentTeamPanes', 'captureCurrentPaneBinding', 'listPanesInWindow', 'findAgentTeamWindowTarget', 'findWindowTargetByName', 'sessionExists', 'markWindowAsAgentTeam', 'refreshWindowPaneLabels'])
const ACTIVE_CAPABILITIES = Object.freeze(['health', 'profile', 'tmuxSnapshotParse', 'tmuxSnapshotCapture', 'compactReadModelFingerprint', 'workerLifecycle', 'tmuxAvailability'])
const CURRENT_SET_PANE_LABEL_COMMAND_SURFACE = Object.freeze([
  Object.freeze({
    rendered: 'tmux set-option -p -t <paneId> @agentteam-name <label>',
    runTmuxNoThrowAsyncCall: "runTmuxNoThrowAsync(['set-option', '-p', '-t', paneId, '@agentteam-name', label], undefined, signal)",
    command: 'set-option',
    scope: 'pane',
    option: '@agentteam-name',
    value: '<label>',
    labelSource: 'formatMemberPaneLabel(member) or formatLeaderPaneLabel(team)',
  }),
  Object.freeze({
    rendered: 'tmux select-pane -t <paneId> -T <label>',
    runTmuxNoThrowAsyncCall: "runTmuxNoThrowAsync(['select-pane', '-t', paneId, '-T', label], undefined, signal)",
    command: 'select-pane',
    scope: 'pane',
    option: '-T',
    value: '<label>',
    labelSource: 'formatMemberPaneLabel(member) or formatLeaderPaneLabel(team)',
  }),
])
const AUTHORIZED_FUTURE_TMUX_COMMANDS = Object.freeze([
  Object.freeze({
    rendered: 'tmux set-option -p -t <paneId> @agentteam-name <label>',
    args: Object.freeze(['set-option', '-p', '-t', '<paneId>', '@agentteam-name', '<label>']),
    command: 'set-option',
    scope: 'pane',
    option: '@agentteam-name',
    value: '<opaque label argv>',
    labelArgumentIndex: 5,
    labelHandling: 'opaque unicode argv; never shell text',
    destructive: false,
    mutatesTmux: true,
  }),
  Object.freeze({
    rendered: 'tmux select-pane -t <paneId> -T <label>',
    args: Object.freeze(['select-pane', '-t', '<paneId>', '-T', '<label>']),
    command: 'select-pane',
    scope: 'pane',
    option: '-T',
    value: '<opaque label argv>',
    labelArgumentIndex: 4,
    labelHandling: 'opaque unicode argv; never shell text',
    destructive: false,
    mutatesTmux: true,
  }),
])
const CURRENT_CLEAR_PANE_LABEL_COMMAND_SURFACE = Object.freeze([
  Object.freeze({
    rendered: 'tmux set-option -up -t <paneId> @agentteam-name',
    runTmuxNoThrowAsyncCall: "runTmuxNoThrowAsync(['set-option', '-up', '-t', paneId, '@agentteam-name'], undefined, signal)",
    command: 'set-option',
    scope: 'pane-clear',
    option: '@agentteam-name',
    authorizedByThisGate: false,
  }),
  Object.freeze({
    rendered: "tmux select-pane -t <paneId> -T ''",
    runTmuxNoThrowAsyncCall: "runTmuxNoThrowAsync(['select-pane', '-t', paneId, '-T', ''], undefined, signal)",
    command: 'select-pane',
    scope: 'pane-title-clear',
    option: '-T',
    authorizedByThisGate: false,
  }),
])
const EXISTING_GO_MUTATING_TMUX_COMMANDS = Object.freeze([
  Object.freeze({
    rendered: 'tmux set-option -w -t <target> automatic-rename off',
    args: Object.freeze(['set-option', '-w', '-t', '<target>', 'automatic-rename', 'off']),
    operation: 'markWindowAsAgentTeam',
    scope: 'window',
  }),
  Object.freeze({
    rendered: 'tmux set-option -w -t <target> allow-rename off',
    args: Object.freeze(['set-option', '-w', '-t', '<target>', 'allow-rename', 'off']),
    operation: 'markWindowAsAgentTeam',
    scope: 'window',
  }),
  Object.freeze({
    rendered: 'tmux set-option -w -t <target> @agentteam-window 1',
    args: Object.freeze(['set-option', '-w', '-t', '<target>', '@agentteam-window', '1']),
    operation: 'markWindowAsAgentTeam',
    scope: 'window',
  }),
  Object.freeze({
    rendered: 'tmux set-option -w -t <target> pane-border-status top',
    args: Object.freeze(['set-option', '-w', '-t', '<target>', 'pane-border-status', 'top']),
    operation: 'refreshWindowPaneLabels',
    scope: 'window',
  }),
  Object.freeze({
    rendered: "tmux set-option -w -t <target> pane-border-format '#{?@agentteam-name,#{@agentteam-name},#{pane_title}}'",
    args: Object.freeze(['set-option', '-w', '-t', '<target>', 'pane-border-format', '#{?@agentteam-name,#{@agentteam-name},#{pane_title}}']),
    operation: 'refreshWindowPaneLabels',
    scope: 'window',
  }),
])
const FORBIDDEN_CURRENT_GO_TMUX_SNIPPETS = Object.freeze([
  'case "setPaneLabel"',
  'func setPaneLabel',
  'set-option", "-p"',
  'set-option", "-up"',
  'select-pane',
])
const FORBIDDEN_FUTURE_SCOPE = Object.freeze([
  'clearPaneLabel(paneId, signal)',
  'tmux set-option -up -t <paneId> @agentteam-name',
  "tmux select-pane -t <paneId> -T ''",
  'refreshWindowPaneLabels(target, signal) changes',
  'markWindowAsAgentTeam(target, signal) changes',
  'new-session',
  'new-window',
  'pane creation/split/layout/resize',
  'wake/send-keys',
  'kill-pane',
  'kill-window',
  'state repository',
  'task/report/PlanRun governance',
  'team panel view-model',
  'release/package verification',
  'native artifact path/name changes',
])
const FACADE_AUTHORITY = Object.freeze([
  'TypeScript/pi facade remains the public product and pi extension compliance boundary',
  'syncPaneLabelsForTeam remains TypeScript-owned orchestration',
  'setPaneLabel remains a private helper boundary unless a later cutover explicitly changes it',
  'Go runtime mutation may only be introduced by an explicit task-scoped contract',
  'Go may not own hidden fallback behavior for a facade after cutover',
])
const FUTURE_FACADE_RULE = Object.freeze({
  afterCutoverNoDirectTypescriptSetOptionFallback: true,
  afterCutoverNoDirectTypescriptSelectPaneFallback: true,
  forbiddenSetOptionFallbackPattern: "runTmuxNoThrowAsync(['set-option', '-p'",
  forbiddenSelectPaneFallbackPattern: "runTmuxNoThrowAsync(['select-pane', '-t', paneId, '-T', label]",
  hiddenTypeScriptFallbackAllowedAfterCutover: false,
  helperStillReturns: 'Promise<void>',
  publicOrchestrationStillTypescriptOwned: true,
})
const FUTURE_PUBLIC_BEHAVIOR = Object.freeze({
  noThrowVoidHelper: true,
  helperFailureBehavior: 'resolve void with compact internal diagnostics',
  invalidPaneBehavior: 'resolve void with compact internal diagnostics',
  invalidLabelBehavior: 'resolve void with compact internal diagnostics',
  abortBehavior: 'resolve void with compact internal diagnostics',
  tmuxFailureBehavior: 'resolve void with compact internal diagnostics',
  rawStdoutStderrLeakageAllowed: false,
  rawHelperOutputLeakageAllowed: false,
  rawLabelLeakageAllowed: false,
})
const FUTURE_INPUT_POLICY = Object.freeze({
  paneIdValidation: 'compact %123-style pane id or other explicitly approved pane target only',
  labelTreatment: 'opaque user-visible Unicode argv value',
  labelMayContainUnicode: true,
  labelMayContainEmoji: true,
  labelShellInterpolationAllowed: false,
  rawLabelDiagnosticsAllowed: false,
  reasonableLengthCapAllowed: true,
})
const RELEASE_PACKAGE_GUARDS = Object.freeze([
  'no npm version',
  'no npm publish',
  'no tag or release creation',
  'no GitHub release assets',
  'no package version bump',
  'no package lockfiles',
  'no go.mod or go.sum',
  'no lifecycle hooks or postinstall downloads',
  'no native artifact rename',
  'no Go source or native artifact rebuild in this gate',
])
const NATIVE_ARTIFACT_SNAPSHOT = Object.freeze({
  root: 'native/tmuxSnapshotParse/0.3.0-read-model-shadow/linux-x64-glibc',
  helperPath: 'native/tmuxSnapshotParse/0.3.0-read-model-shadow/linux-x64-glibc/agentteam-tmuxSnapshotParse',
  helperSha256: '2dd64cf7d7f3cc78351216afafc742fb17df3bd2d83bce0773f1b8a2f58fff6c',
  helperSize: 3475905,
  manifestSha256: 'd265bd909ea87d68656399002ecbf334ce4a5fea7c5558f465f7c8ba16bd1761',
  provenanceSha256: '9cf33444a1fd63604fcde0ada705c9478ecc78f84fbbb0a7b2b7500436926965',
  attestationSha256: '5f89b5db24028f89000a634de0e0998fe35d5f5e011ef784baecd62f7b64b8d2',
  sourceRevision: 'e341edd381812aff511fb6494734a544d8946872',
  setPaneLabelSmokePresent: false,
})
const goPaneLabelSettingGate = Object.freeze({
  schemaVersion: GO_PANE_LABEL_SETTING_GATE_SCHEMA_VERSION,
  theme: GO_PANE_LABEL_SETTING_GATE_THEME,
  packageVersion: PACKAGE_VERSION,
  helperVersion: HELPER_VERSION,
  protocolVersion: PROTOCOL_VERSION,
  capability: CAPABILITY,
  activeOperations: ACTIVE_OPERATIONS,
  activeCapabilities: ACTIVE_CAPABILITIES,
  contractStatus: CONTRACT_STATUS,
  futureOperation: FUTURE_OPERATION,
  helperName: HELPER_NAME,
  orchestratorName: ORCHESTRATOR_NAME,
  clearHelperName: CLEAR_HELPER_NAME,
  runtimeFile: RUNTIME_FILE,
  authorizedFutureMutatingCandidates: Object.freeze([FUTURE_OPERATION]),
  authorizedFutureTmuxCommands: AUTHORIZED_FUTURE_TMUX_COMMANDS,
  currentSetPaneLabelCommandSurface: CURRENT_SET_PANE_LABEL_COMMAND_SURFACE,
  currentClearPaneLabelCommandSurface: CURRENT_CLEAR_PANE_LABEL_COMMAND_SURFACE,
  existingGoMutatingTmuxCommands: EXISTING_GO_MUTATING_TMUX_COMMANDS,
  forbiddenCurrentGoTmuxSnippets: FORBIDDEN_CURRENT_GO_TMUX_SNIPPETS,
  forbiddenFutureScope: FORBIDDEN_FUTURE_SCOPE,
  facadeAuthority: FACADE_AUTHORITY,
  futureFacadeRule: FUTURE_FACADE_RULE,
  futurePublicBehavior: FUTURE_PUBLIC_BEHAVIOR,
  futureInputPolicy: FUTURE_INPUT_POLICY,
  releasePackageGuards: RELEASE_PACKAGE_GUARDS,
  nativeArtifactSnapshot: NATIVE_ARTIFACT_SNAPSHOT,
  gateOnly: true,
  noRuntimeMigrationInThisSlice: true,
  futureCandidateMutatesTmux: true,
  futureCandidateDestructive: false,
  setPaneLabelMigrated: false,
  clearPaneLabelMigrated: false,
  syncPaneLabelsMigrated: false,
  markWindowAsAgentTeamMigrated: true,
  refreshWindowPaneLabelsMigrated: true,
  newSessionMigrated: false,
  newWindowMigrated: false,
  createTeammatePaneMigrated: false,
  wakePaneMigrated: false,
  killPaneMigrated: false,
  stateRepositoryMigrated: false,
  taskReportPlanRunMigrated: false,
  teamPanelViewModelMigrated: false,
  releasePackageVerificationMigrated: false,
  nativeArtifactRenamed: false,
  nativeHelperRebuilt: false,
  goSourceChanged: false,
  packageVersionChanged: false,
  packageReleaseApproved: false,
  npmVersionChanged: false,
  npmPublished: false,
  tagReleaseCreated: false,
})

module.exports = {
  ACTIVE_CAPABILITIES,
  ACTIVE_OPERATIONS,
  AUTHORIZED_FUTURE_TMUX_COMMANDS,
  CAPABILITY,
  CLEAR_HELPER_NAME,
  CONTRACT_STATUS,
  CURRENT_CLEAR_PANE_LABEL_COMMAND_SURFACE,
  CURRENT_SET_PANE_LABEL_COMMAND_SURFACE,
  EXISTING_GO_MUTATING_TMUX_COMMANDS,
  FACADE_AUTHORITY,
  FORBIDDEN_CURRENT_GO_TMUX_SNIPPETS,
  FORBIDDEN_FUTURE_SCOPE,
  FUTURE_FACADE_RULE,
  FUTURE_INPUT_POLICY,
  FUTURE_OPERATION,
  FUTURE_PUBLIC_BEHAVIOR,
  GO_PANE_LABEL_SETTING_GATE_SCHEMA_VERSION,
  GO_PANE_LABEL_SETTING_GATE_THEME,
  HELPER_NAME,
  HELPER_VERSION,
  NATIVE_ARTIFACT_SNAPSHOT,
  ORCHESTRATOR_NAME,
  PACKAGE_VERSION,
  PROTOCOL_VERSION,
  RELEASE_PACKAGE_GUARDS,
  RUNTIME_FILE,
  goPaneLabelSettingGate,
}
